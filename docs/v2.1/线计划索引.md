# TinyUI v2.1 线计划索引

- 当前仓库硬规则：`AGENTS.md`
- 当前设计真相源：`docs/v2.1/2026-06-10-tinyui-v2-1-design.md`
- 当前 `TinyUI v2.1` 施工真相源：
  - `tinyui/src/core/*`
  - `tinyui/src/display/*`
  - `tinyui/src/indev/*`
  - `tinyui/src/layout/*`
  - `tinyui/src/theme/*`
  - `tinyui/src/tick/*`
  - `tinyui/src/widgets/*`
  - `tinyui/src/backend/ldgui/*`
  - `tinyui/include/picoui/*`
  - `tinyui/include/*`

## 当前机器真相源入口

- 当前 canonical 机器真相源入口：
  - `tests/tinyui/contract/check_tinyui_v21_transition_guards.py`
  - `tests/tinyui/contract/tinyui_v21_transition_inventory.json`
  - `tests/tinyui/contract/check_tinyui_transition_guards.py`
  - `tests/tinyui/contract/tinyui_transition_inventory.json`
- 当前仍保留的历史兼容/旧真相源入口：
  - `tests/picoui/contract/picoui_tinyui_transition_inventory.json` 当前只保留给历史文档引用，不在 live checker 执行链中
  - legacy 目录下与 canonical 完全一致的 `check_tinyui_*.py` 兼容壳与 `tinyui_{transition_inventory,v21_transition_inventory,release_capability_matrix}.json` 兼容副本已在 `V5` residue 清理批退场；当前这批旧入口仅用于历史兼容验证或文档记录，不再是主线 canonical truth。

## 当前基线清单

- `docs/v2.1/2026-06-10-tinyui-v2-1-baseline-inventory.md`

## 阅读顺序

1. `docs/v2.1/2026-06-10-tinyui-v2-1-design.md`
2. `docs/v2.1/plans/v2.1-orchestration-plan.md`
3. `docs/v2.1/plans/stages/README.md`
4. 按阶段顺序阅读并执行 `V0` 到 `V5`
5. 做 closeout / release 判断时，再回看：
   - `docs/v2.1/v2.1-closeout.md`
   - `docs/v2.1/v2.1-release-matrix.md`
   - `docs/v2.1/v2.1-performance-baseline.md`

## 固定顺序

固定顺序：

1. `V0` 基线与 rename 守门
2. `V1` 顶层目录与 public header 收口
3. `V2` backend 并回 widgets
4. `V3` shared layer 命名与 include path 收口
5. `V4` demo/test/contract/CMake 全量迁移
6. `V5` closeout 与 release 收口

## 当前进度

- 当前状态：`V0-V5` 已完成；当前 closeout / release 口径以 `docs/v2.1/{v2.1-closeout,v2.1-release-matrix,v2.1-performance-baseline}.md` 与 fresh broad gates 为准
- 已有事实：
  - `v2.1` design、orchestration 和 stages README 已存在
  - `v2.1` 阶段顺序已固定为 `V0 -> V1 -> V2 -> V3 -> V4 -> V5`
  - 顶层产品目录已统一到唯一 `tinyui/`
  - `v2.1` baseline inventory 与 `rename/backend/api` transition guard 已落地并通过 fresh gate
  - `tinyui/include/tinyui.h` 已是当前 canonical umbrella header；`tinyui/include/picoui/*` 兼容 include 子树仍暂留，留待后续命名清理阶段退场
  - `V4` 当前已完成第一批最小收口：`tests/picoui/CMakeLists.txt` 的 contract/runtime/perf CTest 注册已切到 `check_tinyui_*` 与 `tinyui` label，`examples/sdl/CMakeLists.txt` 的 demo helper 已切到 `add_tinyui_demo()`，`cmake/LingDongGUI.cmake` 已建立 `ld_apply_tinyui_runtime_screen_config()`
  - `V4` 当前又完成了 `contract/runtime/perf` 物理迁移：`tests/tinyui/{contract,runtime,perf}` 已承接 canonical checker 与 TinyUI 主 artifact，`tests/picoui/{contract,runtime,perf}` 当前只保留兼容薄壳/兼容副本
  - `V4` 当前又完成了 `unit` 物理迁移：`tests/tinyui/unit/*` 已承接 canonical unit tests，原 `tests/picoui/unit/test_picoui_*.c` 已迁到 `tests/tinyui/unit/test_tinyui_*.c`，`tests/picoui/unit/` 当前已清空；`tests/picoui/CMakeLists.txt` 现已直接引用 `../tinyui/unit/test_tinyui_*.c`
  - `V4` 当前又完成了 canonical contract 证据串收口与兼容壳瘦身：`tests/tinyui/contract/tinyui_release_capability_matrix.json` 已统一到 `test_tinyui_*` / `check_tinyui_*` / `tests/tinyui/runtime/*` 口径；`tests/picoui/{contract,perf,runtime}` 的剩余 `check_picoui_*` / `check_picoui_tinyui_*` 当前已进一步收紧为 thin wrapper
  - `V4` 当前又完成了 `tests/picoui/contract/*` 旧真相源的证据串归一化；同时 `native_100` inventory 的 canonical truth 当前已转到 `tests/tinyui/contract/*`
  - `V5` 当前又完成了一小批 native-100 truth 收口：`tests/tinyui/contract/check_tinyui_native_100_inventory.py` 与 `tests/tinyui/contract/tinyui_native_100_inventory.json` 已建立 canonical 副本，旧 `picoui_native_100_inventory.json` 当前只剩 legacy/docs 残留面
  - `V5` 当前又完成了一小批 release-matrix truth 收口：canonical exhaustiveness/release-matrix truth 已切到 `tests/tinyui/contract/*`，`tinyui/docs/*` 与 `docs/ability/*` 当前口径也已切到 canonical matrix；旧 `picoui_release_capability_matrix.json` 当前只剩 legacy/docs 残留面
- `V5` 当前又完成了一小批 canonical umbrella/header 收口：补齐了 `tinyui/include/{animation,arc,checkbox,clock,date_time,gauge,list,port,progress_bar,progress_wheel,qrcode,slider}.h` 顶层转发头，并把 `tinyui/include/tinyui.h` 扩成可承接当前 demo umbrella 的 canonical 入口；`tinyui/demo/{list_basic,settings_panel,graph_basic,message_box_basic,combo_box_basic,line_edit_basic,layout_grid,layout_flex}/main.c` 已切到 `tinyui.h`
- `V5` 当前又完成了一小批 unit/support umbrella 收口：`tests/support/picoui_test_support.h` 与剩余 `tests/tinyui/unit/*` umbrella consumer 已切到 `tinyui.h`；同时已补齐 `tinyui/include/port/sdl.h` 顶层转发头，`test_tinyui_port_sdl.c` 已切到 `tinyui.h + port/sdl.h`
- `V5` 当前又完成了一小批 direct widget header 收口：`tests/tinyui/unit/{test_tinyui_animation,test_tinyui_arc,test_tinyui_gauge,test_tinyui_list,test_tinyui_progress_wheel}.c` 已从 `picoui/<widget>.h` 切到顶层 `{animation,arc,gauge,list,progress_wheel}.h`
- `V5` 当前又完成了一小批 demo/port consumer 收口：`tinyui/demo/basic_widgets/main.c` 已从 `picoui/*` include 集合切到顶层 `{button,checkbox,image,layout,runtime,slider,switch,text,widget,window}.h`；`tinyui/port/sdl/sdl.c` 也已切到 `port/sdl.h`
- `V5` 当前又完成了 widgets 实现层第一批收口：`tinyui/src/widgets/{animation,background,button,calendar,checkbox,graph,image,label,progress_bar,text}.c` 已从 `picoui/<widget>.h` 切到顶层同名头
- `V5` 当前又完成了 widgets 实现层第二批收口：`tinyui/src/widgets/{clock,date_time,list,qrcode,window}.c` 已从 `picoui/*` include 集合切到顶层 `{clock,date_time,list,qrcode,widget,window}.h`
- `V5` 当前又完成了 widgets 实现层第三批收口：`tinyui/src/widgets/{arc,gauge,icon_slider,message_box,radial_menu}.c` 已从 `picoui/*` include 集合切到顶层 `{arc,gauge,icon_slider,message_box,radial_menu,widget}.h`
- `V5` 当前又完成了 widgets 实现层第四批收口：`tinyui/src/widgets/{canvas,combo_box,keyboard,line_edit,progress_wheel,scroll_selecter,slider,switch,table}.c` 已从 `picoui/*` include 集合切到顶层 `{canvas,combo_box,keyboard,line_edit,progress_wheel,scroll_selecter,slider,switch,table}.h`
- `V5` 当前又完成了 backend bridge include 收口第一步：`tinyui/src/backend/ldgui/backend.h` 已从 `picoui/{image,native,widget,window}.h` 切到顶层 `{image,native,widget,window}.h`，且 `check_tinyui_v21_transition_guards.py` baseline 已新增 `backend_compat_includes=[]` 守卫
- `V5` 当前又完成了 legacy widget-contract checker 退场：`tests/picoui/contract/check_picoui_widget_contract_matrix.py` 已删除；current live checker 只走 canonical `tests/tinyui/contract/check_tinyui_widget_contract_matrix.py`
- `V5` 当前又完成了 top-level wrapper baseline 钉住：`check_tinyui_v21_transition_guards.py` 已新增 `top_level_wrapper_forward_count=43` 与 `top_level_wrapper_forward_names=*` 守卫，机器真相已能直接报告剩余 compat wrapper 面
- `V5` 当前又完成了 native-api-exhaustiveness residue 清理：`tests/tinyui/contract/check_tinyui_native_api_exhaustiveness.py` 已建立 canonical 入口，旧 `check_picoui_native_api_exhaustiveness.py` 当前也已退场
- `V5` 当前又完成了 top-level wrapper 第一批真实迁出：`tinyui/include/qrcode.h` 已不再 forward 到 `picoui/qrcode.h`，且 transition baseline 已把剩余 compat wrapper 面从 `43` 收窄到 `42`
- `V5` 当前又完成了 top-level wrapper 第二批真实迁出：`tinyui/include/{app,display,indev,osal,tick,port}.h` 已不再 forward 到 `picoui/*`，且 transition baseline 已把剩余 compat wrapper 面从 `42` 收窄到 `36`
- `V5` 当前又完成了 top-level wrapper 第三批真实迁出：`tinyui/include/{background,arc,gauge,icon_slider,progress_wheel,radial_menu}.h` 已不再 forward 到 `picoui/*`，且 transition baseline 已把剩余 compat wrapper 面从 `36` 收窄到 `30`
- `V5` 当前又完成了 compat include chain rewiring：`tinyui/include/picoui/widget.h` 内部已从 `picoui/{layout,native,theme}.h` 切到顶层 `{layout,native,theme}.h`，为后续高耦合 `widget/layout/native/theme` 退场线铺路；当前 transition baseline 仍保持 `30`
- `V5` 当前又完成了 top-level wrapper 第四批真实迁出第一步：`tinyui/include/layout.h` 已不再 forward 到 `picoui/layout.h`，`tinyui/include/picoui/{widget,line_edit}.h` 也已改为显式走顶层 `../layout.h` / `../native.h` / `../theme.h`；transition baseline 已把剩余 compat wrapper 面从 `30` 收窄到 `29`
- `V5` 当前又完成了 top-level wrapper 第四批真实迁出第二步：`tinyui/include/{native,theme}.h` 已不再 forward 到 `picoui/{native,theme}.h`，`tinyui/include/picoui/{canvas,table,widget}.h` 当前也已改为显式走顶层 `../native.h` / `../theme.h`；transition baseline 已把剩余 compat wrapper 面从 `29` 收窄到 `27`
- `V5` 当前又完成了 top-level wrapper 第四批真实迁出第三步：`tinyui/include/{progress_bar,clock}.h` 已不再 forward 到 `picoui/{progress_bar,clock}.h`；transition baseline 已把剩余 compat wrapper 面从 `27` 收窄到 `25`
- `V5` 当前又完成了 top-level wrapper 第四批真实迁出第四步：`tinyui/include/{button,checkbox,label,text,graph}.h` 已不再 forward 到对应 `picoui/*.h`；transition baseline 已把剩余 compat wrapper 面从 `25` 收窄到 `20`，同时 `tinyui_public_api_count` 已随 canonical inline helper 真相从 `26` 刷新到 `28`
- `V5` 当前又完成了 top-level wrapper 第四批真实迁出第五步：`tinyui/include/window.h` 已不再 forward 到 `picoui/window.h`，且 `tinyui/include/picoui/{button,label,switch}.h` 已改为显式走顶层 `../window.h`；transition baseline 已把剩余 compat wrapper 面从 `20` 收窄到 `19`
- `V5` 当前又完成了 compat widget include chain rewiring：`tinyui/include/picoui/{animation,button,calendar,checkbox,combo_box,date_time,graph,image,keyboard,label,line_edit,list,message_box,progress_bar,runtime,scroll_selecter,slider,switch,table,text}.h` 已从 `picoui/widget.h` 切到顶层 `../widget.h`；这一步不减少 top-level wrapper 数量，但已清掉 `widget/obj/runtime` 退场线的大部分 compat 回流面，同时补齐了 `tinyui/include/button.h` 缺失的 `picoui_button_{set,get}_text_color` public 声明
- `V5` 当前又完成了 top-level wrapper 第四批真实迁出第六步：`tinyui/include/{widget,obj}.h` 已不再 forward 或直连 `picoui/widget.h`，分别改为 canonical public 声明与顶层 `widget.h` 复用；transition baseline 已把剩余 compat wrapper 面从 `19` 收窄到 `17`，当前 direct `picoui/widget.h`` 代码依赖已只剩 legacy umbrella `tinyui/include/picoui/picoui.h`
- `V5` 当前又完成了 legacy umbrella 第一小步收口：`tinyui/include/picoui/picoui.h` 已把 direct `widget/window` include 切到顶层 `../widget.h` 与 `../window.h`；当前主线代码面已无 direct `#include "picoui/widget.h"` / `#include "picoui/window.h"` 残留，下一步可直接转向 `runtime.h/screen.h`
- `V5` 当前又完成了 top-level wrapper 第四批真实迁出第七步：`tinyui/include/{runtime,screen,core}.h` 已不再 forward 或直连 `picoui/runtime.h`，分别改为 canonical runtime 声明与顶层 `runtime.h` 复用；transition baseline 已把剩余 compat wrapper 面从 `17` 收窄到 `14`，`tinyui_public_api_count` 当前也已从 `28` 刷新到 `30`
- `V5` 当前又完成了 legacy umbrella 第二小步收口：`tinyui/include/picoui/picoui.h` 已把最后一条 direct `runtime` 入口切到顶层 `../runtime.h`；当前主线代码面已无 direct `#include "picoui/{widget,window,runtime}.h"` 残留，下一步可直接转向剩余 `14` 个 top-level wrapper
- `V5` 当前又完成了剩余 wrapper 第一小批：`tinyui/include/animation.h` 已不再 forward 到 `picoui/animation.h`，transition baseline 已把剩余 compat wrapper 面从 `14` 收窄到 `13`；同批摸底也已确认 `image.h` 受 `picoui/canvas.h -> picoui/image.h` 组合面影响，需和 `canvas.h` 同批处理
- `V5` 当前又完成了剩余 wrapper 第二小批：`tinyui/include/{combo_box,date_time}.h` 已不再 forward 到 `picoui/{combo_box,date_time}.h`，transition baseline 已把剩余 compat wrapper 面从 `13` 收窄到 `11`；focused proof `test_tinyui_{combo_box,date_time,widgets}` 已通过，当前剩余边界保持不变：`image.h` 继续要求与 `canvas.h` 同批处理
- `V5` 当前又完成了剩余 wrapper 第三小批：`tinyui/include/{keyboard,line_edit,list}.h` 已不再 forward 到 `picoui/{keyboard,line_edit,list}.h`，transition baseline 已把剩余 compat wrapper 面从 `11` 收窄到 `8`；focused proof `test_tinyui_{keyboard,line_edit,list,widgets}` 已通过。当前下一步边界仍然清楚：`image.h` 仍需和 `canvas.h` 联批处理
- `V5` 当前又完成了剩余 wrapper 第四小批：`tinyui/include/{message_box,scroll_selecter,slider,table}.h` 已不再 forward 到 `picoui/{message_box,scroll_selecter,slider,table}.h`，transition baseline 已把剩余 compat wrapper 面从 `8` 收窄到 `4`；focused proof `test_tinyui_{message_box,scroll_selecter,slider,table,widgets}` 已通过。当前剩余 wrapper 已收窄到 `calendar.h / canvas.h / image.h / switch.h`
- `V5` 当前又完成了剩余 wrapper 第五小批：`tinyui/include/switch.h` 已不再 forward 到 `picoui/switch.h`，并保留 `tinyui_switch_{create,set_checked,is_checked,set_on_toggled}` helper；transition baseline 已把剩余 compat wrapper 面从 `4` 收窄到 `3`，`tinyui_public_api_count` 当前已从 `30` 刷新到 `31`。当前剩余 wrapper 已收窄到 `calendar.h / canvas.h / image.h`
- `V5` 当前又完成了 `canvas+image` 联批摸底：最小联动集合已经确认是 `tinyui/include/{canvas,image}.h + tinyui/include/picoui/canvas.h`；根因是 `tinyui.h -> canvas.h -> picoui/canvas.h -> picoui/image.h` 与 `tinyui.h -> image.h` 会在组合面重叠，若不同时改 `picoui/canvas.h`，会在 `picoui_image_source` / `picoui_image_props` / `picoui_font_*` 这组声明上重定义
- `V5` 当前又完成了 `canvas+image` 联批本体：`tinyui/include/{canvas,image}.h` 已不再 forward 到 `picoui/{canvas,image}.h`，且 `tinyui/include/picoui/canvas.h` 已改为走顶层 `../image.h`；transition baseline 已把剩余 compat wrapper 面从 `3` 收窄到 `1`。当前剩余 wrapper 已只剩 `calendar.h`
- `V5` 当前又完成了最后一个 top-level wrapper：`tinyui/include/calendar.h` 已不再 forward 到 `picoui/calendar.h`，`test_tinyui_calendar` 当前 fresh proof 已通过；`check_tinyui_v21_transition_guards.py` baseline 已把 `top_level_wrapper_forward_count` 从 `1` 收窄到 `0`。当前 top-level wrapper 已清零
- `V5` 当前又完成了 contract 真相源第一小批 wording 对齐：`tests/tinyui/contract/{native_api_gap_ledger.json,tinyui_release_capability_matrix.json}` 与 `tests/picoui/contract/native_api_gap_ledger.json` 已把 header evidence wording 切到 `tinyui/include/*.h`
- `V5` 当前又完成了 canonical transition guard baseline 刷新：`tests/tinyui/contract/tinyui_v21_transition_inventory.json` 已更新到 `tinyui_public_header_count=44`，`compat_public_headers_require_followup` 当前只剩 `{picoui.h,sdl.h}`
- `V5` 当前又完成了 canonical widget-contract checker 口径切换：`check_tinyui_widget_contract_matrix.py` 已切到 `tinyui/include/*.h` 根路径，并通过单层 wrapper resolve 继续解析承载声明的 `picoui/*.h` 真头
- `V5` 当前又完成了 canonical public-api checker 口径切换：`check_tinyui_public_api.py` 已切到 `tinyui/include/*.h` 根路径，并只扫描与 legacy compat surface 同名的顶层头，再通过单层 wrapper resolve 继续解析承载声明的 `picoui/*.h` 真头
  - `V5` 当前又完成了一小批 unit consumer 收口：`tests/tinyui/unit/{test_tinyui_clock,test_tinyui_date_time,test_tinyui_progress_bar,test_tinyui_qrcode}.c` 已切到顶层 `{clock,date_time,progress_bar,qrcode}.h`
  - `V4` 当前又完成了 include 消费面第一小批 safe move：`tinyui/port/sdl/sdl.c` 已切到顶层 `display.h`/`osal.h`/`tick.h`，`tinyui/demo/animation_basic/main.c` 已切到顶层 `image.h`；对应 configure/build proof 已 fresh 通过
  - `V4` 当前又完成了 canonical 头可组合性修复：`tinyui/include/{core,screen,label,button,switch}.h` 已去掉与 `tinyui/include/picoui/*` 兼容头重复的 `tinyui_*` inline/create 定义，`check_tinyui_v21_transition_guards.py` 已新增多头组合 `cc -fsyntax-only` probe；去重后 `tinyui_v21_transition_inventory.json` 的 `tinyui_public_api_count` 从重复计数 `34` 更新为当前实测 `26`，`tinyui_transition_inventory.json` 从重复计数 `29` 更新为当前实测 `21`
  - `V4` 当前又完成了 broad gate 修复：先执行 `rtk cmake --build build` 生成全部 `test_tinyui_*` 目标后，`rtk ctest --test-dir build -L 'tinyui' --output-on-failure` 已 fresh 通过 `56/56`；本批同时修复了 `test_tinyui_{gauge,image,graph,qrcode}` 与 `check_tinyui_release_capability_matrix` 暴露的路径/旧证据串问题
- 当前剩余重点：
  - `v2.1` 当前已无实现层未收口项；后续若继续推进，只应视作后续版本线或非本阶段目标，而不是 `v2.1` 未完成
  - 当前明确保留的 non-goal / allowed residue 以 `docs/v2.1/v2.1-release-matrix.md` 为准：release matrix 已允许的 `picoui_*` 过渡态 public C API、`tinyui/include/picoui/*` compatibility subtree、`tests/picoui/*` 与历史文档所需的冻结资产入口
  - `V5` 当前又补齐了 final residue scan 的解释边界：`rg -n "picoui" tinyui tests cmake docs/v2.1` 现在不能再按机械 `zero hits` 解读；release matrix 已明确允许继续存在的命中包括 `tinyui/include/picoui/*` compatibility subtree、`tests/picoui/*` 历史兼容/历史资产说明，以及仍处过渡态的 public C API / demo / runtime `picoui_*` 符号
  - `V5` 当前又完成了一轮上层文档口径纠偏：`2026-06-10-tinyui-v2-1-design.md` 与 `plans/v2.1-orchestration-plan.md` 已不再把 “public C API 全量改成 tinyui_*” 写成 `v2.1` 当前完成前提，而是与 `v2.1-release-matrix.md` 保持一致，明确这部分仍属过渡态
  - `V5` 当前又完成了一批 current-facing `tinyui/docs/*` 真相对齐：`tinyui/docs/{api_overview,demo_guide,porting_rules}.md` 已回到 `v2.1` 口径；其中 `runtime/mapping/visible` 当前统一指向 `tests/tinyui/runtime/*`，而 `manual artifact` 当前仍诚实保留为 `tests/picoui/runtime/check_picoui_manual_window_artifact.py` + `docs/picoui-serial/C-线人工窗口验收记录.md` 这条历史入口链
  - `V5` 当前又完成了一批 current-facing `docs/ability/*` 口径收口：`docs/ability/README.md` 与各控件页总述当前已统一改成 `TinyUI 当前覆盖` / `current public API` 口径，不再把 `picoui_*` 过渡态 public C 符号误写成整套能力文档的当前主叙事；表格中的 `picoui_*` 列名与符号值当前保留为真实过渡态证据
  - `V5` 当前又完成了一小批历史文档边界补注：`docs/v2.0/{2026-06-07-tinyui-v2-0-baseline-inventory,v2.0-release-matrix,v2.0-closeout,线计划索引}.md` 当前都已明确只对应 `v2.0` 阶段真相，不再允许被误读成当前 `v2.1` canonical truth
  - `V5` 当前又完成了一小批 `docs/picoui-serial/*` 历史线边界补注：`H-线发布测试矩阵.md` 与 `H-线计划索引.md` 当前都已明确只对应 PicoUI `H10/J7` 历史发布线，不再允许把其中的 `tests/picoui/*` / `check_picoui_*` / `picoui_release_capability_matrix.json` 误读成当前 `v2.1` canonical truth
  - `V5` 当前又完成了一小批 `docs/picoui-serial/a-0.x*` 历史线入口降级：`a-0.4/a-0.5/a-0.7~a-0.12/a-0.14` 索引里的“当前机器真相源入口 / 当前 final release 真相源入口”当前都已改成“本线历史真相源入口”，`a-0.6-final-release-closeout.md` 里的“当前真相源入口”也已明确只对应 `a-0.6` 历史 truth-source
  - `V5` 当前又完成了一小批 `a-0.3` 历史语境降级：`docs/superpowers/specs/plans` 里的 `a-0.3 truth-source / current-15` 目标语句当前已补齐“只对应 `2026-05-31 / a-0.3` 阶段语境”的边界说明，`docs/picoui-serial/a-0.3/README.md` 中“当前主仓 truth-source 入口”也已改成“`a-0.3` 当时主仓 truth-source 入口”
  - `V5` 当前又完成了一小批 `docs/superpowers/*` 历史语境收口：`a-0.3 / a-0.4 / a-0.5` 的 review/spec 里，几处会被误读成当前主线入口的 `current truth-source / 当前最新入口` 语句，现在都已限定回“当时该阶段/该 review 的 truth-source”语境
  - `V5` 当前又完成了一小批 `docs/superpowers/*` 零散语境收口：`2026-06-01-picoui-a-0-3-a-0-4-a-0-5-deep-review.md` 中的 `当前 machine truth-source / 当前 truth-source` 表述，和 `2026-06-01-picoui-a-0-6-full-release-design.md` 中的 “当前 truth-source 的边界”，现在都已限定回各自历史阶段语境
  - `V5` 当前又完成了一小批 `picoui-serial` 历史索引语境收口：`a-0.3/a-0.4/a-0.5` 索引中的 `当前 deep review` 已统一改成 `当时 deep review`，`a-0.4-a-0.6-后续版本记录.md` 与 `a-0.3-closeout-标准.md` 里悬空的 `current truth-source` 也已补上“当时阶段”锚点
  - `V5` 当前又完成了一小批 `closeout / review` 入口锚点补注：`a-0.3/a-0.4/a-0.5` 索引中的 `当前 closeout review` 已统一改成 `当时 closeout review`，`a-0.6` 设计/实现中的 `truth-source 入口` 也已限定为 `a-0.6` 当时语境
  - `V5` 当前又补完了 `docs/superpowers/reviews/2026-05-31-picoui-a-0-3-closeout-review.md` 最后两处入口型措辞：`a-0.3` 已明确只是在当时可作为后续 `a-0.4` 历史串行线输入，“推荐的文档与执行入口”也已限定回当时语境
  - `V5` 当前又完成了一小批历史 closeout 前状态锚点补注：`H-线发布closeout前状态.md`、`J-线v0.1-closeout前状态.md` 里的“当前最终发布结论”已限定回各自历史线语境，`2026-06-01-picoui-a-0-6-icon-slider-radial-menu-parity-repair.md` 的 `release truth-source` 也已限定为 `a-0.6` 当时语境
  - `V5` 当前又补齐了 release-facing truth 交付物：`docs/v2.1/v2.1-release-matrix.md` 与 `docs/v2.1/v2.1-performance-baseline.md` 已建立，并与 `docs/v2.1/v2.1-closeout.md` 一起构成当前 `v2.1` closeout/release 文档真相源
  - `tinyui/include/picoui/*` 兼容 public include 子树当前还被 `tinyui/include/*.h`、backend bridge 与 canonical contract checker 直接依赖；demo/unit/port/widgets 实现层 include 已基本收空，需继续迁 header/checker 面，再于 `V5` 退场
  - include consumer 面此前确认的 canonical 头多头组合 `tinyui_*` inline redefinition blocker 已完成最小修复；当前 canonical checker 口径切换已基本打通，后续重点转向 compatibility subtree 真正退场顺序与 legacy contract 资产清理
  - `V2` 的 `backend_c_files=0` 守门已经实测通过；backend 目录独立编译面已清零
  - `V3` 当前最小安全第一批已完成：`tinyui/src/{core,display,indev,layout,theme,tick,osal}` 已不再直接 `#include "picoui/..."`，对应 fail-first path scan 已清零；本批只新增 `tinyui/include/*.h` 顶层 wrapper 并改 shared 源码入口，不触碰 `tests/picoui/*`、`check_picoui_*`、`test_picoui_*`、CTest label 或 demo/contract 命名
  - `V3` 第一轮 focused compile proof 已 fresh 通过：`test_picoui_runtime_model`、`test_picoui_layout`、`test_picoui_theme`、`test_picoui_native_bridge`
  - `V3 Step 3` 的第一批 shared symbol naming 也已落下：`tinyui/src/core/runtime_bridge.{c,h}` 及其 shared/core 直系调用点里的 `picoui_runtime_bridge_*` internal/shared 接口现已统一改为 `tinyui_runtime_bridge_*`
  - 这一步只覆盖 internal/shared 接口及其直系源码/测试符号引用；public `picoui_*` API、`tests/picoui/*` 路径、`test_picoui_*` / `check_picoui_*` 文件名、CTest label 与 contract/demo 命名仍留待 `V4`
  - `V3 Step 3` 的第二批 internal-only helper naming 也已完成：`window_apply_*` 这组 shared layout helper 已从 `picoui_window_apply_*` 统一收口为 `tinyui_window_apply_*`，只改动 `window.c + layout/flex.c + layout/grid.c + core/widget.c + core/internal.h` 这一条 internal/shared 调用链
  - `V3 Step 3` 的第三批 internal-only helper naming 也已完成：`native_*` 这组 shared conversion helper 已从 `picoui_native_align_to_ld_grid()`、`picoui_native_nav_dir_to_ld()`、`picoui_native_signal_to_ld()`、`picoui_native_readback_policy_to_backend()` 统一收口为 `tinyui_native_*`，只改动 `core/internal.h + core/native.c + core/widget.c + widgets/window.c + widgets/switch.c + widgets/table.c + test_picoui_native_bridge` 这一条 internal/shared 调用链，仍未触碰 public `picoui_*` API、`tests/picoui/*` 路径、`test/check` 文件名或 CTest label
  - `V3 Step 3` 的第四批 internal-only helper naming 也已完成：`display/indev` 的文件内静态 helper 已从 `g_picoui_default_display_config`、`picoui_display_config_is_valid()`、`picoui_display_resolve_config()`、`picoui_input_key_is_valid()` 收口为 `tinyui_*` 命名，只改动 `display.c + indev.c + test_picoui_port_display + test_picoui_port_input` 这一条 internal/shared 调用链，public `picoui_display_*` / `picoui_input_*` API 不变
  - `V3 Step 3` 的第五批 internal-only bridge naming 也已完成：`runtime_bridge` 的 4 个 shared/internal seam 已从 `picoui_backend_widget_bind_host()`、`picoui_backend_widget_bind_ld_event_bridge()`、`picoui_backend_widget_unbind_host()`、`picoui_backend_widget_detach_from_parent()` 统一收口为 `tinyui_runtime_bridge_*`，只改动 `core/internal.h + core/runtime_bridge.c + widgets/*` 直系调用点，以及 `test_picoui_widgets/test_picoui_image/test_picoui_qrcode` 的 internal/shared 符号引用，仍未触碰 public widget API、`tests/picoui/*` 路径、`test/check` 文件名或 CTest label
  - `V3 Step 3` 的第六批 internal-only helper naming 也已完成：`theme.c` 里的 pure-static helper 已从 `picoui_theme_rgb_to_ld_color()`、`picoui_theme_state_is_valid()`、`picoui_theme_part_is_valid()`、`picoui_theme_part_supported()`、`picoui_theme_map_widget_colors()`、`picoui_theme_apply_widget_metrics()`、`picoui_theme_backend_can_apply_style()`、`picoui_theme_apply_{window,label,text,button,checkbox,switch,slider,list,image,calendar}_style()` 与 `picoui_theme_apply_native_widget_style()` 统一收口为 `tinyui_theme_*`，只改动 `theme.c + test_picoui_theme` 这一条 internal/shared 调用链，明确不触碰 `picoui_theme_apply_widget_style()`、public `picoui_theme_*` API 或 `picoui_app_set_theme()`
  - `V3 Step 3` 的第七批 internal-only helper naming 也已完成：`layout/flex.c` 与 `layout/grid.c` 里的 pure-static helper 已从 `picoui_window_get_backend()`、`picoui_window_is_valid()` 统一收口为 `tinyui_window_get_backend()`、`tinyui_window_is_valid()`，只改动 `layout/flex.c + layout/grid.c + test_picoui_layout` 这一条 internal/shared 调用链，明确不触碰 public `picoui_flex_*` / `picoui_grid_*` API、contract/demo 命名或 `tests/picoui/*` 路径
  - `V3 Step 3` 的第八批 internal-only helper naming 也已完成：`runtime_host.c` 里最前面的 pure-static/internal runtime helper 已从 `picoui_backend_touch_log_enabled()`、`picoui_backend_runtime_bootstrap()`、`picoui_backend_runtime_page_init()`、`picoui_backend_runtime_page_quit()`、`struct picoui_backend_runtime_state` 与 `g_picoui_backend_runtime_page` 收口为 `tinyui_runtime_host_*` / `struct tinyui_runtime_host_state` / `g_tinyui_runtime_host_page`，只改动 `core/runtime_host.c + test_picoui_app_lifecycle` 这一条 internal/shared 调用链，明确不触碰 `picoui_backend_step_app()`、`prepare/render` helper、public `picoui_*` API 或 runtime/test 资产命名
  - `V3 Step 3` 的第九批 internal-only helper naming 也已完成：`runtime_host.c` 里支撑 mapping/logging 判定的 pure-static helper 已从 `picoui_backend_widget_is_supported_real()`、`picoui_backend_widget_is_real_mapped()`、`picoui_backend_append_id()`、`picoui_backend_widget_needs_fallback()`、`picoui_backend_window_has_real_layout()`、`picoui_backend_widget_excludes_formal_mapping()`、`picoui_backend_widget_allows_smoke_layout()`、`picoui_backend_append_widget_ids()` 收口为 `tinyui_runtime_host_*`，只改动 `core/runtime_host.c + test_picoui_app_lifecycle` 这一条 internal/shared 调用链，明确不触碰 runtime marker 字符串、`prepare/render/step` helper、public `picoui_*` API 或 runtime/test 资产命名
  - `V3 Step 3` 的第十批 internal-only helper naming 也已完成：`runtime_host.c` 里支撑 capture/render 路径但尚未被现有 focused proof 锁名的 pure-static helper 已从 `picoui_backend_log_image_source_marker()`、`picoui_backend_parse_auto_quit_ms()`、`picoui_backend_pixel_to_rgb888()`、`picoui_backend_pixel_to_argb8888()`、`picoui_backend_runtime_state_from_app()`、`picoui_backend_app_state_from_window()`、`picoui_backend_present_real_frame()`、`picoui_backend_apply_real_widget_layout()` 收口为 `tinyui_runtime_host_*`，只改动 `core/runtime_host.c + test_picoui_app_lifecycle` 这一条 internal/shared 调用链，明确不触碰 runtime marker 字符串、`write_capture` / `apply_smoke_cursor_layout` / `render` / `prepare` / `step` helper、public `picoui_*` API 或 runtime/test 资产命名
  - `V3 Step 3` 的第十一批 internal-only helper naming 也已完成：`runtime_host.c` 里剩余的 internal runtime 主链 helper 已从 `picoui_backend_prepare_runtime_state()`、`picoui_backend_prepare_runtime_scene()`、`picoui_backend_log_runtime_ready()`、`picoui_backend_prepare_runtime()`、`picoui_backend_pump_sdl_events()`、`picoui_backend_step_app()` 统一收口为 `tinyui_runtime_host_*`，同步改动 `core/runtime_host.c + core/runtime_bridge.c + core/internal.h + test_picoui_app_lifecycle` 这一条 internal/shared 调用链，明确不触碰 runtime marker 字符串、public `picoui_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 contract/demo 命名
  - `runtime_host.c` 当前已不再保留任何 `picoui_backend_*` internal helper 前缀；`test_picoui_app_lifecycle` 现已改守 `tinyui_runtime_host_prepare_runtime_state()`、`tinyui_runtime_host_prepare_runtime_scene()`、`tinyui_runtime_host_log_runtime_ready()`、`tinyui_runtime_host_prepare_runtime()`、`tinyui_runtime_host_pump_sdl_events()` 与 `tinyui_runtime_host_step_app()` 这条 runtime 主链
  - `V3 Step 3` 的第十二批 internal-only helper naming 也已完成：`core/widget.c` 里的 pure-static/internal helper 已从 `picoui_widget_is_valid()`、`picoui_widget_get_ld_base()`、`picoui_widget_get_backend()`、`picoui_widget_expected_native_type()`、`picoui_widget_validate_native_binding()`、`picoui_backend_widget_can_attach_child()`、`picoui_backend_widget_clear_owner_and_root()`、`picoui_backend_widget_bind_subtree_owner_and_root()`、`picoui_backend_widget_get_host()` 与 `g_picoui_backend_next_data_model_identity` 收口为 `tinyui_*` / `g_tinyui_*`，只改动 `core/widget.c + test_picoui_widgets` 这一条 internal/shared 调用链，明确不触碰 `picoui_backend_widget_init_root()`、`picoui_backend_widget_init_child()`、`picoui_backend_widget_attach_child()`、`picoui_backend_widget_claim_focus()`、`picoui_backend_widget_release_focus()`、`picoui_backend_widget_is_kind()`、`picoui_widget_update_value()` 这些仍被 widgets/tests 当 internal entry 使用的非静态接口
  - `V3 Step 3` 当前又继续完成了 shared/core `host-binding seam`：`picoui_widget_bind_backend_host()`、`picoui_widget_backend_host()`、`picoui_widget_backend_detach()`、`picoui_widget_owner_app()`、`picoui_widget_has_ld_binding()` 已统一收口为 `tinyui_widget_*`，只改动 `core/internal.h + core/widget.c + core/runtime_bridge.c + widgets/list.c + test_picoui_widgets + test_picoui_button_events` 这一条 internal/shared 调用链，并同步切换多份只读 extern 测试引用
  - 这一步继续保持边界：本批只收 host-binding helper cluster，不触碰 `picoui_widget_update_value()`、`picoui_widget_dispatch_signal()`、`picoui_widget_dispatch_event()`、`picoui_widget_dispatch_native_signal()` 或 `picoui_backend_set_text()`；当前 `V3 Step 3` 仍应视为进行态
  - `V3 Step 3` 当前又继续完成了 shared/core `update-value seam`：`picoui_widget_update_value()` 已统一收口为 `tinyui_widget_update_value()`，只改动 `core/internal.h + core/widget.c + widgets/checkbox.c + widgets/switch.c + widgets/slider.c + test_picoui_widgets` 这一条 internal/shared 调用链
  - 这一步继续保持边界：本批只收 `update_value` 这一条 shared setter-helper，不触碰 `picoui_widget_dispatch_signal()`、`picoui_widget_dispatch_event()`、`picoui_widget_dispatch_native_signal()` 或 `picoui_backend_set_text()`
  - `V3 Step 3` 当前又继续完成了 shared/core `dispatch seam`：`picoui_widget_dispatch_signal()`、`picoui_widget_dispatch_event()`、`picoui_widget_dispatch_native_signal()` 已统一收口为 `tinyui_widget_dispatch_signal()`、`tinyui_widget_dispatch_event()`、`tinyui_widget_dispatch_native_signal()`，只改动 `core/internal.h + core/event.c + core/runtime_bridge.c + test_picoui_button_events + test_picoui_widgets + test_picoui_keyboard` 这一条 internal/shared 调用链
  - 这一步继续保持边界：本批只收 `dispatch_*` 这一组 shared dispatch helper，不触碰 public `picoui_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `V3 Step 3` 这轮又继续完成了 `progress_wheel` 的 internal/widget-local/test seam：`picoui_backend_progress_wheel_fail_next_set_percent`、`struct picoui_progress_wheel_cfg_bridge`、`picoui_progress_wheel_rgb_to_ld_color()`、`struct picoui_progress_wheel_test_dispose_snapshot`、`picoui_progress_wheel_last_dispose_snapshot*`、`picoui_progress_wheel_backend()`、`picoui_progress_wheel_get_ld()`、`picoui_progress_wheel_finish_detach_after_backend_failure()`、`picoui_progress_wheel_test_reset_internal_state()`、`picoui_backend_progress_wheel_test_{reset_state,take_last_dispose_snapshot,capture_dispose_snapshot,fail_next_set_percent}()`、`picoui_progress_wheel_test_finish_detach_after_backend_failure()`、`picoui_progress_wheel_disable_dirty_regions()`、`picoui_progress_wheel_dispose_partial_impl()` 与 `picoui_progress_wheel_props_are_valid()` 已统一收口为 `tinyui_progress_wheel_*`，只改动 `widgets/progress_wheel.c + test_picoui_progress_wheel` 这一条 internal/widget-local 调用链
  - 这一步继续保持边界：`progress_wheel` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_progress_wheel_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `progress_wheel` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_progress_wheel` 与 `rtk ctest --test-dir build -R '^test_picoui_progress_wheel$' --output-on-failure`
  - 到当前真相，`progress_wheel.c` 内部已不再残留 `picoui_progress_wheel_*` / `picoui_backend_progress_wheel_*` internal/test seam；下一自然顺序前移到其他仍保留 `picoui_*` widget-local seam 的控件批次，如 `switch` / `keyboard` / `table`
  - `V3 Step 3` 这轮又继续完成了 `switch` 的 internal/widget-local seam：`picoui_switch_backend()`、`picoui_switch_get_ld()`、`picoui_switch_nav_dir_to_ld()`、`picoui_switch_attach_native()`、`picoui_switch_props_are_valid()` 已统一收口为 `tinyui_switch_*`，只改动 `widgets/switch.c` 这一条 internal/widget-local 调用链
  - 这一步继续保持边界：`switch` 这轮只收 widget-local/internal helper 名称，不触碰 public `picoui_switch_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `switch` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_switch$' --output-on-failure`
  - 到当前真相，`switch.c` 这批 retained internal seam 已切到 `tinyui_switch_*`；下一自然顺序继续前移到仍保留较密集 `picoui_*` widget-local seam 的控件批次，如 `keyboard` / `table`
  - `V3 Step 3` 这轮又继续完成了 `keyboard` 的 internal/widget-local seam：`picoui_keyboard_free_layout()`、`picoui_keyboard_props_are_valid()`、`picoui_keyboard_get_selected_key_code_internal()`、`picoui_keyboard_get_ld_widget()`、`picoui_keyboard_get_target_line_edit_local()`、`picoui_keyboard_get_custom_button_list_local()`、`picoui_keyboard_prepare_local()` 已统一收口为 `tinyui_keyboard_*`，主要改动 `widgets/keyboard.c`，并兼容测试侧已存在的并行 `tinyui_widget_dispatch_native_signal()` 调整
  - 这一步继续保持边界：`keyboard` 这轮只收 widget-local/internal helper 名称，不触碰 public `picoui_keyboard_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `keyboard` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_keyboard$' --output-on-failure` 与 `rtk ctest --test-dir build/picoui-runtime -R '^test_picoui_keyboard$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `table` 的 internal/widget-local/test seam：`struct picoui_table_test_dispose_snapshot`、`picoui_table_last_dispose_snapshot*`、`picoui_backend_table_fail_next_set_keyboard_binding`、`picoui_table_dims_are_valid()`、`picoui_table_keyboard_binding_is_valid()`、`picoui_table_finish_detach_after_backend_failure()`、`picoui_table_dispose_partial()`、`picoui_table_props_are_valid()`、`picoui_table_get_ld_widget()`、`picoui_table_get_strict_ld_widget()`、`picoui_table_align_to_ld()`、`picoui_table_sync_current_cell_local()`、`picoui_table_native_slot()`、`picoui_table_bind_native_slot()` 以及对应测试 seam 已统一收口为 `tinyui_table_*`
  - 这一步继续保持边界：`table` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_table_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `table` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_table$' --output-on-failure`
  - 到当前真相，`keyboard/table` 这两条 retained internal seam 都已继续向 `tinyui_*` 收口；下一自然顺序可前移到剩余仍保留较密集 `picoui_*` widget-local seam 的批次
  - `V3 Step 3` 这轮又继续完成了 `checkbox` 的 internal/widget-local seam：`picoui_checkbox_fail_next_set_check_color`、`picoui_checkbox_rgb_to_ld_color()`、`picoui_checkbox_get_ld()`、`picoui_checkbox_props_are_valid()`、`picoui_checkbox_dispose_partial()`、`picoui_backend_checkbox_test_fail_next_set_check_color()` 已统一收口为 `tinyui_checkbox_*`
  - 这一步继续保持边界：`checkbox` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_checkbox_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `checkbox` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_checkbox$' --output-on-failure`
  - 到当前真相，`checkbox` 也已继续向 `tinyui_*` 收口；当前并行自然顺序已前移到 `combo_box` / `date_time`
  - `V3 Step 3` 这轮又继续完成了 `combo_box` 的 residual internal seam：`picoui_combo_box_props_are_valid()`、`picoui_combo_box_dispose_partial()` 已统一收口为 `tinyui_combo_box_*`，并在 `test_picoui_combo_box` 里补了源码扫描断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`combo_box` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_combo_box_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `combo_box` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_combo_box$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `date_time` 的 residual internal seam：`picoui_date_time_rgb_to_ld_color()`、`picoui_date_time_map_align()`、`picoui_date_time_get_ld()`、`picoui_date_time_props_are_valid()` 已统一收口为 `tinyui_date_time_*`，并在 `test_picoui_date_time` 里补了 focused 源码扫描断言，锁定旧 helper 名不再出现
  - 这一步继续保持边界：`date_time` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_date_time_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `date_time` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_date_time$' --output-on-failure`
  - 到当前真相，`combo_box/date_time` 这两条 retained internal seam 也已继续向 `tinyui_*` 收口；下一自然顺序继续前移到仍残留较密集 `picoui_*` widget-local seam 的控件批次
  - `V3 Step 3` 这轮又继续完成了 `line_edit` 的 residual internal seam：`picoui_line_edit_type_is_valid()`、`picoui_line_edit_keyboard_binding_is_valid()`、`picoui_line_edit_props_are_valid()`、`picoui_line_edit_dispose_partial()` 已统一收口为 `tinyui_line_edit_*`，并在 `test_picoui_line_edit` 里补了源码扫描与二进制符号缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`line_edit` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_line_edit_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `line_edit` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_line_edit$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `calendar` 的 residual internal seam：`picoui_calendar_props_are_valid()`、`picoui_calendar_sync_grid()`、`picoui_calendar_dispose_partial()` 已统一收口为 `tinyui_calendar_*`，并在 `test_picoui_calendar` 里补了源码扫描与二进制符号缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`calendar` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_calendar_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `calendar` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_calendar$' --output-on-failure`
  - 到当前真相，`calendar/line_edit` 这两条 retained internal seam 也已继续向 `tinyui_*` 收口；下一自然顺序继续前移到仍残留较密集 `picoui_*` widget-local seam 的控件批次
  - `V3 Step 3` 这轮又继续完成了 `progress_bar` 的 residual internal/test seam：`picoui_progress_bar_last_dispose_snapshot*`、`picoui_progress_bar_test_reset_internal_state()`、`picoui_progress_bar_rgb_to_ld_color()`、`picoui_progress_bar_backend()`、`picoui_progress_bar_get_ld()`、`picoui_progress_bar_finish_detach_after_backend_failure()`、`picoui_progress_bar_dispose_partial_impl()`、`picoui_progress_bar_props_are_valid()`、`picoui_progress_bar_create_with_props_impl()`、`picoui_backend_progress_bar_test_{dispose_partial,reset_state,take_last_dispose_snapshot,create_with_props_fail_before_inverted}()` 已统一收口为 `tinyui_progress_bar_*`，并在 `test_picoui_progress_bar` 里补了源码定义缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`progress_bar` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_progress_bar_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `progress_bar` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_progress_bar$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `gauge` 的 residual internal/test seam：`picoui_gauge_props_are_valid()`、`picoui_gauge_rgb_to_ld_color()`、`picoui_gauge_ld_color_to_rgb()`、`picoui_gauge_backend()`、`picoui_gauge_get_ld()`、`picoui_gauge_finish_detach_after_backend_failure()`、`picoui_gauge_dispose_partial_impl()`、`picoui_gauge_last_dispose_snapshot*`、`picoui_backend_gauge_test_{take_last_dispose_snapshot,create_with_props_fail_before_centre_offset}()` 已统一收口为 `tinyui_gauge_*`，并在 `test_picoui_gauge` 里补了源码定义缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`gauge` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_gauge_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `gauge` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_gauge$' --output-on-failure`
  - 到当前真相，`gauge/progress_bar` 这两条 retained internal seam 也已继续向 `tinyui_*` 收口；主线程随后又补掉了 `gauge.c` 里 2 个漏网旧调用点，把 `picoui_backend_gauge_get_angle()` 与 `picoui_backend_gauge_set_pointer_color()` 的内部取 ld 路径也统一切到 `tinyui_gauge_get_ld()`
  - `V3 Step 3` 这轮又继续完成了 `text` 的 residual internal seam：`picoui_text_dispose_partial()`、`picoui_text_props_are_valid()` 已统一收口为 `tinyui_text_*`，并在 `test_picoui_text` 里补了旧 seam 名不存在断言，同时把源码路径探测收敛到基于 `__FILE__` 的仓库根解析，避免 `ctest --test-dir build` 下误判
  - 这一步继续保持边界：`text` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_text_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `text` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_text` 与 `rtk ctest --test-dir build -R '^test_picoui_text$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `qrcode` 的 residual internal/test seam：`picoui_qrcode_props_are_valid()`、`picoui_qrcode_get_ld()`、`picoui_qrcode_finish_detach_after_backend_failure()`、`picoui_qrcode_dispose_partial_impl()`、`picoui_qrcode_create_with_props_impl()`，以及 `picoui_backend_qrcode_test_{reset_state,create_with_props_fail_before_text,take_last_dispose_snapshot}()` 与相关测试 snapshot/helper 名称，已统一收口为 `tinyui_qrcode_*`，并在 `test_picoui_qrcode` 里补了旧 seam 名不存在断言
  - 这一步继续保持边界：`qrcode` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_qrcode_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `qrcode` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_qrcode` 与 `rtk ctest --test-dir build -R '^test_picoui_qrcode$' --output-on-failure`
  - 到当前真相，`text/qrcode` 这两条 retained internal seam 也已继续向 `tinyui_*` 收口；主线程随后又补掉了 `qrcode.c` 里 1 个漏网旧调用点，把 `picoui_qrcode_set_zoom()` 的内部取 ld 路径统一切到 `tinyui_qrcode_get_ld()`；下一自然顺序继续前移到仍残留较密集 `picoui_*` widget-local seam 的控件批次
  - `V3 Step 3` 这轮又继续完成了 `button` 的 residual internal/test seam：`picoui_button_fail_next_set_font`、`picoui_button_get_ld()`、`picoui_button_default_font()`、`picoui_button_resolve_font()`、`picoui_button_dispose_partial()`、`picoui_backend_button_test_fail_next_set_font()`、`picoui_button_props_are_valid()` 已统一收口为 `tinyui_button_*`，并在 `test_picoui_button_events` 里补了源码定义缺失与二进制符号缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`button` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_button_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `button` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_button_events$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `slider` 的 residual internal/test seam：`picoui_slider_fail_indicator_width_id`、`picoui_slider_last_disposed_backend_snapshot`、`picoui_slider_last_disposed_backend_valid`、`picoui_slider_backend()`、`picoui_slider_get_ld()`、`picoui_slider_should_fail_indicator_width()`、`picoui_slider_dispose_partial()`、`picoui_slider_props_are_valid()`、`picoui_backend_slider_test_{fail_indicator_width_for_id,last_disposed_backend}()` 已统一收口为 `tinyui_slider_*`，并在 `test_picoui_slider` 里补了源码定义缺失断言，同时把源码路径探测收敛到基于 `__FILE__` 的仓库根解析
  - 这一步继续保持边界：`slider` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_slider_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `slider` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_slider$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `label` 的 residual internal seam：`picoui_backend_label_get_ld()`、`picoui_label_props_are_valid()`、`picoui_label_dispose_partial()` 已统一收口为 `tinyui_label_*`，并把 `label.c` 内部 shared text bridge 调用统一切到 `tinyui_widget_set_backend_text()`；`test_picoui_label` 里已补了源码存在/缺失断言与二进制符号缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`label` 这轮只收 widget-local/internal helper 名称，不触碰 public `picoui_label_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `label` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_label$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `image` 的 residual internal/test seam：`picoui_image_rgb_to_ld_color()`、`picoui_image_props_are_valid()`、`picoui_image_finish_detach_after_backend_failure()`、`picoui_image_dispose_partial_impl()`、`picoui_image_create_with_props_impl()`，以及 `picoui_backend_image_test_{reset_state,create_with_props_fail_before_size,take_last_dispose_snapshot}()` 与对应 test-local snapshot/helper 名称，已统一收口为 `tinyui_image_*`
  - 这一步继续保持边界：`image` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_image_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `image` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_image$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `graph` 的 residual internal/test seam：`picoui_graph_backend()`、`picoui_graph_backend_const()`、`picoui_graph_get_ld()`、`picoui_graph_get_ld_const()`、`picoui_graph_props_are_valid()`、`picoui_graph_apply_native_geometry_candidate()` 已统一收口为 `tinyui_graph_*`，并在 `test_picoui_graph` 里补了源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`graph` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_graph_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `graph` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_graph$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `arc` 的 residual internal/test seam：`struct picoui_arc_test_dispose_snapshot`、`picoui_arc_last_dispose_snapshot*`、`picoui_arc_props_are_valid()`、`picoui_arc_rgb_to_ld_color()`、`picoui_arc_ld_color_to_rgb()`、`picoui_arc_backend()`、`picoui_arc_get_ld()`、`picoui_arc_finish_detach_after_backend_failure()`、`picoui_arc_dispose_partial_impl()`，以及 `picoui_backend_arc_test_{take_last_dispose_snapshot,create_with_props_fail_before_parent_color}()` 已统一收口为 `tinyui_arc_*`
  - 这一步继续保持边界：`arc` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_arc_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `arc` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_arc$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `clock` 的 residual internal seam：`picoui_clock_props_are_valid()`、`picoui_clock_get_ld()`、`picoui_clock_apply_background()`、`picoui_clock_apply_pointer()` 已统一收口为 `tinyui_clock_*`，并在 `test_picoui_clock` 里补了 focused 源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`clock` 这轮只收 widget-local/internal helper 名称，不触碰 public `picoui_clock_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `clock` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_clock$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `message_box` 的 residual internal/test seam：`picoui_message_box_props_are_valid()`、`picoui_message_box_get_ld()`、`picoui_message_box_confirm_bridge()` 已统一收口为 `tinyui_message_box_*`，并在 `test_picoui_message_box` 里补了 focused 源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`message_box` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_message_box_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `message_box` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_message_box$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `animation` 的 residual internal/test seam：`picoui_animation_get_ld()`、`picoui_animation_props_are_valid()`、`picoui_animation_attach_native()` 已统一收口为 `tinyui_animation_*`，并在 `test_picoui_animation` 里补了 focused 源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`animation` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_animation_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `animation` 这轮 focused proof 已 fresh 通过：`rtk ctest --test-dir build -R '^test_picoui_animation$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `scroll_selecter` 的 residual internal/test seam：`picoui_scroll_selecter_props_are_valid()`、`picoui_scroll_selecter_rgb_to_ld_color()`、`picoui_scroll_selecter_backend_from_widget()`、`picoui_scroll_selecter_ld_from_backend()`、`picoui_scroll_selecter_selected_text_from_public_state()` 已统一收口为 `tinyui_scroll_selecter_*`，并在 `test_picoui_scroll_selecter` 里补了 focused 源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`scroll_selecter` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_scroll_selecter_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `scroll_selecter` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_scroll_selecter` 与 `rtk ctest --test-dir build -R '^test_picoui_scroll_selecter$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `icon_slider` 的 residual internal/test seam：`picoui_icon_slider_get_ld()`、`picoui_icon_slider_backend_set_selected_index()`、`picoui_icon_slider_native_slot()`、`picoui_icon_slider_backend_add_item()`、`picoui_icon_slider_backend_add_item_with_source()`、`picoui_icon_slider_backend_get_selected_index()`、`picoui_icon_slider_backend_set_horizontal()`、`picoui_icon_slider_backend_get_horizontal()`、`picoui_icon_slider_backend_set_speed()`、`picoui_icon_slider_bind_host()`、`picoui_icon_slider_props_are_valid()` 已统一收口为 `tinyui_icon_slider_*`，并在 `test_picoui_icon_slider` 里补了 focused 源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`icon_slider` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_icon_slider_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `icon_slider` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_icon_slider` 与 `rtk ctest --test-dir build -R '^test_picoui_icon_slider$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `radial_menu` 的 residual internal/test seam：`picoui_radial_menu_get_ld()`、`picoui_radial_menu_backend_set_selected_index()`、`picoui_radial_menu_native_slot()`、`picoui_radial_menu_backend_add_item()`、`picoui_radial_menu_backend_add_item_with_source()`、`picoui_radial_menu_backend_get_selected_index()`、`picoui_radial_menu_backend_offset_selection()`、`picoui_radial_menu_backend_set_default_item()`、`picoui_radial_menu_backend_click_item()`、`picoui_radial_menu_backend_offset_item()`、`picoui_radial_menu_bind_host()`、`picoui_radial_menu_props_are_valid()` 已统一收口为 `tinyui_radial_menu_*`，并在 `test_picoui_radial_menu` 里补了 focused 源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`radial_menu` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_radial_menu_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `radial_menu` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_radial_menu` 与 `rtk ctest --test-dir build -R '^test_picoui_radial_menu$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `canvas` 的 residual internal/test seam：`picoui_canvas_rgb_to_ld()`、`picoui_canvas_align_to_ld()`、`picoui_canvas_push_native()`、`picoui_canvas_clear_native()`、`picoui_canvas_is_valid()` 已统一收口为 `tinyui_canvas_*`，并在 `test_picoui_canvas` 里补了 focused 源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`canvas` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_canvas_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `canvas` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_canvas` 与 `rtk ctest --test-dir build -R '^test_picoui_canvas$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `background` 的 residual internal/test seam：`struct picoui_background_backend_host`、`picoui_background_get_root_size()` 已统一收口为 `tinyui_background_*`，并在 `test_picoui_background` 里补了 focused 源码存在/缺失断言，锁定旧 seam 名不再出现
  - 这一步继续保持边界：`background` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_background_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `background` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_background` 与 `rtk ctest --test-dir build -R '^test_picoui_background$' --output-on-failure`
  - 到当前真相，`label/image/graph/arc/clock/message_box/animation/scroll_selecter/icon_slider/radial_menu/canvas/background` 这几条 retained internal seam 也已继续向 `tinyui_*` 收口；下一自然顺序继续前移到仍残留较密集 `picoui_*` widget-local seam 的控件批次，如 `list`
  - 到当前真相，`button/slider` 这两条 retained internal seam 也已继续向 `tinyui_*` 收口；下一自然顺序继续前移到仍残留较密集 `picoui_*` widget-local seam 的控件批次
  - `core/widget.c` 当前仍保留的 `picoui_*` 名称已经收敛到 public widget API 与现有 internal/shared entry seam；这一批 focused proof 已直接锁定上述 pure-static helper 和 data-model identity 计数器不再以 `picoui_*` 形式出现在 source 中
  - `V3 Task 2 Step 1` 的第一批 shared target naming 也已完成：`cmake/LingDongGUI.cmake` 中的 shared/product-layer target 已建立 `tinyui_core`、`tinyui_backend_ldgui`、`tinyui_backend_ldgui_runtime`、`tinyui_port_sdl` 实名，`tests/support/CMakeLists.txt` 中也已建立 `tinyui_test_support` 实名
  - 为避免提前撞进 `V4` 的 test/demo/contract/archive 命名面，这一批采用 `tinyui_*` 实名 + `picoui_*` CMake alias 兼容：现有 `tests/picoui/CMakeLists.txt`、runtime/perf/contract 脚本和 focused tests 仍可继续引用 `picoui_core`、`picoui_backend_ldgui`、`picoui_backend_ldgui_runtime`、`picoui_port_sdl`、`picoui_test_support`，但底层真实 target 名已经切到 `tinyui_*`
  - `V3 Task 2 Step 1` 这轮又继续完成了测试图内部 target 引用收口：`tests/picoui/CMakeLists.txt` 里的 `SUPPORT_LIB` / `MAIN_LIB` / `target_link_libraries(test_picoui_port_sdl ...)` 现已直接使用 `tinyui_test_support`、`tinyui_backend_ldgui`、`tinyui_port_sdl` 实名；外部测试可执行文件名、`check_picoui_*` / `test_picoui_*` 脚本与 label 保持不变，仍通过 `picoui_*` alias 与现有 gate 兼容
  - 这一步 focused compile proof 已 fresh 通过：`test_picoui_runtime_model`、`test_picoui_layout`、`test_picoui_theme`、`test_picoui_native_bridge`、`test_picoui_port_sdl`；当前仍未触碰 `libpicoui_backend_ldgui.a` 这类 archive 名、`test_picoui_*` / `check_picoui_*` 文件名、`tests/picoui/*` 路径、CTest label 或 contract/demo 命名
  - 这一步继续保持边界：不改 public `picoui_window_set_*` / `picoui_grid_*` / `picoui_flex_*` API，不改 `tests/picoui/*` 路径、`test_picoui_*` / `check_picoui_*` 文件名、CTest label 或 contract/demo 命名
  - `V3 Task 2` 当前又补齐了一批 gate 真相修复，但仍不是 `V4`：`test_picoui_wrapper_struct_overhead` 已从 `perf` label 退回 `probe` label，`ctest -L perf` 不再因为未预构建裸 probe 出现假红；真正的 wrapper/object overhead gate 继续由 `check_picoui_tinyui_object_overhead` 负责
  - 同一批里，`tinyui/src/backend/ldgui/backend.h` 已对 backend layout/widget cache 的小标量宽度做收紧，`backend_widget_struct_bytes` 已从回归态 `536` 压回 `464`，重新低于当前 perf baseline gate `<=512`
  - `V3 Step 3` 这轮又完成了一批 shared core internal event/callback seam 命名收口：`picoui_backend_emit_value_changed()`、`picoui_backend_emit_event()`、`picoui_backend_emit_clicked()` 已统一收口为 `tinyui_widget_emit_value_changed()`、`tinyui_widget_emit_event()`、`tinyui_widget_emit_clicked()`，只改动 `core/internal.h + core/widget.c + core/event.c + backend.h + test_picoui_button_events` 这一条 internal/shared 调用链
  - 同一小批里，`theme.c` 的 backend-facing internal helper `picoui_theme_apply_widget_style()` 也已收口为 `tinyui_theme_apply_widget_style()`，只改动 `theme.c + test_picoui_theme` 这一条 internal/shared 调用链；这一步继续明确不触碰 public `picoui_theme_*` API、`picoui_app_set_theme()`、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 demo/contract 命名
  - `V3 Step 3` 这轮又继续完成了 shared widget-tree lifecycle seam 的第一小批：`picoui_backend_widget_is_kind()` 已收口为 `tinyui_widget_is_kind()`，只改动 `core/internal.h + core/widget.c + test_picoui_widgets + test_picoui_image + test_picoui_qrcode` 这一条 internal/shared 调用链
  - `V3 Step 3` 这轮又继续完成了 shared widget-tree lifecycle seam 的第二小批：`picoui_backend_widget_init_root()`、`picoui_backend_widget_init_child()`、`picoui_backend_widget_attach_child()` 已收口为 `tinyui_widget_init_root()`、`tinyui_widget_init_child()`、`tinyui_widget_attach_child()`，只改动 `core/internal.h + core/widget.c + widgets/*.c + test_picoui_widgets + test_picoui_image + test_picoui_qrcode` 这一条 internal/shared 调用链
  - `V3 Step 3` 这轮又继续完成了 `list selected-index seam`：`picoui_list_backend_set_selected_index()`、`picoui_list_backend_get_selected_index()`、`picoui_list_backend_sync_selected_index()` 已收口为 `tinyui_list_set_selected_index()`、`tinyui_list_get_selected_index()`、`tinyui_list_sync_selected_index()`，只改动 `core/internal.h + core/event.c + widgets/list.c + test_picoui_list` 这一条 internal/shared 调用链
  - 这一步继续保持边界：`list` 这轮只收 `selected-index` 这 3 个 internal/shared entry，不顺手扩到 `picoui_backend_list_*` 的其他 widget-local moved helper
  - `V3 Step 3` 这轮又继续完成了 `combo_box` 的 internal/widget-local helper seam：`picoui_backend_combo_box_*`、`picoui_combo_box_create_backend_local()`、`picoui_backend_combo_box_native_slot()` 已统一收口为 `tinyui_combo_box_*`，只改动 `widgets/combo_box.c + test_picoui_combo_box` 这一条 internal/widget-local 调用链
  - 这一步继续保持边界：`combo_box` 这轮只收 widget-local/internal helper 名称，不触碰 public `picoui_combo_box_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `V3 Step 3` 这轮又继续完成了 `line_edit` 的 internal/widget-local helper seam：`picoui_backend_line_edit_*`、`picoui_line_edit_create_backend_local()`、`picoui_backend_line_edit_native_slot()` 已统一收口为 `tinyui_line_edit_*`，只改动 `widgets/line_edit.c + test_picoui_line_edit` 这一条 internal/widget-local 调用链
  - 这一步继续保持边界：`line_edit` 这轮只收 widget-local/internal helper 名称，不触碰 public `picoui_line_edit_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `V3 Step 3` 这轮又继续完成了 `core/event` shared helper seam：`picoui_widget_accepts_event()`、`picoui_widget_slider_value_to_percent()`、`picoui_widget_slider_percent_to_value()`、`picoui_widget_sync_ld_value()`、`picoui_widget_emit_ld_event_bridge()`、`picoui_widget_claim_focus_for_signal()`、`picoui_widget_restore_rejected_list_selection()`、`picoui_widget_get_owner_app()`、`picoui_widget_note_focus_event()` 已统一收口为 `tinyui_widget_*`，只改动 `core/event.c + core/internal.h + core/widget.c + test_picoui_button_events` 这一条 internal/shared 调用链
  - 这一步继续保持边界：`core/event` 这轮只收 shared/internal helper 名称，不触碰 `picoui_widget_update_value()`、`picoui_widget_dispatch_signal()`、`picoui_widget_dispatch_event()`、`picoui_widget_dispatch_native_signal()` 这些 shared/public entry，也不触碰 public `picoui_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `V3 Step 3` 这轮又继续完成了 `text` 的 widget-local/internal seam：`picoui_backend_text_*` 与 `picoui_backend_text_test_fail_next_set_font()` 已统一收口为 `tinyui_text_*`，改动范围覆盖 `widgets/text.c + test_picoui_text`，并补了一个直系测试调用点 `test_picoui_widgets`
  - 这一步继续保持边界：`text` 这轮只收 widget-local/internal helper 与 test seam 名称，不触碰 public `picoui_text_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `V3 Step 3` 这轮又继续完成了 `list` 的剩余 widget-local/internal seam：`picoui_list_rgb_to_ld_color()`、`picoui_list_map_align()`、`picoui_list_backend()`、`picoui_list_ld_widget()` 以及 `picoui_backend_list_{set_items,set_item_height,set_padding_group,set_margin_group,set_text_color,set_bg_color,set_select_color,set_align,set_item_widget}()` 已统一收口为 `tinyui_list_*`，改动范围覆盖 `widgets/list.c + test_picoui_list`
  - 这一步继续保持边界：`list` 这轮只收 widget-local/internal helper 名称与旧声明清理，不触碰 public `picoui_list_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `list` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_list` 与 `rtk ctest --test-dir build -R '^test_picoui_list$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `label` 的 residual internal seam：`picoui_backend_rgb_to_ld_color()`、`picoui_backend_ld_color_to_rgb()`、`picoui_backend_map_label_align()`、`picoui_backend_unmap_label_align()` 已统一收口为 `tinyui_label_*`，改动范围覆盖 `widgets/label.c + test_picoui_label`
  - 这一步继续保持边界：`label` 这轮只收 widget-local/internal helper 名称与 focused proof，不触碰 public `picoui_label_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `label` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_label` 与 `rtk ctest --test-dir build -R '^test_picoui_label$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `button` 的 residual internal seam：`picoui_button_alloc()`、`picoui_button_set_event()` 已统一收口为 `tinyui_button_*`，改动范围覆盖 `widgets/button.c + test_picoui_button_events`
  - 这一步继续保持边界：`button` 这轮只收 widget-local/internal helper 名称与 focused proof，不触碰 public `picoui_button_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `button` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_button_events` 与 `rtk ctest --test-dir build -R '^test_picoui_button_events$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `core/app` 的 residual internal seam：`picoui_app_timer_unlink()` 已统一收口为 `tinyui_app_timer_unlink()`，改动范围覆盖 `core/app.c + test_picoui_app_timer`
  - 这一步继续保持边界：`core/app` 这轮只收单个 static helper 名称与 focused proof，不触碰 public `picoui_app_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `core/app` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_app_timer` 与 `rtk ctest --test-dir build -R '^test_picoui_app_timer$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `core/widget` 的 residual internal seam：`picoui_align_to_ld_horizontal()`、`picoui_align_to_ld_vertical()`、`picoui_rect_to_ld_region()`、`picoui_ld_location_to_arm()`、`picoui_ld_location_from_arm()`、`picoui_ld_region_to_arm()`、`picoui_ld_region_from_arm()`、`picoui_rect_from_ld_region()`、`picoui_widget_type_from_backend_kind()` 已统一收口为 `tinyui_*`
  - 这一步继续保持边界：`core/widget` 这轮只收 pure-static geometry/type helper 名称与 focused proof，不触碰 public `picoui_widget_*` / `picoui_rect_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `core/widget` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_widgets` 与 `rtk ctest --test-dir build -R '^test_picoui_widgets$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `core/runtime` 的 residual internal seam：`g_picoui_runtime_app` 已统一收口为 `g_tinyui_runtime_app`，改动范围覆盖 `core/runtime.c + test_picoui_runtime_model`
  - 这一步继续保持边界：`core/runtime` 这轮只收单个 static runtime state 名称与 focused proof，不触碰 public `picoui_init()` / `picoui_deinit()` / `picoui_screen_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `core/runtime` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_runtime_model` 与 `rtk ctest --test-dir build -R '^test_picoui_runtime_model$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `radial_menu` 的 residual internal seam：`picoui_radial_menu_create_with_backend_config()` 已统一收口为 `tinyui_radial_menu_create_with_backend_config()`，改动范围覆盖 `widgets/radial_menu.c + test_picoui_radial_menu`
  - 这一步继续保持边界：`radial_menu` 这轮只收单个 widget-local create helper 名称与 focused proof，不触碰 public `picoui_radial_menu_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `radial_menu` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_radial_menu` 与 `rtk ctest --test-dir build -R '^test_picoui_radial_menu$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `icon_slider` 的 residual internal seam：`picoui_icon_slider_create_with_backend_config()` 已统一收口为 `tinyui_icon_slider_create_with_backend_config()`，改动范围覆盖 `widgets/icon_slider.c + test_picoui_icon_slider`
  - 这一步继续保持边界：`icon_slider` 这轮只收单个 widget-local create helper 名称与 focused proof，不触碰 public `picoui_icon_slider_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `icon_slider` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_icon_slider` 与 `rtk ctest --test-dir build -R '^test_picoui_icon_slider$' --output-on-failure`
  - `V3 Step 3` 这轮又继续完成了 `canvas` 的 residual internal seam：`picoui_canvas_push()` 已统一收口为 `tinyui_canvas_push()`，改动范围覆盖 `widgets/canvas.c + test_picoui_canvas`
  - 这一步继续保持边界：`canvas` 这轮只收单个 widget-local command-push helper 名称与 focused proof，不触碰 public `picoui_canvas_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `canvas` 这轮 focused proof 已 fresh 通过：`rtk cmake --build build --target test_picoui_canvas` 与 `rtk ctest --test-dir build -R '^test_picoui_canvas$' --output-on-failure`
  - 到当前真相，`V3 Step 3` 这条 old internal seam rename 线已经收口：`static picoui_*` / `g_picoui_*` / retained `create_with_backend_config` 这类 shared/widget-local residual seam 在 `tinyui/src/{core,widgets}` 下已清零
  - 当前收口证据：`rg -n "static .*picoui_|g_picoui_|picoui_[A-Za-z0-9_]+create_with_backend_config" tinyui/src/core tinyui/src/widgets | sort` 已不再命中旧命名 residual；且 `test_picoui_{widgets,button_events,icon_slider,radial_menu,list,app_timer,runtime_model,label,canvas}` 这 9 个 focused gate 已 fresh 全绿
  - `V3 Step 3` 这轮又继续完成了 `calendar` 的 widget-local/internal seam：旧的 day-name globals、native readback/helper、widget-local create helper，以及整组 date/header/color/grid/system-date retained helper 已统一收口为 `g_tinyui_calendar_*` 与 `tinyui_calendar_*`，改动范围覆盖 `widgets/calendar.c + backend.h + test_picoui_calendar`
  - 这一步继续保持边界：`calendar` 这轮只收 widget-local/internal helper 名称与旧声明清理，不触碰 public `picoui_calendar_*` API、`tests/picoui/*` 路径、`test/check` 文件名、CTest label 或 `V4` 资产命名
  - `V3 Step 3` 这轮又继续完成了 `window` 的最小 `color test seam`：旧的 bg-color fail flag、RGB/native color conversion helper 与对应 test seam 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 color helper 与 test seam 名称，不触碰 public `picoui_window_*` API，也不把 `root/layout/background` shared 主链混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `layout mapper seam`：旧的 flex/grid align-flow-track mapping helper 与 grid-track copy helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 pure-static mapper/copy helper 名称，不触碰 public `picoui_window_*` API，也不把 `get_ld_window/get_root_size/init_defaults/dispose_partial/apply_*` 或 `root/layout/background` shared 主链混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `native access seam`：旧的 backend/native access helper 与 layout-type mapping helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 `get_backend/get_backend_host/get_ld_window/map_layout_type` 这组 static helper 名称，不触碰 public `picoui_window_*` API，也不把 `get_root_size/init_defaults/dispose_partial/apply_*` 或 `root/layout/background` shared 主链混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `validation seam`：旧的 `is_valid/props_are_valid` 这组 static 校验 helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 `is_valid/props_are_valid` 这组 static 校验 helper 名称，不触碰 public `picoui_window_*` API，也不把 `get_root_size/init_defaults/dispose_partial/apply_*` 或 `root/layout/background` shared 主链混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `init-defaults seam`：旧的 `init_defaults` static helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 `init_defaults` 这一项 static 初始化 helper 名称，不触碰 public `picoui_window_*` API，也不把 `get_root_size/dispose_partial` 或 `apply_*` 与 `root/layout/background` shared 主链混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `root-size seam`：旧的 `get_root_size` static helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 `get_root_size` 这一项 static root-size helper 名称，不触碰 public `picoui_window_*` API，也不把 `dispose_partial` 或 `apply_*` 与 `root/layout/background` shared 主链混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `dispose-partial seam`：旧的 `dispose_partial` static lifecycle helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 `dispose_partial` 这一项 static lifecycle helper 名称，不触碰 public `picoui_window_*` API，也不把 `set_padding_group_impl/apply_padding_contract/apply_layout_type_impl/apply_flex_contract_impl/apply_generic_gap_impl` 那条 layout-contract 主链混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `generic-gap seam`：旧的 `apply_generic_gap_impl` static gap helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 `apply_generic_gap_impl` 这一项 static gap helper 名称，不触碰 public `picoui_window_*` API，也不把 `apply_flex_contract_impl`、`set_padding_group_impl`、`apply_layout_type_impl` 与共享核心 `apply_padding_contract` 混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `padding-contract seam` 第一批：旧的 `set_padding_group_impl` 与共享核心 `apply_padding_contract` 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 `set_padding_group_impl + apply_padding_contract` 这一组 internal padding-contract helper 名称，不触碰 public `picoui_window_*` API，也不把 `apply_layout_type_impl` 或死 helper `apply_flex_contract_impl` 混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的 `layout-type seam`：旧的 `apply_layout_type_impl` static helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 这一步继续保持边界：`window` 这轮只收 `apply_layout_type_impl` 这一项 internal helper 名称，不触碰 public `picoui_window_*` API，也不把最后的死 helper `apply_flex_contract_impl` 混进来
  - `V3 Step 3` 这轮又继续完成了 `window` 的最后一个 `flex-contract dead-helper seam`：旧的 `apply_flex_contract_impl` static dead helper 已统一收口为 `tinyui_window_*`，改动范围覆盖 `widgets/window.c + test_picoui_window`
  - 到当前真相，`window.c` 这条 `V3 Step 3` internal rename 线已经收口：`color test / layout mapper / native access / validation / init-defaults / root-size / dispose-partial / generic-gap / padding-contract / layout-type / dead-helper` 这些 widget-local/internal seam 都已统一切到 `tinyui_window_*`
  - 后续若继续动 `window`，边界不再是同类 internal rename，而应转向 public API / shared truth / `V4` 资产面
  - `V3 Task 2 Step 3` 也已补齐当前 closeout 文档口径：`docs/v2.1/v2.1-closeout.md` 已建立 `V3` 阶段 closeout truth，明确 shared layer 是产品层 `tinyui` 子系统、shared retention 是有意设计、backend removal 没有把 shared responsibilities 粗暴塌缩进 widgets；但这还不是 `v2.1` 全阶段最终 release closeout
  - `switch` 已完成首批 widget-specific backend 行为并回；`backend_switch.c` 已被识别为纯空编译单元，不再承载任何 production shared helper，这轮只把它从编译面退场并删除
  - `background/window/layout` 的 focused unit proof 已建立：`test_picoui_background`、`test_picoui_window`、`test_picoui_layout`、`test_picoui_app_window_switch`、`test_picoui_switch` 当前同批通过
  - `layout` 已新增 explicit `flex/grid` padding follow-up contract，后续 `flow/align/gap/track` 更新不得覆盖已显式设置的四边 padding
  - `label` 已新增 `create_with_props()` 失败回滚合同：后续 widget-specific setter 失败时，不得在 parent child 链上遗留半挂载 backend child
  - `button` 当前已补上的失败路径证据仍是有限口径：`font` setter 失败时，`test_picoui_button_events` 只证明 parent child 链不会遗留半挂载 child；`xBtnAction/action_info` 清理尚未在本批测试内形成 fresh proof，因此文档暂不把它写成已证实合同
  - `button` 的 widget-specific `font/image/transparent/checkable/key_value/pressed` 行为已并回 `tinyui/src/widgets/button.c`；对应纯 disabled-stub `backend_button.c` 已从 CMake 退编并删除
  - `window` 的 widget-specific `background_source/background_offset/bg_color/padding_group/getters` 行为已并回 `tinyui/src/widgets/window.c`；对应纯 disabled-stub `backend_window.c` 已从 CMake 退编并删除
  - `window` 已补上 root `create_with_props()` 失败回滚合同：`bg_color` setter 失败时不得在 `ld_scene->ptNodeRoot` 残留 root node
  - `checkbox` 的 create 路径与 widget-specific `check_color/text_color/unchecked_source/checked_source/radio_group/string_left_space` 行为现已全部并回 `tinyui/src/widgets/checkbox.c`；对应 `backend_checkbox.c` 已不再承载 production shared helper，可从编译面退场并删除
  - `checkbox` 当前 focused proof 已补到 native helper 行为：除 direct-create / props happy path / checked-text round-trip 外，还直接断言 `fgColor/textColor/image tiles/radioButtonGroup/isRadioButton/boxWidth` 与非法参数拒绝；但 `create_with_props()` 失败回滚尚未在 fresh test 中坐实，文档暂不写成已证明合同
  - `checkbox` focused/runtime 证据当前为绿：`test_picoui_checkbox`、`test_picoui_widgets`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - `slider` 的 create 路径与 widget-specific `horizontal/background_source/indicator_source/image/color/indicator_width/slim_size/get_percent` 行为已并回 `tinyui/src/widgets/slider.c`；对应纯 disabled-stub `backend_slider.c` 已从 CMake 退编并删除
  - `slider` 已补上更强的 `create_with_props()` 失败回滚合同：指定 `id` 的 `indicator_width` 失败注入路径触发后，不得残留 parent/owner/root/host/event bridge 绑定，也不得遗留 `ldBase.pInfo` 或 sibling 链脏状态
  - `slider` 专项 direct-create tree/binding 合同已锁定：`owner/root/parent/name_id/ld_widget/host_widget` 与 LD event bridge 必须一致
  - `slider` focused/runtime 证据当前为绿：`test_picoui_slider`、`test_picoui_widgets`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - `progress_bar` 的 create 路径与 widget-specific `percent/horizontal/image/bg_source/fg_source/frame_source/color/frame_color/inverted` 行为已并回 `tinyui/src/widgets/progress_bar.c`
  - `progress_bar` 已补上 `create_with_props()` props-stage 失败回滚合同：失败后不得残留 parent/owner/root/host/event bridge 绑定，`ldBase.pInfo` 与 sibling 链也必须清干净
  - `progress_bar` 专项 direct-create tree/binding 合同已锁定：`kind/owner/root/parent/ld_name_id/host_widget/ld_event_bridge/ldBase.pInfo` 必须一致
  - `progress_bar` 这批低风险声明面收口已完成：`backend.h` 中 legacy `picoui_backend_create_progress_bar()` 声明已删除；对应 unit test 不再把 disabled stub 当成当前 `V2` 合法合同，继续只守 direct-create tree/binding 与 rollback 真相
  - `progress_bar` focused/runtime 证据当前为绿：`test_picoui_progress_bar`、`test_picoui_widgets`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - `progress_wheel` 的 widget-local create 路径与 widget-specific `percent/progress alias/wheel_color/dot_color/dot_enabled/get_percent` 行为已稳定落在 `tinyui/src/widgets/progress_wheel.c`；本轮进一步把 `dirty-region` helper 与 rollback test seam 也并回 widget 文件
  - `progress_wheel` 已补上 direct-create tree/binding 合同：`kind/owner/root/parent/ld_name_id/host_widget/ld_event_bridge/ldBase.pInfo` 必须一致
  - `progress_wheel` 已补上 `create_with_props()` props-stage 失败回滚合同：失败后不得残留 parent/owner/root/host/event bridge 绑定，`ldBase.pInfo` 与 sibling 链也必须清干净
  - `progress_wheel` 这批已完成 `V2` 当前收口：`backend_progress_wheel.c` 不再需要保留，helper-only 真实边界已消失；对应 compile-surface cleanup 以 widget 文件内 test seam + public-path rollback proof 为准
  - `progress_wheel` focused/runtime 证据当前为绿：`test_picoui_progress_wheel`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - `arc` 的 create 路径与 widget-specific `background/foreground/rotation angle`、`quarter_source`、`parent_color`、`bg/fg color` 行为已并回 `tinyui/src/widgets/arc.c`；对应纯 disabled-stub `backend_arc.c` 已从 CMake 退编并删除
  - `arc` 已补上 `create_with_props()` props-stage 失败回滚合同：失败后不得残留 parent/owner/root/host/event bridge 绑定，`ldBase.pInfo` 与 sibling 链也必须清干净
  - `arc` 颜色读回合同已锁定为 production `rgb565 -> rgb888` round-trip 真相，不允许回退到自等式或弱化断言
  - `arc` focused/runtime 证据当前为绿：`test_picoui_arc`、`test_picoui_widgets`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - `gauge` 的 create 路径与 widget-specific `angle/bg_source/pointer_source/centre_offset/pointer_color/auto_move/trail/progress_bar` 行为已并回 `tinyui/src/widgets/gauge.c`；对应纯 disabled-stub `backend_gauge.c` 已从 CMake 退编并删除
  - `gauge` 已补上 `create_with_props()` props-stage 失败回滚合同：失败后不得残留 parent/owner/root/host/event bridge 绑定，`ldBase.pInfo` 与 sibling 链也必须清干净
  - `gauge` 的 `pointer_color` 读回合同已锁定为 production `rgb565 -> rgb888` round-trip 真相，不允许回退到 `!= 0` 之类弱断言
  - `gauge` focused/runtime 证据当前为绿：`test_picoui_gauge`、`test_picoui_widgets`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - `window/label/button/checkbox/slider/arc/gauge` 这一批 low-risk legacy create 声明面收口与 compile-surface cleanup 已完成：`tinyui/src/backend/ldgui/backend.h` 中 `picoui_backend_create_window`、`picoui_backend_create_child_window`、`picoui_backend_create_background`、`picoui_backend_create_label`、`picoui_backend_create_button`、`picoui_backend_create_checkbox`、`picoui_backend_create_slider`、`picoui_backend_create_arc`、`picoui_backend_create_gauge` 声明已删除
  - 对应 unit test 已同步切到 `V2` 当前真相：不再把 disabled stub 当成合法入口，只继续守 public/direct-create tree/binding、props-stage rollback 与 widget-local 行为合同
  - 本批 compile-surface cleanup 已完成：`backend_window.c`、`backend_button.c`、`backend_slider.c`、`backend_arc.c`、`backend_gauge.c` 这 5 个纯 disabled-stub backend 文件已从 `cmake/LingDongGUI.cmake` 退编并删除
  - `backend_label.c` 仍保留在编译面，因为它当前仍承载 shared helper；`backend_checkbox.c` 则已完成进一步收口：legacy `picoui_backend_create_checkbox()` 与 widget-specific helper 实现均已迁离 backend 路径，因此 `backend_checkbox.c` 已从 CMake 退编并删除
  - `image` 的 create 路径与 widget-specific `source/mask_color` 行为已并回 `tinyui/src/widgets/image.c`
  - `image` 已补上 `create_with_props()` props-stage 失败回滚合同：失败后不得残留 parent/owner/root/host/event bridge 绑定，`ldBase.pInfo` 与 sibling 链也必须清干净
  - `image` 的 rollback test seam 已从 production 主实现剥离到 `tests/support/picoui_test_support.c`，不再把 fail switch 和 snapshot 状态编进 `widgets/image.c`
  - `image` 这批低风险声明面收口已完成：`backend.h` 中 legacy `picoui_backend_create_image()` 声明已删除；对应 unit test 不再把 disabled stub 当成当前 `V2` 合法合同，继续只守 direct-create tree/binding 与 rollback 真相
  - `image` focused/runtime 证据当前为绿：`test_picoui_image`、`test_picoui_widgets`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - `qrcode` 的 create 路径与 widget-specific `text/qr_color/bg_color/ecc/max_version/zoom` 行为已并回 `tinyui/src/widgets/qrcode.c`
  - `qrcode` 已补上 `create_with_props()` props-stage 失败回滚合同：失败后不得残留 parent/owner/root/host/event bridge 绑定，`ldBase.pInfo` 与 sibling 链也必须清干净
  - `qrcode` 的 rollback test seam 已从 production 主实现剥离到 `tests/support/picoui_test_support.c`，不再把 fail switch 和 snapshot 状态编进 `widgets/qrcode.c`
  - `qrcode` 这批低风险声明面收口已完成：`backend.h` 中 legacy `picoui_backend_create_qrcode()` 声明已删除；对应 unit test 不再把 disabled stub 当成当前 `V2` 合法合同，继续只守 direct-create tree/binding 与 rollback 真相
  - `qrcode` focused/runtime 证据当前为绿：`test_picoui_qrcode`、`test_picoui_widgets`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - 在当前 `cmake/LingDongGUI.cmake` 已迁到 `tinyui/*` 路径的工作树真相下，`image + qrcode + progress_bar` 这一批 compile-surface cleanup 已进一步完成：`LD_PICOUI_BACKEND_LDGUI_SOURCES` 已移除 `backend_image.c`、`backend_qrcode.c`、`backend_progress_bar.c` 这 3 个纯 disabled-stub backend 条目，且对应文件已删除
  - `progress_wheel` 当前不再保留 helper-only backend 编译面；`backend_progress_wheel.c` 已退出 `V2` 当前真相，focused proof 继续只守 widget-local helper、public create tree/binding 与 rollback 合同
  - `test_picoui_widgets` 已同步到 `V2` 新真相：不再把已禁用的 legacy create 当成合法构造路径，改为通过 public create 验证 direct-create tree/binding
  - `calendar/combo_box/line_edit` 这一低风险清理批次已完成当前收口：对应 widget-local create 真相保留在 `tinyui/src/widgets/calendar.c`、`tinyui/src/widgets/combo_box.c`、`tinyui/src/widgets/line_edit.c`，而 `backend_calendar.c`、`backend_combo_box.c`、`backend_line_edit.c` 已从 CMake 编译面退场，`backend.h` 中的 legacy constructor 声明也已删除
  - `test_picoui_calendar`、`test_picoui_widgets`、`test_picoui_line_edit` 已改为通过 public widget create 验证 backend tree/binding 仍由 widget-local 路径提供，不再把 legacy backend constructor 当成 V2 合法入口
  - `text/canvas/date_time` 这一批已完成 widget-local create merge：`tinyui/src/widgets/{text,canvas,date_time}.c` 现在直接完成 backend child 建树与 `host_widget` 绑定，不再调用 `picoui_backend_create_text/canvas/date_time()`
  - 其中 `canvas` 已进一步完成 widget-specific helper 收口：`picoui_backend_canvas_sync()` 的 native push/clear 逻辑已并回 `tinyui/src/widgets/canvas.c`，`backend.h` 中对应 helper 声明已删除，`backend_canvas.c` 也已从 CMake 退编并删除；`date_time` 这轮也已进一步完成 widget-specific helper 收口：`format/date/time/text_color/bg_color/align/transparent/use_system_time/get_*` 已全部并回 `tinyui/src/widgets/date_time.c`，`backend.h` 中对应 helper 声明已删除，`backend_date_time.c` 已从 CMake 退编并删除；本轮 `text` 也已进一步完成 widget-specific helper 收口：`font/static_text/transparent/text_color/bg_color/background_source/scroll_*` 已并回 `tinyui/src/widgets/text.c`，`backend.h` 中对应 helper 声明已删除，`backend_text.c` 已从 CMake 退编并删除
  - 本批 focused proof 当前应以 `test_picoui_text`、`test_picoui_canvas`、`test_picoui_date_time` 为准，重点守住 `parent/root/owner/host_widget/ld_widget` 的 direct-create tree/binding 合同；其中 `canvas` 继续锁定 native command push/clear 与 text snapshot 不被外部 buffer 污染的回归点，`date_time` 继续锁定 public/native readback、transparent/bg_color 交互，以及 corrupted backend binding 下不得污染 native 状态的负向合同
  - `list/scroll_selecter` 这一批已完成 widget-local create merge：`tinyui/src/widgets/{list,scroll_selecter}.c` 现在直接完成 backend child 建树、attach 与 `host_widget` 绑定，不再依赖独立 legacy create 路径
  - `backend.h` 中 `picoui_backend_create_list()`、`picoui_backend_create_scroll_selecter()` 声明已删除；`tests/picoui/unit/test_picoui_{list,scroll_selecter}.c` 已同步锁定 direct-create tree/binding 合同，并额外断言 legacy constructor 符号已不存在
  - `backend_list.c` 已完成当前收口：它不再承载正式 create 入口，原先残留的 `selected_index/get_selected_index/sync_selected_index` 兼容 helper 现已并入 `tinyui/src/widgets/list.c`；对应 `backend_list.c` 已从 CMake 退编并删除
  - `backend_scroll_selecter.c` 则已完成进一步收口：在确认 `picoui_backend_create_scroll_selecter()` impact 为 `LOW`、`direct callers = 0` 后，legacy disabled create 空壳已从 `cmake/LingDongGUI.cmake` 退编并删除；scroll_selecter 的 setter/getter/sync helper 真相现已全部并入 `tinyui/src/widgets/scroll_selecter.c`
  - 本批 focused proof 当前应以 `test_picoui_list`、`test_picoui_scroll_selecter` 为准，重点守住 `parent/root/owner/host_widget/ld_widget/ldBase.pInfo` 的 direct-create tree/binding 合同；其中 `list` 继续锁定 migrated helper 漏网调用必须 fail-closed、selected-index readback/sync 继续遵守 widget-local/native 真相，并额外锁定 legacy `picoui_backend_list_*` selected-index 符号已从测试二进制消失，`scroll_selecter` 则继续锁定 `set_items()` 后 native/public selected state 同步重置，不得再出现 stale-readback
  - `animation/graph/clock` 这一批已完成 widget-local create merge：`tinyui/src/widgets/{animation,graph,clock}.c` 现在直接完成 backend child 建树、attach 与 `host_widget` 绑定，不再经由 `picoui_backend_create_animation/graph/clock()`
  - `animation` 当前已进一步完成 widget-specific helper 收口：`source/period_ms/show_frame` 逻辑已并回 `tinyui/src/widgets/animation.c`，`backend.h` 中这 3 个 helper 声明已删除，`backend_animation.c` 已从 CMake 退编删除；`graph` 这轮也已进一步完成 widget-specific helper 收口：`axis/axis_offset/frame_space/grid_offset/point_mask/add_series/set_value/move_add/get_*` 已并回 `tinyui/src/widgets/graph.c`，`backend.h` 中对应 helper 声明已删除，`backend_graph.c` 已从 CMake 退编删除；`clock` 则更早已把 `background/pointer/time` 等 helper 全部并回 widget，并完成 `backend_clock.c` 退编删除
  - 本批 focused proof 当前应以 `test_picoui_animation`、`test_picoui_graph`、`test_picoui_clock` 为准，重点守住 `parent/root/owner/host_widget/ld_widget/ldBase.pInfo` 的 direct-create tree/binding 合同；其中 `animation` 继续保留轻量 `create/init` 语义，native attach 早已并回 `create_with_props()`，这轮又补强了 init 顺序和跨行 sprite sheet frame 定位合同；`graph` 这轮继续锁定 corrupted backend binding 下 host/native 双侧都不得漂移，以及 `point_mask width > frame_space` 时 native 合法自适应必须成功并同步回 public state；`clock` 则已补强 `use_system_time/step_second/background/pointer/mask/anchor` 的 native 状态断言
  - `message_box/keyboard/table` 这一批已完成 widget-local create merge：`tinyui/src/widgets/{message_box,keyboard,table}.c` 现在直接完成 backend child 建树、attach 与 `host_widget` 绑定，不再经由 `picoui_backend_create_message_box/keyboard/table()`
  - `message_box` 这轮已继续完成 widget-specific helper 收口：`title/message/confirm_text/buttons/string_colors/button_colors/bg_color/callback bridge` 真相现已全部并回 `tinyui/src/widgets/message_box.c`；`backend.h` 中对应 helper 声明已删除，`backend_message_box.c` 已从 `cmake/LingDongGUI.cmake` 退编并删除
  - `keyboard/table` 这一子批次当前都已完成 compile-surface cleanup：`backend_keyboard.c` 更早已退场；本轮 `backend_table.c` 也已从 CMake 退编并删除，`table` 不再保留独立 helper-only backend 文件
  - 本批 focused proof 当前应以 `test_picoui_message_box`、`test_picoui_keyboard`、`test_picoui_table` 为准；其中 `message_box` 继续锁定 `parent/root/owner/host_widget/ld_widget/ldBase.pInfo` 的 direct-create tree/binding 合同，以及 widget 文件直接驱动 native title/message/buttons/colors/callback bridge 的真相；`table` 当前 focused proof 已覆盖 `current_cell sync / editable commit-cancel / image-button / static_text / keyboard binding / align-grid / region getter` 等真实合同，并新增锁定 `picoui_backend_table_bind_host()` 不再以 backend 符号形式暴露
  - `table` 这轮已完成 compile-surface cleanup：原先 `backend_table.c` 中承载过的 `picoui_backend_table_bind_host()`、`native_slot`、`set/get_keyboard_binding()`、`set_selected_cell()`、`set_current_cell()`、`set/get_cell_text()`、`set_cell_editable()`、`set/get_item_align()`、`get_item_editable()`、`get_item_region()`、`set_excel_type()`、`set_background_color()`、`set_item_static_text()`、`set_item_font()`、`set_item_width()`、`set_item_height()`、`set_item_color()`、`set_item_image()`、`set_item_button()`、`navigate()` 与 `sync_current_cell()` 已全部并回 `tinyui/src/widgets/table.c` 的 widget-local/native 路径；`backend_table.c` 已不再保留任何 production retained seam
  - `table` 这轮补上了 `create_with_props()` 的 props-stage 失败回滚合同：若 `keyboard_binding` 在 props 应用阶段失败，必须回滚已 attach 的 backend child，不得残留 `parent/owner/root/host_widget`、event bridge、`ldBase.pInfo` 或 sibling 链脏状态
  - `radial_menu/icon_slider/switch` 这一批已完成 widget-local create merge：`tinyui/src/widgets/{radial_menu,icon_slider,switch}.c` 现在直接完成 backend child 建树、attach 与 `host_widget` 绑定；其中 `switch` 也已收掉 widget 文件内自留的 legacy `picoui_backend_create_switch()` 边界
- `backend_switch.c` 已确认只是空编译单元，不再承载 shared helper，因此已从 `cmake/LingDongGUI.cmake` 退编并删除；`icon_slider/radial_menu` 这轮也已进一步完成 widget-specific helper 收口：`item add/select/click/native bind`、`horizontal/speed` 等逻辑已全部并回 `tinyui/src/widgets/{icon_slider,radial_menu}.c`，`backend.h` 中对应 helper 声明已删除，`backend_icon_slider.c`、`backend_radial_menu.c` 已从 `cmake/LingDongGUI.cmake` 退编并删除
  - 本批 focused proof 当前应以 `test_picoui_radial_menu`、`test_picoui_icon_slider`、`test_picoui_switch` 为准，重点守住 `parent/root/owner/host_widget/ld_widget/ldBase.pInfo` 的 direct-create tree/binding 合同
  - `label/text` 这轮已进一步完成 shared helper 收口：`label` 的 `text_color/bg_color/transparent/align/background_source/get_*` 与 `text` 的 `font/static_text/transparent/text_color/bg_color/background_source/scroll_*` 真相继续保留在 `tinyui/src/widgets/{label,text}.c`；与此同时，原先留在 `backend_label.c` 的 `set_text/style_class/user_data` shared bridge 已并回 `tinyui/src/core/widget.c`，`label` 仅剩的 `font` bridge 也已并回 `tinyui/src/widgets/label.c`，因此 `backend_label.c` 已从 `cmake/LingDongGUI.cmake` 退编并删除
  - 本批 focused proof 当前应以 `test_picoui_label`、`test_picoui_text`、`test_picoui_widgets` 为准；其中 `label/text` 均已锁定 `create_with_props()` 失败回滚不得在 parent child 链遗留半挂载 backend child，`text` 还继续锁定 runtime font rebind 与 fail-next seam 的原子性合同
  - `progress_wheel` 这轮已进一步完成 widget-specific helper 收口：`percent/get_percent/wheel_color/dot_color/dot_enabled/dirty-region disable` 与 rollback test seam 真相现已全部并回 `tinyui/src/widgets/progress_wheel.c`；`backend_progress_wheel.c` 已从 `cmake/LingDongGUI.cmake` 退编并删除，`backend.h` 中对应 production helper 声明已删除
  - 本批 focused proof 当前应以 `test_picoui_progress_wheel` 为准；重点锁定 `create_with_props()` 失败回滚不得在 parent child 链遗留半挂载 backend child，同时保持 native `iProgress`、wheel/dot color、dot enabled 和 dirty-region 合同不回退
  - `layout` 当前 focused/runtime 证据已恢复并通过：`test_picoui_layout`、`test_picoui_widgets`、`check_picoui_visible_ui`、`check_picoui_backend_mapping`
  - `layout` 新增并锁定的真实合同包括：
    - 显式 `flex` padding 在后续 `flow/align/gap` 更新后不得回退
    - 显式 `grid` padding 在后续 `columns/rows/gap/align` 更新后不得回退
    - `padding_group` 在后续 `flex/grid` 配置更新后必须持续生效
    - grid track 从长模板切到短模板时，传给 `ldWindow` 的 descriptor tail 必须正确终止，不得残留旧模板
    - native binding 缺失时，layout setter 必须拒绝并保持 public state 不变
    - `picoui_widget_set_padding()` 的 window/background 分支、`picoui_window_set_padding()`、`picoui_window_set_grid_padding()` 已直接落到 `tinyui/src/widgets/window.c` 的 native apply + backend cache 路径；`test_picoui_layout` 直接锁定 `picoui_backend_widget_set_padding()`、`picoui_backend_window_set_padding()`、`picoui_backend_window_set_grid_padding()` 不得再出现在 `libpicoui_backend_ldgui.a`
    - `picoui_window_set_layout_type()`、`picoui_flex_set_flow()`、`picoui_flex_set_align()`、`picoui_flex_set_gap()` 现已直接落到 `tinyui/src/widgets/window.c` 的 native apply + state/cache 路径；`test_picoui_layout` 直接锁定 `picoui_backend_window_set_layout_type()`、`picoui_backend_window_set_flex_flow()`、`picoui_backend_window_set_flex_align()`、`picoui_backend_window_set_flex_gap()` 不得再出现在 `libpicoui_backend_ldgui.a`
    - `picoui_grid_set_columns()`、`picoui_grid_set_rows()`、`picoui_grid_set_gap()`、`picoui_grid_set_align()` 现已直接落到 `tinyui/src/widgets/window.c` 的 state/cache/native grid 路径；`test_picoui_layout` 直接锁定 `picoui_backend_window_set_grid_columns()`、`picoui_backend_window_set_grid_rows()`、`picoui_backend_window_set_grid_gap()`、`picoui_backend_window_set_grid_align()` 不得再出现在 `libpicoui_backend_ldgui.a`
    - `picoui_window_set_gap()` 现已直接落到 `tinyui/src/widgets/window.c` 的 layout-neutral gap apply 路径，保持 `layoutTpye` 不漂移；残余 shared helper `picoui_native_align_to_ld_grid()` 已迁到 `tinyui/src/core/native.c`；`test_picoui_layout` 直接锁定 `picoui_backend_window_set_gap()` 与 `backend_layout.c.o` 都不得再出现在 `libpicoui_backend_ldgui.a`
    - `picoui_widget_set_flex_grow()`、`picoui_widget_set_flex_new_track()`、`picoui_widget_set_ignore_layout()`、`picoui_widget_set_grid_cell()` 已直接落到 `tinyui/src/core/widget.c` 的 native apply + child-layout cache 路径；`test_picoui_layout` 直接锁定这 4 个 legacy backend 符号不得再出现在 `libpicoui_backend_ldgui.a` 或测试二进制中
    - child layout setter 在 backend `ld_widget` 非空但 native `widgetType` 与 backend `kind` 不一致时，必须 `fail-closed` 返回 `-1`，且不得污染既有 public/native layout 状态
  - `list` 这一小步已进一步完成 compile-surface cleanup：原先留在 `backend_list.c` 的 `selected_index/get_selected_index/sync_selected_index` 兼容 helper 现已迁到 `tinyui/src/widgets/list.c` 的 widget-local hidden helper，不再保留独立 backend 文件
  - `list` 当前 selected-index 真相已改为 widget-local/native readback：`backend_event.c` 的 list selection restore/native dispatch 现已改走 `tinyui/src/widgets/list.c` 内部 helper；与此同时，corrupted-binding 负向合同继续要求 selected-index sync 在 backend `kind/ld_widget` 绑定损坏时返回 `-1`，不得污染 `list->selected_index` / `backend->value` 缓存
  - `keyboard` 新增 corrupted-binding 负向合同：`picoui_backend_keyboard_button_update()` 在 `kind/ld_widget` 绑定损坏时必须返回 `-1`，不得把损坏绑定误判成合法 keyboard，也不得污染 native `keyCode/isKeySelect` 或 callback 计数
  - `keyboard` 这轮又进一步完成最小 shared/widget 边界收口：`picoui_backend_keyboard_click()` 与 `picoui_backend_keyboard_exit()` 已并回 `tinyui/src/widgets/keyboard.c` 的 widget-local retained seam；`test_picoui_keyboard` 当前直接锁定这两个 legacy backend 符号不再以 public/backend 形式暴露
  - `keyboard` 这轮又进一步完成最小 shared/widget 边界收口：`picoui_backend_keyboard_update()` 与 `picoui_backend_keyboard_button_update()` 也已并回 `tinyui/src/widgets/keyboard.c` 的 widget-local retained seam；`test_picoui_keyboard` 当前继续直接锁定这两个 legacy backend 符号不再以 public/backend 形式暴露，同时保持 native `pBtnList/isWaitInit/keyCode/isKeySelect` 与 callback 合同不回退
  - `keyboard` 这轮已完成 compile-surface cleanup：剩余的 `input_ascii/navigate` 也已并回 `tinyui/src/widgets/keyboard.c` 的 widget-local/native 路径，`backend_keyboard.c` 已从 `cmake/LingDongGUI.cmake` 退编并删除；`test_picoui_keyboard` 当前继续直接锁定 legacy `picoui_backend_keyboard_input_ascii()` / `picoui_backend_keyboard_navigate()` 不得再以 public/backend 形式暴露
  - `table` 的 corrupted-binding、current-cell readback、navigate、rollback 与 archive symbol absence 合同当前都已继续保留在 `test_picoui_table`，且新增锁定 `libpicoui_backend_ldgui.a` 不得再包含 `backend_table.c.o`
  - `table` 当前已不再是 `V2` 的 retained-boundary 候选；它的 widget-specific backend seam 已清零退场，`V2` 最后一批落在 SDL host runtime compile-surface cleanup
  - `backend_layout.c` 这轮已完成 compile-surface cleanup：`padding trio`、`flex quartet`、`grid quartet`、`generic gap tail` 和 child-layout setter quartet 都已从 production surface 收回 shared/widget 路径；残余 shared helper `picoui_native_align_to_ld_grid()` 也已迁到 `tinyui/src/core/native.c`，`cmake/LingDongGUI.cmake` 已将 `backend_layout.c` 退编并删除
  - `layout` 这轮又补上 window padding/grid padding 的 corrupted-binding 负向合同：`picoui_window_set_padding()` / `picoui_window_set_grid_padding()` 在 backend `ld_widget` 非空但 native `widgetType` 损坏时，必须 `fail-closed` 返回 `-1`，且不得提前污染 backend cache 或 native `flexPadding/gridPadding` 状态
  - `layout` 这轮又补上 grid columns/rows/gap/align 的 corrupted-binding 负向合同：`picoui_grid_set_columns()` / `picoui_grid_set_rows()` / `picoui_grid_set_gap()` / `picoui_grid_set_align()` 在 backend `ld_widget` 非空但 native `widgetType` 损坏时，必须 `fail-closed` 返回 `-1`，且不得污染 backend cache 或 native `ldWindow` grid descriptor/gap/align 状态
  - `backend_event.c` 这一小步已继续完成 shared dispatch 收口：`picoui_backend_emit_value_changed()`、`picoui_backend_emit_event()`、`picoui_backend_emit_clicked()` 已并入 `tinyui/src/core/widget.c`，`picoui_widget_dispatch_signal()` / `picoui_widget_dispatch_event()`、`picoui_widget_sync_ld_value()`、`picoui_widget_emit_ld_event_bridge()` 也已并入 `tinyui/src/core/event.c`；legacy `picoui_backend_widget_dispatch_*`、`picoui_backend_sync_ld_value()`、`picoui_backend_emit_ld_event_bridge()` 不再继续以 backend 符号形式暴露
  - `backend_event.c` 这轮又进一步完成 shared setter-helper 收口：`picoui_backend_widget_update_value()` 已并入 `tinyui/src/core/widget.c` 的 `picoui_widget_update_value()`，`checkbox/switch/slider` 的 setter/data-model path 现已不再依赖 backend 符号；`test_picoui_widgets` 继续直接扫描测试二进制符号表，锁定 legacy `picoui_backend_widget_update_value()` 不得再出现
  - `backend_event.c` 这轮又进一步完成 shared binding 收口：`picoui_backend_widget_bind_host()` 与 `picoui_backend_widget_bind_ld_event_bridge()` 已迁到 `tinyui/src/core/runtime_bridge.c`，不再继续挂在 `backend/` 路径；`test_picoui_widgets` 新增合同锁定 `bind_ld_event_bridge()` 在 native `ld_widget` 缺失时必须 `fail-closed` 且不得脏写 `ld_event_bridge_scene/sender`，并继续锁定 legacy `picoui_backend_sync_ld_value()` / `picoui_backend_emit_ld_event_bridge()` 不得再出现在测试二进制符号表
  - `backend_event.c` 这轮又完成 native dispatch 主链收口：`picoui_backend_widget_dispatch_native_signal()` 与 rejected-list-selection restore 已迁到 `tinyui/src/core/event.c`，shared/internal 入口统一命名为 `picoui_widget_dispatch_native_signal()`；`tinyui/src/core/runtime_bridge.c` 中的 `ldMsg` bridge slot 现已直接走 shared/core 路径
  - `test_picoui_widgets` / `test_picoui_keyboard` 当前继续直接锁定 legacy `picoui_backend_widget_dispatch_native_signal()` 与 `backend_event.c.o` 不得再出现在测试二进制或 `libpicoui_backend_ldgui.a`；同时新增 list hidden/disabled reject 路径合同，要求 native dispatch 回退 selected-index 时不得污染 native/current cache，也不得误发业务 callback
  - `test_picoui_button_events` 当前还直接扫描测试二进制符号表，锁定 legacy `picoui_backend_widget_dispatch_signal()` / `picoui_backend_widget_dispatch_event()` 不得再以 backend 符号形式出现；同时保持 pressed/released/value-changed 的 callback、focus claim、dispatch_count 与 invisible/disabled fail-closed 合同不回退
  - `backend_event.c` 当前已完成 compile-surface cleanup：shared dispatch/setter/binding/native-dispatch 真相已分别并回 `tinyui/src/core/widget.c`、`tinyui/src/core/event.c`、`tinyui/src/core/runtime_bridge.c`，`cmake/LingDongGUI.cmake` 已将 `backend_event.c` 退编并删除文件本体
  - `backend_app.c` 前序 shared seam 已全部收口：timer 已迁到 `tinyui/src/core/app.c`，pointer bridge / app init / run / shutdown / ensure-window 已迁到 `tinyui/src/core/runtime_bridge.c`，legacy `picoui_backend_runtime_step()` 也已清零
  - `backend_app.c` 文件本体已在最后一批从 `tinyui/src/backend/ldgui/backend_app.c` 迁到 `tinyui/src/core/runtime_host.c`，继续承载 SDL host runtime、visible smoke marker、capture 写图与 internal `picoui_backend_step_app()`；它已不再占用 `backend/*.c` compile surface
  - `picoui_backend_ldgui` 与 `picoui_backend_ldgui_runtime` 当前都编译 `tinyui/src/core/runtime_host.c`；`check_picoui_runtime.py` 的 runtime screen-define 真相源也已同步切到 `runtime_host.c`
  - `test_picoui_app_lifecycle` 与 `test_picoui_native_bridge` 当前已改守 `runtime_host.c` / `runtime_bridge.c` 的新边界；`check_tinyui_v21_transition_guards.py` 现已通过，实测真相是 `backend_c_files=0`
  - `backend_widget.c` 已在本批 shared-helper 迁位后清零退场：`picoui_native_nav_dir_to_ld()`、`picoui_backend_widget_init_data_model()`、`picoui_backend_widget_claim_focus()`、`picoui_backend_widget_release_focus()` 统一落到 `tinyui/src/core/widget.c`，对应编译面条目已从 `cmake/LingDongGUI.cmake` 移除，文件本体也已删除
  - `theme` 这一小步也已完成 shared bridge 收口：`tinyui/src/theme/theme.c` 中 `picoui_app_set_theme()` 现已直接调用 `picoui_runtime_bridge_bind_theme()`，不再经由 `backend_theme.c` 的薄包装；对应 `backend_theme.c` 已从 `cmake/LingDongGUI.cmake` 退编并删除
  - `theme` 这一小步也已完成 shared style apply 收口：`picoui_theme_apply_to_widget()` 现已在 `tinyui/src/theme/theme.c` 内直接完成 `window/label/text/button/checkbox/switch/slider/list/image/calendar` 的 native style dispatch，不再经由 `backend_style_apply.c`
  - `backend_widget_tree.c` 这一步也已完成 shared helper 收口：`picoui_backend_widget_is_kind()` 已并入 `tinyui/src/core/widget.c`，`picoui_backend_widget_unbind_host()` / `picoui_backend_widget_detach_from_parent()` 已并入 `tinyui/src/core/runtime_bridge.c`；对应 `backend_widget_tree.c` 已从 `cmake/LingDongGUI.cmake` 退编并删除
  - 本批 focused proof 当前以 `test_picoui_widgets`、`test_picoui_image`、`test_picoui_qrcode` 为准：继续锁定 shared widget-tree helper 在 null 输入下必须 fail-closed，且 image/qrcode rollback 路径继续经由 shared helper 完成 detach/unbind
  - `backend_style_apply.c` 已从 `cmake/LingDongGUI.cmake` 退编并删除；对应 retained/shared style 边界现已收敛到 `theme.c`，不再保留独立 backend style apply 层
  - `backend_label.c` 之后最后残留的 `backend_app.c` 也已完成 compile-surface cleanup；`V2 backend-to-widgets` 当前已把 `backend/*.c` 清零
