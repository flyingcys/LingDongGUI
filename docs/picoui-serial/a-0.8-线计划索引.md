# PicoUI a-0.8 线计划索引

- 当前仓库硬规则：`AGENTS.md`
- v0.7 review 缺口清单：`docs/picoui-serial/a-0.8-v0.7-native-100-review-gap-list.md`
- 当前设计 spec：`docs/superpowers/specs/2026-06-02-picoui-a-0-8-native-api-exhaustiveness-design.md`
- 当前执行 plan：`docs/superpowers/plans/2026-06-02-picoui-a-0-8-native-api-exhaustiveness-implementation.md`
- 当前机器真相源入口：`tests/picoui/contract/picoui_release_capability_matrix.json`

## a-0.8 线定位

`a-0.8` 是 `v0.7 native-100` 的真实性修正线。

`v0.7` 已做到 `27/27` widget-like 控件进入 PicoUI 盘点，但不能诚实写成“100% 控件能力覆盖”。主要原因是 `v0.7` gate 仍以手写 inventory / matrix 自洽为主，没有从 `src/gui/ld*.h` 自动抽取全部 public API 后逐项证明 PicoUI public API、真实 backend 字段、unit、runtime/mapping/visible/manual 证据闭环。

`a-0.8` 的唯一目标：

1. 建立 `ldgui_public_api_inventory`，从 `src/gui/ld*.h` 自动抽取 public native API。
2. 建立 native API exhaustiveness gate，让遗漏能红。
3. 生成全量 `native API gap ledger`，先证明当前代码到底缺哪些能力。
4. 修复 ledger 中所有真实缺口；`text / image / window/layout / line_edit / keyboard / slider / button / qrcode` 只是已知高风险样本，不是完整范围。
5. 清理 `message_box / keyboard` 旧 special-case 证据冲突。
6. 禁止 temporary smoke layout 参与 full parity 结论。
7. 最终才能重新声明“100% 控件覆盖 + 对应控件 100% 能力覆盖”。

## worktree

- 目录：`.worktree/a-0.8`
- 建议分支：`feat/picoui-a-0-8-native-api-exhaustiveness`

创建或切换后必须执行：

```bash
git submodule sync --recursive
git submodule update --init --recursive
```

## 串行依赖

固定依赖链：

1. `R0`：native API inventory / extractor
2. `R1`：exhaustiveness matrix gate
3. `R2`：shared readback / resource / smoke-path policy
4. `R3`：全量 gap ledger 分批修复 A：base / layout / text / image / old widgets
5. `R4`：全量 gap ledger 分批修复 B：input / navigation / data widgets
6. `R5`：全量 gap ledger 分批修复 C：visual / instrument / composite / remaining widgets
7. `R6`：message_box / keyboard / manual artifact evidence 修复
8. `R7`：matrix / runtime / mapping / visible / manual gate closeout
9. `R8`：文档 closeout / release readiness review

顺序原因：

1. `R0/R1` 不先完成，后续修复无法证明“全量能力”。
2. `R2` 不先冻结 readback 和 smoke-path policy，各控件会继续各自定义完成口径。
3. `R3~R5` 的具体控件清单必须来自 `R0/R1` 生成的 gap ledger；不能只修 review 文档列出的样本。
4. `R6/R7/R8` 必须最后串行收口证据和文档。

## subagent 拓扑

推荐拓扑：

1. 主线程
   - 决策阶段边界
   - 跑 GitNexus impact
   - 派发 fresh subagent
   - 收敛 review 结果
   - 做最终验证
2. `SG-0.8-R0`
   - 只做 native API extractor / inventory
3. `SG-0.8-R1`
   - 只做 exhaustiveness checker / matrix schema
4. `SG-0.8-R2`
   - 只做 shared policy / smoke path gate
5. `SG-0.8-R3`
   - 只做 ledger A：base / layout / text / image / old widgets
6. `SG-0.8-R4`
   - 只做 ledger B：input / navigation / data widgets
7. `SG-0.8-R5`
   - 只做 ledger C：visual / instrument / composite / remaining widgets
8. `SG-0.8-R6`
   - 只做 `message_box / keyboard manual evidence`
9. `SG-0.8-R7`
   - 只做 final matrix / gate / artifact catalog
10. `SG-0.8-Review`
   - 每阶段独立 review

规则：

1. 每个阶段结束必须 review。
2. review 不通过时由原阶段 subagent 修复。
3. 涉及 C / Python 符号修改前必须跑 GitNexus impact。
4. `R3~R5` 可并行探索，但合流必须串行，避免 matrix / public API / CMake 写面冲突。

## closeout 标准

`a-0.8` 完成时至少满足：

1. `ldgui_public_api_inventory.json` 覆盖所有 `src/gui/ld*.h` public native API。
2. `check_picoui_native_api_exhaustiveness.py` 通过，且 allowlist 只含 lifecycle-only、render-only、internal helper、非 widget public API 或明确 backend-private extension，并逐项带 reason。
3. release matrix capability rows 与 native API inventory 完全对齐。
4. `native API gap ledger` 中所有 `missing_picoui_api / missing_backend_proof / missing_unit / missing_gate / overwrapped` 项全部关闭。
5. `message_box / keyboard` 旧 manual special-case 被 fresh evidence 替换或从 full parity 结论剥离。
6. temporary smoke layout 不参与 native-100 formal mapping / visible / manual 结论。
7. `manual_artifact` 明确区分 artifact exists、manual review required、manual reviewed passed。
8. 全套 contract / unit / runtime / mapping / visible / manual gate 通过。
