# PicoUI a-0.4 线计划索引

- 当前仓库硬规则：`AGENTS.md`
- 当前 truth-source 入口：`docs/picoui-serial/a-0.3/README.md`
- 当前 closeout review：`docs/superpowers/reviews/2026-05-31-picoui-a-0-3-closeout-review.md`
- 当前设计 spec：`docs/superpowers/specs/2026-05-31-picoui-a-0-4-input-shared-core-design.md`
- 当前执行 plan：`docs/superpowers/plans/2026-05-31-picoui-a-0-4-input-shared-core-implementation.md`
- 当前后续版本总记录：`docs/picoui-serial/a-0.4-a-0.6-后续版本记录.md`

## a-0.4 线定位

`a-0.4` 线不是“再包四个控件”。

它是 PicoUI 的输入 shared-core 线，只做一件事：

1. 先把输入/焦点/选择的 shared-core 做实。
2. 再在这套 shared-core 上串行接入 `line_edit / keyboard / combo_box / scroll_selecter`。

## worktree

- 目录：`.worktree/a-0.4`
- 建议分支：`feat/picoui-a-0-4-input-shared-core`

创建或切换后必须执行：

```bash
git submodule sync --recursive
git submodule update --init --recursive
```

## a-0.4 线做什么

1. 清理 runtime 的通用补 layout 路径，恢复 backend/layout honesty。
2. 冻结 getter truth policy：哪些回 backend，哪些只是 host cache，哪些暂不暴露。
3. 建立 focus ownership。
4. 建立 editable text contract。
5. 建立 keyboard bridge。
6. 建立 selection / navigation contract。
7. 建立 dropdown contract。
8. 串行接入 `line_edit / keyboard / combo_box / scroll_selecter`。

## a-0.4 线不做什么

1. 不做 `table / graph / calendar`。
2. 不做 `arc / gauge / icon_slider / radial_menu`。
3. 不允许用 demo 硬编码或 runtime 补位制造 visible PASS。
4. 不允许把 host cache getter 写成 backend truth。
5. 不允许四个控件并行平推。

## 严格串行顺序

固定顺序：

1. `a-0.4-R0 honesty + readback policy`
2. `a-0.4-R1 focus ownership`
3. `a-0.4-R2 editable text contract + line_edit`
4. `a-0.4-R3 keyboard bridge + keyboard`
5. `a-0.4-R4 selection / navigation contract`
6. `a-0.4-R5 dropdown contract + combo_box`
7. `a-0.4-R6 scroll_selecter`
8. `a-0.4-R7 matrix / gate / docs closeout`

顺序原因：

1. 不先处理 `R0`，visible/gate 还会继续把补位后的 UI 当成真实结果。
2. 不先处理 `R1`，`line_edit` 和 `keyboard` 会互相卡住。
3. 不先处理 `R4`，`combo_box` 与 `scroll_selecter` 会各自重造 selection 逻辑。

## shared-owner 写面

以下文件默认由 `a-0.4` shared-core 阶段独占写权：

- `picoui/src/core/internal.h`
- `picoui/src/core/widget.c`
- `picoui/src/core/event.c`
- `picoui/src/backend/ldgui/backend.h`
- `picoui/src/backend/ldgui/backend_widget.c`
- `picoui/src/backend/ldgui/backend_event.c`
- `picoui/src/backend/ldgui/backend_app.c`
- `picoui/src/backend/ldgui/backend_layout.c`
- `tests/picoui/runtime/check_picoui_backend_mapping.py`
- `tests/picoui/runtime/check_picoui_visible_ui.py`
- `tests/picoui/contract/picoui_release_capability_matrix.json`
- `tests/picoui/contract/check_picoui_release_capability_matrix.py`

以下文件按阶段接入：

- `picoui/include/picoui/line_edit.h`
- `picoui/include/picoui/keyboard.h`
- `picoui/include/picoui/combo_box.h`
- `picoui/include/picoui/scroll_selecter.h`
- `picoui/src/widgets/*`
- `picoui/src/backend/ldgui/backend_*`
- `picoui/demo/*`
- `tests/picoui/unit/*`

## subagent 串行拓扑

推荐拓扑：

1. 主线程
   - 冻结阶段边界
   - 跑 GitNexus impact
   - 收敛 matrix / gate / 文档
   - 做最终验证
2. `SG-0.4-R0`
   - 只做 honesty + readback policy
3. `SG-0.4-R1`
   - 只做 focus ownership
4. `SG-0.4-R2`
   - 只做 editable text + `line_edit`
5. `SG-0.4-R3`
   - 只做 keyboard bridge + `keyboard`
6. `SG-0.4-R4`
   - 只做 selection / navigation
7. `SG-0.4-R5`
   - 只做 dropdown + `combo_box`
8. `SG-0.4-R6`
   - 只做 `scroll_selecter`
9. `SG-0.4-Review`
   - 每阶段独立 review

规则：

1. 任意时刻只允许一个写 subagent。
2. review 不通过，由同阶段 subagent 修，不新开主线程修。
3. 阶段间必须主线程验收后再进入下一阶段。

## 每阶段最小验收

每阶段结束至少执行：

```bash
rtk cmake -S . -B build -DUSE_DEMO=0
rtk cmake --build build
ctest --test-dir build -L picoui --output-on-failure
python3 tests/picoui/contract/check_picoui_public_api.py
git diff --check
```

涉及 mapping / visible / runtime / release matrix 时，再追加：

```bash
python3 tests/picoui/contract/check_picoui_release_capability_matrix.py
python3 tests/picoui/runtime/check_picoui_backend_mapping.py
python3 tests/picoui/runtime/check_picoui_visible_ui.py --all
python3 tests/picoui/runtime/check_picoui_runtime.py
```

对 `R0 / R1 / R4` 必须额外验证：

1. frame/event 后 getter 与 backend truth 一致
2. hidden / disabled / focus transfer 不失真
3. 不再依赖通用补 layout 逻辑

## closeout 标准

`a-0.4` 完成时至少要满足：

1. `line_edit / keyboard / combo_box / scroll_selecter` 全部进入 PicoUI public widget。
2. 输入 shared-core 不再依赖 demo 补丁或 runtime 补位。
3. getter truth policy、matrix、中文文档完全一致。
4. `a-0.5` 可以直接拿 `focus / edit / navigation` 合同做输入前置，不再返工 `0.4`。
