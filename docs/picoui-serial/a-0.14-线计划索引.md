# PicoUI a-0.14 线计划索引

- 当前仓库硬规则：`AGENTS.md`
- 当前能力索引与对照入口：`docs/ability/README.md`
- 当前机器真相源入口：
  - `tests/picoui/contract/ldgui_public_api_inventory.json`
  - `tests/picoui/contract/native_api_gap_ledger.json`
  - `tests/picoui/contract/picoui_release_capability_matrix.json`

## a-0.14 线定位

`a-0.14` 不是重新做一遍 `a-0.8` exhaustiveness，而是基于当前 worktree 再做一轮“代码 + 文档 + 复核”闭环，目标是继续逼近你要求的 user-facing 100%。

本线的硬要求：

1. 不能只看 `docs/ability`。
2. 必须以当前代码、public header、backend 映射、focused test、contract truth 为准。
3. 每修掉一个真实缺口，都要同步更新 `docs/ability`。
4. 每轮修复后都要重新启动只读 subagent 复核，而不是只靠主线程自证。

## 当前真实缺口

基于当前仓库代码、focused test 和 `docs/ability`，这轮代码面已把最后已知缺口 `background` 独立 public widget` 补上；下一步是继续同步 contract 三件套与独立复核证据，确认是否还有隐藏代码级缺口。

## 本轮顺序

固定顺序：

1. 已先修 `keyboard` 单键自定义绘制
2. 已补 `background` 独立 public widget`
3. 现在进入重新复核与 contract/document truth 对齐

顺序原因：

1. `keyboard key draw` 已在本线先行修补。
2. `background` public widget 代码与 focused test 已补；剩余风险转移到 contract 三件套与独立复核是否仍保留旧结论。

## 当前文档纪律

- `docs/ability` 允许同时记录两种口径，但必须写清：
  - `native symbol parity`
  - `user-facing capability equivalence`
- 不允许再把：
  - `strict direct-wrapper 清零`
  - `policy complete`
  - `native/user-facing 100%`
  混成一件事。

## 当前进度

- `a-0.14` 已启动。
- 已完成：
  - 新一轮只读 subagent 审计
  - `docs/ability` 的总览误导点收紧
  - `picoui_keyboard_set_draw_callback()` + backend + focused test
  - `docs/ability/keyboard.md` 与总览同步到当前代码事实
  - `picoui_background_*` public widget + app root contract + focused test
- 下一步：
  - 同步 contract 三件套旧 policy 口径
  - 新启动只读 subagent，重新从代码 + contract + docs 复核是否仍有真实缺口
