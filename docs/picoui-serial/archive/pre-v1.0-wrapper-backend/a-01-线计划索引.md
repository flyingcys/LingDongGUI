# PicoUI a-01 线计划索引

- 并行拓扑说明：本文只定义 `a-01` worktree 的长期职责、写面边界和合并节奏；不替代 `H线/J线` 作为发布真相源。
- 当前仓库硬规则：`AGENTS.md`
- 当前发布真相源：`docs/picoui-serial/H-线计划索引.md`
- 当前 `v0.1/v0.2` 边界：`docs/picoui-serial/J-线计划索引.md`
- 当前 9 控件发布合同：`docs/picoui-serial/H-线当前9控件发布合同.md`

## a-01 线定位

`a-01` 线负责当前已经 `wrapped` 的 PicoUI 控件中，仍未真正收口的 backlog 控件能力补齐。

它的唯一目标不是扩新控件，而是把现有 `5` 个 backlog 控件做实：

- `image`
- `text`
- `checkbox`
- `switch`
- `list`

`a-01` 线是当前 PicoUI 并行拓扑中的 shared-owner 线。凡是容易影响多控件语义的共享层，默认由 `a-01` 持有写权。

## worktree

- 目录：`.worktree/a-01`
- 建议分支：`feat/picoui-a-01-wrapped-backlog`

创建或切换后必须执行：

```bash
git submodule sync --recursive
git submodule update --init --recursive
```

## a-01 线做什么

1. 逐个串行收口当前 backlog 控件。
2. 允许修改共享 widget/backend/style/event 合同。
3. 为 backlog 控件补齐 unit / contract / mapping / visible 证据。
4. 收紧已支持能力与 reject/deferred/incomplete_contract 边界。
5. 为主线程提供可小批量 merge-back 的 clean slice。

## a-01 线不做什么

1. 不新增 `progress_bar`、`qrcode`、`message_box`、`line_edit`、`combo_box`。
2. 不把 `H线/J线` 发布 closeout 文档当成当前开发索引去重写。
3. 不靠 demo 硬编码或 fake visual 补丁掩盖 backend 缺口。
4. 不把 `wrapped` 偷换成 parity complete。

## 串行顺序

固定顺序：

1. `a-01-A1 image`
2. `a-01-A2 text`
3. `a-01-A3 checkbox`
4. `a-01-A4 switch`
5. `a-01-A5 list`

顺序原因：

- `image` 合同最薄，先做最容易把 reject/deferred 边界钉死。
- `text` 与字体、背景、readback 合同相关，但交互耦合低。
- `checkbox` / `switch` 都会碰 shared style/event 语义，放在中段连续处理。
- `list` 最容易扩散到 callback、marker、shared backend 口径，必须最后做。

## 当前实施状态

当前 `a-01` 已按既定顺序把 backlog 五控件收口到当前代码口径：

1. `A1 image` 已完成
2. `A2 text` 已完成
3. `A3 checkbox` 已完成
4. `A4 switch` 已完成
5. `A5 list` 已完成

当前实现结论：

- `image`
  - `theme`、`bg/text/border/radius`、`enabled` 都维持明确 `reject`
  - `style_class / user_data` 只保持 metadata-only / `incomplete_contract`
- `text`
  - `text/font/bg/text_color/align` 读写合同已稳定
  - 仍未把更重资源系统或更高阶文本能力写成当前完成面
- `checkbox`
  - `checked` 与 native state 同步
  - toggled callback 走 native bridge，不是 shadow state 自转
- `switch`
  - `checked / disabled / toggled` 与 native state 口径一致
  - 未把 `direction/navigation` 等更重能力偷渡成 support
- `list`
  - `selected_index / on_selected callback cookie` 已稳定
  - `item marker` 明确不是独立 backend widget
  - widget-level `user_data` 与 callback cookie 明确分离

## 共享写面所有权

以下文件默认由 `a-01` 独占写权：

- `picoui/src/core/widget.c`
- `picoui/src/backend/ldgui/backend_style_apply.c`
- `picoui/src/backend/ldgui/backend_event.c`
- `picoui/src/backend/ldgui/backend_app.c`
- `tests/picoui/contract/picoui_release_capability_matrix.json`
- `tests/picoui/contract/check_picoui_release_capability_matrix.py`

以下文件 `a-01` 可改，但每个阶段只做本控件相关改动：

- `picoui/src/widgets/image.c`
- `picoui/src/widgets/text.c`
- `picoui/src/widgets/checkbox.c`
- `picoui/src/widgets/switch.c`
- `picoui/src/widgets/list.c`
- `picoui/src/backend/ldgui/backend_image.c`
- `picoui/src/backend/ldgui/backend_text.c`
- `picoui/src/backend/ldgui/backend_checkbox.c`
- `picoui/src/backend/ldgui/backend_switch.c`
- `picoui/src/backend/ldgui/backend_list.c`
- `tests/picoui/unit/test_picoui_widgets.c`
- `tests/picoui/unit/test_picoui_theme.c`
- `tests/picoui/unit/test_picoui_list.c`

## 与 02 线的隔离规则

1. `a-01` 不碰 `progress_bar`、`qrcode`、`message_box` 新控件文件。
2. `a-01` 只在阶段末尾才修改聚合入口文件。
3. 只要 `a-01` 正在改共享层，`a-02` 不得并行改 shared-owner 文件。
4. `a-02` 若确需接入共享聚合文件，先 rebase 到 `a-01` 最近 checkpoint，再做最小接入。

## 聚合文件规则

以下聚合文件不允许在日常开发中高频改动，统一留到阶段末尾一次性接入：

- `picoui/include/picoui/picoui.h`
- `tests/picoui/CMakeLists.txt`
- `tests/picoui/runtime/check_picoui_runtime.py`
- `tests/picoui/runtime/check_picoui_backend_mapping.py`
- `tests/picoui/runtime/check_picoui_visible_ui.py`
- `tests/picoui/contract/check_picoui_public_api.py`
- `tests/picoui/contract/check_picoui_demo_boundary.py`

## 每阶段最小验收

每个 `a-01-Ax` 阶段结束至少执行：

```bash
rtk cmake -S . -B build -DUSE_DEMO=0
rtk cmake --build build
ctest --test-dir build -L picoui --output-on-failure
python3 tests/picoui/contract/check_picoui_public_api.py
git diff --check
```

若阶段改到 mapping / visible / release matrix，再追加：

```bash
ctest --test-dir build -L mapping --output-on-failure
ctest --test-dir build -L visible --output-on-failure
python3 tests/picoui/contract/check_picoui_release_capability_matrix.py
```

注意：

- `runtime / visible / mapping` 共用本 worktree 的 `build/picoui-runtime`。
- `a-01` 内部也不要并行跑这些标签。
- `examples/common/Arm-2D` 与 `examples/common/perf_counter` 在 worktree 内必须保持完整 checkout；若 worktree 子模块目录退化成空目录壳，`picoui-runtime` 相关 gate 会直接失效。

## merge-back 节奏

不建议 `a-01` 五个阶段全做完再回灌。推荐三次回灌：

1. `image + text`
2. `checkbox + switch`
3. `list`

每次回灌前主线程至少检查：

```bash
git status --short --branch --ignore-submodules=all
git diff --stat
git diff --check
```

## 当前正确口径

- `a-01` 线是 backlog 收口线，不是发布 closeout 线。
- `a-01` 线可以提升当前 PicoUI 覆盖率和合同密度，但不自动等于 `release ready`。
- `a-01` 线任何阶段都必须继续遵守 `unit / contract / mapping / visible / manual_artifact` 五层证据边界。

## 当前验收结果

当前 worktree 已验证通过：

```bash
rtk cmake -S . -B build -DUSE_DEMO=0
rtk cmake --build build
ctest --test-dir build -L picoui --output-on-failure
python3 tests/picoui/contract/check_picoui_public_api.py
python3 tests/picoui/contract/check_picoui_release_capability_matrix.py
git diff --check
rm -rf build/picoui-runtime
ctest --test-dir build -R '^check_picoui_backend_mapping$' --output-on-failure
rm -rf build/picoui-runtime
ctest --test-dir build -R '^check_picoui_visible_ui$' --output-on-failure
rm -rf build/picoui-runtime
ctest --test-dir build -R '^check_picoui_runtime$' --output-on-failure
```

验证注意事项：

- `check_picoui_backend_mapping`
- `check_picoui_visible_ui`
- `check_picoui_runtime`

这三个 gate 必须严格串行跑，不能并行共用 `build/picoui-runtime`。
