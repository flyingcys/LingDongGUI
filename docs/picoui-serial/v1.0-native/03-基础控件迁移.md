# 03 基础控件迁移

## 状态

当前执行任务：`P3-H basic widgets native artifact`

`P3-A` 已完成 native contract、最小 native 状态路径和 pointer toggle 路径。`P3-B switch native migration` 已完成 native contract、最小 native 状态路径和 pointer toggle 路径。`P3-C slider native migration` 已完成 native range/value/render proof 和 pointer drag 路径。`P3-D text native migration` 已完成 UTF-8 text/readback/wrap-width render smoke。

## P3-A checkbox

### 范围

- 新增 `tests/picoui/native/test_picoui_native_checkbox.c`
- 新增 `picoui/src/native/native_checkbox.c`
- 修改 `picoui/src/widgets/checkbox.c`
- 修改 `picoui/src/native/native_render.c`
- 修改 `picoui/src/native/native_event.c`
- 修改 `cmake/LingDongGUI.cmake`
- 修改 `tests/picoui/CMakeLists.txt`

### 合同

- `picoui_checkbox_set_checked()` 可在 native 路径同步 checked state。
- native render traversal 可识别 checkbox，并记录 checkbox 最近一次渲染使用的 checked state。
- pointer down 命中 checkbox 时不触发 toggle。
- pointer up 仍命中同一 checkbox 时触发一次 `SIGNAL_VALUE_CHANGED`，checked state 反转，并调用 `on_toggled`。
- native checkbox 合同不依赖 `ldCheckBox*` 作为完成证明。

### RED 证据

首次 `ctest --test-dir build -R '^test_picoui_native_checkbox$' --output-on-failure` 失败于 pointer release 后 checked state 未反转：

```text
Assertion failed: (picoui_checkbox_is_checked(checkbox) == 1)
```

### GREEN 证据

`ctest --test-dir build -R '^test_picoui_native_checkbox$' --output-on-failure` 已通过。

## P3-B switch

### 范围

- 新增 `tests/picoui/native/test_picoui_native_switch.c`
- 新增 `picoui/src/native/native_switch.c`
- 修改 `picoui/src/widgets/switch.c`
- 修改 `picoui/src/native/native_render.c`
- 修改 `picoui/src/native/native_event.c`
- 修改 `cmake/LingDongGUI.cmake`
- 修改 `tests/picoui/CMakeLists.txt`

### 合同

- `picoui_switch_set_checked()` 可在 native 路径同步 checked state。
- native render traversal 可识别 switch，并记录 switch 最近一次渲染使用的 checked state。
- pointer down 命中 switch 时不触发 toggle。
- pointer up 仍命中同一 switch 时触发一次 `SIGNAL_VALUE_CHANGED`，checked state 反转，并调用 `on_toggled`。
- native switch 合同不依赖 `ldSwitch*` 作为完成证明。
- 本阶段保留旧 `picoui_backend_switch_*` 扩展接口，未进入 P3-C。

### RED 证据

首次 `cmake --build build --target test_picoui_native_switch` 在重新配置前失败于目标尚未生成：

```text
make: *** No rule to make target `test_picoui_native_switch'.  Stop.
```

重跑 `cmake -S . -B build` 后，`cmake --build build --target test_picoui_native_switch` 失败于 native switch render helper 缺失：

```text
Undefined symbols for architecture arm64:
  "_picoui_native_switch_get_rendered_checked"
```

### GREEN 证据

`ctest --test-dir build -R '^test_picoui_native_switch$' --output-on-failure` 已通过。

`ctest --test-dir build -R 'test_picoui_native_input_button|test_picoui_native_checkbox|test_picoui_native_switch' --output-on-failure` 已通过。

## P3-C slider

### 范围

- 新增 `tests/picoui/native/test_picoui_native_slider.c`
- 新增 `picoui/src/native/native_slider.c`
- 修改 `picoui/src/widgets/slider.c`
- 修改 `picoui/src/native/native_render.c`
- 修改 `picoui/src/native/native_event.c`
- 修改 `cmake/LingDongGUI.cmake`
- 修改 `tests/picoui/CMakeLists.txt`

### 合同

- `picoui_slider_set_range()` 和 `picoui_slider_set_value()` 可在 native 路径同步 slider value。
- native render traversal 可识别 slider，并记录 slider 最近一次渲染使用的 value。
- pointer down 命中 slider 时建立 pressed backend；拖动期间按 pointer x 相对 widget width 计算 permille，并触发 `SIGNAL_VALUE_CHANGED`。
- pointer 右边缘可到达 max value；只有 value 真实变化时调用 `on_value_changed`。
- native slider 合同不依赖 `ldSlider*` 作为完成证明；旧 backend 路径保留。

### RED 证据

首次 `cmake --build build --target test_picoui_native_slider` 在重新配置前失败于目标尚未生成：

```text
make: *** No rule to make target `test_picoui_native_slider'.  Stop.
```

重跑 `cmake -S . -B build` 后，`cmake --build build --target test_picoui_native_slider` 失败于 native slider render helper 缺失：

```text
Undefined symbols for architecture arm64:
  "_picoui_native_slider_get_rendered_value"
```

### GREEN 证据

`ctest --test-dir build -R '^test_picoui_native_slider$' --output-on-failure` 已通过。

`ctest --test-dir build -R 'test_picoui_native_input_button|test_picoui_native_checkbox|test_picoui_native_switch|test_picoui_native_slider' --output-on-failure` 已通过。

## P3-D text

### 范围

- 新增 `tests/picoui/native/test_picoui_native_text.c`
- 新增 `picoui/src/native/native_text.c`
- 修改 `picoui/include/picoui/text.h`
- 修改 `picoui/src/widgets/text.c`
- 修改 `picoui/src/native/native_render.c`
- 修改 `cmake/LingDongGUI.cmake`
- 修改 `tests/picoui/CMakeLists.txt`

### 合同

- `picoui_text_get_text()` 作为最小 public readback API，null 输入返回 `NULL`。
- `picoui_text_set_text()` 可写入 UTF-8 文本，并通过 `picoui_text_get_text()` public readback。
- `picoui_text_set_static_text()` 可写入静态 UTF-8 文本，并同步 native text state。
- `picoui_text_set_text()` / `picoui_text_set_static_text()` 在 backend/native 任一路径失败时返回 `-1`，且不留下已提交的 public text state。
- wrap width 由现有 `picoui_widget_set_size((struct picoui_widget *)text, width, height)` 表达，不新增 public API。
- native render traversal 可识别 text，并记录最近一次 render 使用的 text pointer/string 和 widget width。
- native text 合同不依赖 `ldText*` 作为完成证明；glyph 绘制仍可在 P5 text renderer hook 后续闭环。

### RED 证据

首次 `cmake --build build --target test_picoui_native_text` 在重新配置前失败于目标尚未生成：

```text
make: *** No rule to make target `test_picoui_native_text'.  Stop.
```

重跑 `cmake -S . -B build` 后，`cmake --build build --target test_picoui_native_text` 失败于 native text render helper 缺失：

```text
Undefined symbols for architecture arm64:
  "_picoui_native_text_get_rendered_text"
  "_picoui_native_text_get_rendered_wrap_width"
```

实现 helper 后，首次 `ctest --test-dir build -R '^test_picoui_native_text$' --output-on-failure` 暴露测试 setup 未绑定 native render root：

```text
Assertion failed: (picoui_native_text_get_rendered_text(text, &rendered_text) == 0)
```

### GREEN 证据

`cmake --build build --target test_picoui_native_text` 已通过。

`ctest --test-dir build -R '^test_picoui_native_text$' --output-on-failure` 已通过。

## P3-E image

### 范围

- 新增 `tests/picoui/native/test_picoui_native_image.c`
- 新增 `picoui/src/native/native_image.c`
- 修改 `picoui/src/widgets/image.c`
- 修改 `picoui/src/native/native_render.c`
- 修改 `cmake/LingDongGUI.cmake`
- 修改 `tests/picoui/CMakeLists.txt`

### 合同

- `picoui_image_create()` 创建出的 image 在 native 路径可作为 blank widget 存在；`source == 0` 合法。
- `picoui_image_set_source(image, 0)` 必须成功，并保持 blank source。
- `picoui_image_set_source(image, non_null_tile_source)` 必须接受 `img_tile != 0` 的 source，并写回 `image->source`。
- `picoui_image_set_source()` 在 native helper 失败时必须回退真实 backend source、public source 和后续 render readback。
- native render traversal 可识别 image，并记录最近一次 render 使用的 `struct picoui_image_source *`；测试通过 `image->source` 和 native helper 做 readback，不新增 public getter。
- image native 合同不依赖 `ldImage*` 作为完成证明；当前仍只闭环 source binding，不进入 P3-F list。

### RED 证据

首次 `cmake --build build --target test_picoui_native_image` 在重新配置前失败于目标尚未生成：

```text
make: *** No rule to make target `test_picoui_native_image'.  Stop.
```

重跑 `cmake -S . -B build` 后，`cmake --build build --target test_picoui_native_image` 失败于 native image render helper 缺失：

```text
Undefined symbols for architecture arm64:
  "_picoui_native_image_get_rendered_source"
```

### GREEN 证据

`cmake --build build --target test_picoui_native_image` 已通过。

`ctest --test-dir build -R '^test_picoui_native_image$' --output-on-failure` 已通过。

## P3-F list

### 范围

- 新增 `tests/picoui/native/test_picoui_native_list.c`
- 新增 `picoui/src/native/native_list.c`
- 修改 `picoui/src/widgets/list.c`
- 修改 `picoui/src/native/native_event.c`
- 修改 `picoui/src/native/native_render.c`
- 修改 `cmake/LingDongGUI.cmake`
- 修改 `tests/picoui/CMakeLists.txt`

### 合同

- `picoui_list_add_item()` 可在 native 路径保留 item storage，render readback 可按 selected index 取回 selected text。
- `picoui_list_set_selected_index()` 只通过 native helper 更新 list/backend 自有数据模型，再提交 public `selected_index`；成功性不依赖 `ldListSetSelectItem()`。
- `picoui_list_get_selected_index()` 在 P3-F native 路径直接返回 `list->selected_index`，不以 `ldListGetSelectItem()` 作为读回前提。
- native render traversal 可识别 list，并记录最近一次 render 使用的 selected index 和 selected text。
- pointer down 命中 list 时不触发 callback；pointer up 仍命中同一 list 时，按 `y / height * item_count` 的简单映射直接更新 native/backend selection state，并调用一次 callback。
- callback 必须收到真实 selected index 和原样 user data pointer。
- 当前 P3-F 只闭环 item storage / selection / click dispatch，不扩展 item height 推导、复杂 hit test、滚动、键盘导航。

### RED 证据

首次 `cmake --build build --target test_picoui_native_list` 在重新配置后失败于 native list render helper 缺失：

```text
Undefined symbols for architecture arm64:
  "_picoui_native_list_get_rendered_selected_index"
  "_picoui_native_list_get_rendered_selected_text"
```

### GREEN 证据

`cmake --build build --target test_picoui_native_list` 已通过。

`ctest --test-dir build -R '^test_picoui_native_list$' --output-on-failure` 已通过。

`python3 tests/picoui/runtime/check_picoui_runtime.py --demo list_basic` 已通过。

## 边界

- `P3-E image native migration` 已完成 source binding/rollback proof。`P3-F list native migration` 已完成 item storage/selection/click dispatch。`P3-G background native migration` 已完成 screen-root bind、background render readback 和 size/color/source proof。

## P3-G background native migration

### 范围

- 新增 `tests/picoui/native/test_picoui_native_background.c`
- 新增 `picoui/src/native/native_background.c`
- 修改 `picoui/include/picoui/background.h`
- 修改 `picoui/include/picoui/display.h`
- 修改 `picoui/include/picoui/screen.h`
- 修改 `picoui/src/widgets/background.c`
- 修改 `picoui/src/native/native_display.c`
- 修改 `picoui/src/native/native_screen.c`
- 修改 `picoui/src/native/native_render.c`
- 修改 `picoui/src/backend/ldgui/backend_window.c`
- 修改 `cmake/LingDongGUI.cmake`
- 修改 `tests/picoui/CMakeLists.txt`

### 合同

- `picoui_background_create(struct picoui_app *app, const char *id)` 保持现有 public app-path 合同。
- 新增 `picoui_background_create_root(struct picoui_screen *screen, const char *id)`，可把 background 作为 screen root widget。
- `picoui_screen_load(screen)` 在存在 root window 时，必须完成 root bind，并让后续 `picoui_timer_handler()` 走到 background native render。
- native render traversal 可识别 background root，并记录最近一次 render 使用的 width、height、color 和 `struct picoui_image_source *`。
- background native proof 以真实 screen/root/load/render 路径为准；测试不得手动补 `picoui_native_render_bind_root()` 来绕过 `picoui_screen_load()`。
- 当前 P3-G 只闭环 background as screen root，不进入 app-global background、复杂布局或 P3-H artifact gate。

### RED 证据

首次 reviewer 指出实现仍停留在 fake root 路径，且 public API/类型边界有破口：

```text
1. public API 用 _Generic 宏重写 create，contract check 会失败
2. background.c 私自重定义 struct picoui_display，头文件边界错误
3. create_root 只是造 compat app，没有把 root 绑定进 screen_load 路径
4. 测试手动 bind render root，绕过真实 screen load contract
```

### GREEN 证据

`python3 tests/picoui/contract/check_picoui_public_api.py` 已通过。

`cmake --build build --target test_picoui_native_background` 已通过。

`ctest --test-dir build -R '^test_picoui_native_background$' --output-on-failure` 已通过。

`ctest --test-dir build -R 'test_picoui_native_list|test_picoui_native_background' --output-on-failure` 已通过。

## 边界

- 当前任务推进到 `P3-H basic widgets native artifact`，尚未开始实现。
- 保留旧 backend 行为；P3-H 只处理 `basic_widgets` native artifact gate，不进入 `P4`。

- 范围：仅收 `background` 的 native root 迁移，目标是 screen root / normal root widget；不进入 P3-H visible/artifact。
- 合同：`picoui_background_create_root(screen, ...)` 创建真实 screen root；`picoui_screen_load(screen)` 后，normal flow 自动把该 root 作为 active/render root，render proof 里 root 尺寸覆盖 display size。
- RED：`tests/picoui/native/test_picoui_native_background.c` 改为显式 `create_root + screen_load`，不再手工 bind root；失败点应直接暴露 screen 未持有/未加载 root。
- GREEN：`screen` 持有 root background，`screen_load()` 自动完成 root 绑定；native render 接受 `BACKGROUND` root，并提供尺寸/颜色/source render proof；`ctest --test-dir build -R '^test_picoui_native_background$' --output-on-failure` 通过。
- 边界：当前仍是 P3-G。这里只闭 `screen root + cover display size + color/image proof`。不拉 visible gate，不拉 runtime artifact，不进入 P3-H，除非主线程后续单独收口推进。

## P3-H basic widgets native artifact

### 范围

- 对齐 P3-H visible gate 入口名称，沿用现有 `tests/picoui/runtime/check_picoui_visible_ui.py --demo basic_widgets`
- 运行 `basic_widgets` runtime / visible fresh gate
- 只有两者都通过时，才允许更新 `tests/picoui/contract/picoui_native_migration_ledger.json` 的 `P3-basic-widgets`

### 口径决策

- 本阶段不新增 `check_picoui_native_visible_ui.py` 包装或别名脚本。
- 理由：仓库当前正式 automatic visible gate 已固定为 `check_picoui_visible_ui.py`，`picoui/docs/demo_guide.md` 也以该脚本为真相源；再加一个 `native` 别名不会增加证明强度，只会制造双入口和后续漂移点。

### RED 证据

首次 P3-H 收口时，`python3 tests/picoui/runtime/check_picoui_runtime.py --demo basic_widgets` 仍打印既有降级口径：

```text
KNOWN_LIMITATION=picoui_basic_widgets_demo native P1 runtime loop exits cleanly, but capture/native render artifact is not ready yet
```

首次 P3-H 收口时，`python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo basic_widgets` fresh 失败：

```text
AssertionError: SMOKE FAIL: demo 'picoui_basic_widgets_demo' did not produce a capture frame.
stdout:
PICOUI_RUNTIME_READY
```

### GREEN 目标

- `check_picoui_runtime.py --demo basic_widgets` 通过
- `check_picoui_visible_ui.py --demo basic_widgets` 通过
- `P3-basic-widgets` ledger 才能改为 `covered`

### GREEN 证据

`ctest --test-dir build -R '^test_picoui_native_runtime$' --output-on-failure` 已通过。

`python3 tests/picoui/runtime/check_picoui_runtime.py --demo basic_widgets` 已退出 `0`，且不再输出 `KNOWN_LIMITATION=picoui_basic_widgets_demo native P1 runtime loop exits cleanly, but capture/native render artifact is not ready yet`。

`python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo basic_widgets` 已 fresh 退出 `0`；但在新的独立 review 通过前，仍不能仅凭 gate 绿灯把 `P3-H` 判为 closed。

### 当前结论

- `P3-H basic widgets native artifact` 已闭环。
- 当前 artifact 路径已收口到：`picoui_timer_handler()` 通过 `picoui_backend_native_render_capture()` 走 `ld_scene -> ldGuiDraw -> write_capture` 真路径。
- `tests/picoui/native/test_picoui_native_runtime.c` 已改为直接拉起真实 `picoui_basic_widgets_demo` 子进程，并对 demo 自己的 `stdout/capture` 做断言，不再手工重建 fixture。
- `tests/picoui/contract/picoui_native_migration_ledger.json` 的 `P3-basic-widgets` 现可更新为 `covered`。

### P3-H Closeout

- fresh verification：
  - `ctest --test-dir build -R '^test_picoui_native_runtime$' --output-on-failure`
  - `python3 tests/picoui/runtime/check_picoui_runtime.py --demo basic_widgets`
  - `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo basic_widgets`
  - `git diff --check`
- 独立 review：
  - `Archimedes` 第二轮结论：`APPROVED`
