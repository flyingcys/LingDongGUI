# P4 扩展控件迁移

## P4-A arc

- 时间：2026-06-05
- 范围：仅收口 `arc` 的 focused native test、public contract、runtime/visible gate 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_arc.c`，覆盖：
  - create arc
  - `set_range`
  - `set_value`
  - `set_background_angle`
  - `set_end_angle`
  - value readback
  - native render smoke
- 首轮 RED 先暴露了两层真实缺口：
  - `build/` 尚未生成 `test_picoui_native_arc`
  - 当前 `arc` native path 还没有对应的 test/impl，且现有 `struct picoui_arc` 没有 `min/max/value` 存储

### GREEN

- 新增 `picoui/src/native/native_arc.c`
  - 为 arc 建立 native render state
  - 提供 `picoui_native_arc_set_value()`
  - 提供 `picoui_native_arc_render()`
  - 提供 render readback helper
- 更新 `picoui/src/widgets/arc.c`
  - `picoui_arc_create()` 继续走真实 `picoui_backend_create_arc()` / `ldArc*`
  - `background/foreground/rotation/color/quarter_source/parent_color` 通过真实 backend arc 路径读写
  - `range/value/start/end angle` 与 value readback 收口到 arc public contract
- 更新 `picoui/include/picoui/arc.h`
  - 同步公开 `range/value/start/end angle` 的 setter/getter 声明
- 更新 build 注册：
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`
- 更新 gate：
  - `tests/picoui/runtime/check_picoui_runtime.py`
  - `tests/picoui/runtime/check_picoui_visible_ui.py`
  - `tests/picoui/native/test_picoui_native_arc.c`

### 验证

- `cmake --build build --target test_picoui_native_arc`
- `ctest --test-dir build -R '^test_picoui_native_arc$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo arc_basic`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo arc_basic`

### 当前结论

- `P4-A arc` 已闭环。
- 真实实现路径是：`picoui_arc_create()` 绑定真实 `backend_arc`，capture 仍通过现有 `picoui_app_run -> picoui_backend_app_run -> ldGuiDraw -> write_capture` 路径产出真实 artifact。
- `arc_basic` fresh stdout 已稳定为 `PICOUI_BACKEND_REAL_WIDGET_IDS=title,arc`，`arc` 不再落入 `PICOUI_BACKEND_FALLBACK_WIDGET_IDS`。
- focused native test、runtime gate、visible gate 均已通过。
- 本轮只关闭 `P4-A`，不提前改 `P4-extended-widgets` ledger 总项；下一任务推进到 `P4-B calendar`。

### Gate 强化

- `tests/picoui/native/test_picoui_native_arc.c`
  - 不再手工前置声明 arc public API
  - 直接通过 public header 编译
  - 明确断言 backend `ld_widget` 的真实类型是 `widgetTypeArc`
  - 继续覆盖 timer-driven native render readback
- `tests/picoui/runtime/check_picoui_runtime.py`
  - `arc_basic` 新增 marker 断言
  - 要求 `arc` 必须在 `PICOUI_BACKEND_REAL_WIDGET_IDS`
  - 要求 `arc` 不能回退到 `PICOUI_BACKEND_FALLBACK_WIDGET_IDS`
- `tests/picoui/runtime/check_picoui_visible_ui.py`
  - `arc_basic` 新增中心空洞约束
  - 新增外环占据率约束
  - 新增四象限覆盖约束
  - 不再只凭面积和两色判绿

### P4-A Closeout

- fresh verification：
  - `cmake --build build --target test_picoui_native_arc`
  - `ctest --test-dir build -R '^test_picoui_native_arc$' --output-on-failure`
  - `python3 tests/picoui/runtime/check_picoui_runtime.py --demo arc_basic`
  - `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo arc_basic`
  - `git diff --check`
- 独立 review：
  - 第一轮 `Turing`：`NOT APPROVED`
  - 第二轮 `Raman`：`APPROVED`

## P4-B calendar

- 时间：2026-06-05
- 范围：仅收口 `calendar` 的 focused native test、selected date public contract、native month model/day hit-test 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_calendar.c`，覆盖：
  - create calendar
  - `set_date` / `get_date`
  - `set_selected_date` / `get_selected_date`
  - selected callback cookie
  - grid/model readback
  - native render readback
- 首轮 RED 先暴露了两层真实缺口：
  - `cmake --build build --target test_picoui_native_calendar` 报目标不存在
  - `cmake -S . -B build` 报缺少 `picoui/src/native/native_calendar.c`

### GREEN

- 新增 `picoui/src/native/native_calendar.c`
  - 为 calendar 建立 native render state
  - 提供 rendered date / selected date readback helper
- 重写 `picoui/src/widgets/calendar.c`
  - 保留真实 `picoui_backend_calendar_*` / `ldCalendar*` date/grid/render 路径
  - native month model + 6x7 grid cache 继续作为 PicoUI 侧 readback/model 合同
  - `selected date` public contract 改为复用真实 backend date truth，不再走测试私有 side state
  - `set_date` / `set_selected_date` 现在维护同一 `year/month/day` 不变量
- 更新 `picoui/src/backend/ldgui/backend_calendar.c`
  - 新增 `selected date` backend helper，当前与真实 `ldCalendar` date truth 对齐
  - 新增 calendar cell geometry / point-to-date / day-center helper
  - native event 与 focused test 都改为复用同一份 backend 几何
- 更新 `picoui/include/picoui/calendar.h`
  - 新增 `picoui_calendar_selected_callback_t`
  - 新增 `set_selected_date`
  - 新增 `get_selected_date`
  - 新增 `set_on_selected`
- 更新 native runtime 路径：
  - `picoui/src/native/native_render.c`
  - `picoui/src/native/native_event.c`
  - calendar 已接入 native render visit
  - pointer release 已接入 day hit-test + selected callback dispatch
- 更新 build 注册：
  - `tests/picoui/CMakeLists.txt`

### 验证

- `cmake -S . -B build`
- `cmake --build build --target test_picoui_native_calendar`
- `ctest --test-dir build -R '^test_picoui_native_calendar$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo calendar_basic`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo calendar_basic`
- `git diff --check`

### 当前结论

- `P4-B calendar` 已完成 closeout。
- 当前证据已经证明：
  - public `selected date` contract 已存在
  - `selected date` 已落到真实 `ldCalendar` date truth
  - native month model / 6x7 grid cache 已建立
  - native pointer hit-test 已切到 backend cell geometry helper 路径
  - timer-driven native render readback 已成立
- 独立 review 已 `APPROVED`。
- 当前阶段下一任务推进到 `P4-C clock`。

## P4-C clock

- 时间：2026-06-06
- 范围：仅收口 `clock` 的 focused native test、手动时间 public contract、native render snapshot 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_clock.c`，覆盖：
  - create clock
  - `set_use_system_time(0)`
  - `set_time(23, 59, 59)`
  - `get_time`
  - `tick` 一秒
  - `23:59:59 -> 00:00:00` 回卷
  - native render readback
- 首轮 RED 失败证据：
  - `cmake --build build --target test_picoui_native_clock`
  - 链接失败，缺少：
    - `picoui_clock_set_time`
    - `picoui_clock_get_time`
    - `picoui_clock_tick`
    - `picoui_native_clock_get_rendered_time`

### GREEN

- 新增 `picoui/src/native/native_clock.c`
  - 为 `clock` 建立 native render snapshot state
  - 提供 `picoui_native_clock_render()`
  - 提供 `picoui_native_clock_get_rendered_time()`
- 更新 `picoui/src/widgets/clock.c`
  - PicoUI 侧新增 `hour/minute/second` 真值
  - 新增 `picoui_clock_set_time/get_time/tick`
  - `tick` 负责 `24h` 回卷
  - 不把 `ldClock*` 当手动时间真值
- 更新 `picoui/src/native/native_render.c`
  - `PICOUI_BACKEND_WIDGET_CLOCK` 已接入 `picoui_native_clock_render()`
  - `test_picoui_native_clock` 现在经由 `picoui_timer_handler()` 主路径拿到 render readback
- 更新最小合同：
  - `picoui/include/picoui/clock.h`
  - `picoui/src/core/internal.h`
- 更新测试注册：
  - `tests/picoui/CMakeLists.txt`

### 主路径补证

- 第二轮 RED：
  - `ctest --test-dir build -R '^test_picoui_native_clock$' --output-on-failure`
  - 失败点：
    - `PICOUI_RUNTIME_READY`
    - `PICOUI_BACKEND_REAL_WIDGET_IDS=clock`
    - `Assertion failed: (picoui_native_clock_get_rendered_time(clock, &hour, &minute, &second) == 0)`
  - 这次失败证明：`clock` 虽然已经有手动时间 state，但当时还没接进 `picoui_timer_handler()` 的 native render 主路径
- 第二轮 GREEN：
  - `clock` 接入 `picoui/src/native/native_render.c` 后
  - `ctest --test-dir build -R '^test_picoui_native_clock$' --output-on-failure` fresh PASS
  - `python3 tests/picoui/runtime/check_picoui_runtime.py --demo clock_basic --build-dir build` exit 0

### 当前结论

- 当前 `P4-C clock` 可按本任务范围 closeout。
- 当前 fresh 证据已经证明：
  - PicoUI manual time truth 已存在，`clock` 自持 `hour/minute/second`
  - `picoui_clock_set_time()` / `picoui_clock_tick()` 已桥接到真实 `ldClock*` 指针角度
  - `23:59:59 -> 00:00:00` 不仅更新 PicoUI state，也更新真实指针角度与 timer-driven readback
  - native snapshot/readback 已接入 `picoui_timer_handler()` 主路径
  - `clock_basic` runtime / visible gate 已通过
- 当前 artifact 仍经现有 `picoui/src/backend/ldgui/backend_clock.c` / `src/gui/ldClock*` 绘制输出；这是当前阶段允许的 mixed boundary，说明 `P4-C` 已闭环，但不等于 `P7` 删除 ldgui runtime 依赖已完成。

## P4-F gauge

- 时间：2026-06-06
- 范围：仅收口 `gauge` 的 focused native test、最小 public contract、native needle/scale state 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_gauge.c`，目标先按 plan 直接写 public contract：
  - `set_range`
  - `set_value`
  - `set_tick_count`
  - `clamped value`
  - `needle angle`
  - native render readback
- 干净 RED 失败证据：
  - `rtk cmake --build build --target test_picoui_native_gauge`
  - 编译失败，`picoui/include/picoui/gauge.h` 不存在以下 public API：
    - `picoui_gauge_set_range`
    - `picoui_gauge_set_value`
    - `picoui_gauge_set_tick_count`
    - `picoui_gauge_get_min_value`
    - `picoui_gauge_get_max_value`
    - `picoui_gauge_get_value`
    - `picoui_gauge_get_tick_count`
- 该 RED 证明：若不最小补 public contract，focused test 只能退回手写 internal/native helper，不能作为 `P4-F` 完成证据。

### GREEN

- 最小补 `picoui/include/picoui/gauge.h`
  - 新增 `range/value/tick count` setter/getter public contract
- 更新 `picoui/src/widgets/gauge.c`
  - 去掉 `gauge.c` 里的进程级 32 槽静态表
  - 改成 `gauge` 自身扩展对象持有 `min/max/value/tick_count/tick_step`
  - `set_range/set_value/set_tick_count` 补事务式更新与 rollback
  - `value -> angle` 映射在 PicoUI 内完成，不把 `ldGauge*` 当 value truth
- 新增 `picoui/src/native/native_gauge.c`
  - render snapshot 改成挂在本 `gauge` 扩展对象上的证据层
  - 不再使用进程级静态 render-state 表
  - render snapshot 不承担 public truth
- 更新 `picoui/src/native/native_render.c`
  - `PICOUI_BACKEND_WIDGET_GAUGE` 接入 native render visit
- 更新 build 注册：
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`

### 验证

- `rtk cmake -S . -B build`
- `rtk cmake --build build --target test_picoui_native_gauge`
- `rtk ./build/tests/picoui/test_picoui_native_gauge`
- `rtk ctest --test-dir build -R '^test_picoui_native_gauge$' --output-on-failure`
- `rtk python3 tests/picoui/runtime/check_picoui_runtime.py --demo gauge_basic --build-dir build`
- `rtk python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo gauge_basic --build-dir build`

### 当前结论

- `P4-F gauge` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - public/native truth 不再依赖 `gauge.c` 或 `native_gauge.c` 的进程级静态表
  - `set_range/set_value` fail-path 已补 rollback，失败后 getter truth 不应被污染
  - focused test 已从弱断言提升到：
    - clamp 后 `needle_angle == 180.0f`
    - render snapshot 中 `tick_step` 随 `range/tick_count` 变化，从 `8` 变为 `6`
- 当前 public contract 已最小补足 plan 必需能力：`range/value/tick count`。
- 当前 truth 分层是：
  - `min/max/value/tick_count/tick_step` 由 `gauge` 自身扩展对象持有
  - render snapshot 只是 timer-driven 证据层
  - artifact 仍经现有 `gauge_basic -> app_run -> backend_ldgui_runtime` 混合路径输出
- 当前 mixed boundary 只说明 `P4-F` 已在本任务范围内闭环；不外推到 `P7` ldgui runtime 依赖删除。
- 独立 review：
  - 第一轮 `Avicenna`：`NOT APPROVED`
  - 第二轮 `Avicenna`：`APPROVED`
- 当前阶段下一任务推进到 `P4-G graph`。

## P4-D combo_box

- 时间：2026-06-06
- 范围：仅收口 `combo_box` 的 focused native test、popup/list state、selected text/callback 合同与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_combo_box.c`，覆盖：
  - create combo box
  - add options
  - `set_selected_index(0)`
  - open dropdown
  - select option `2`
  - assert selected text
  - assert callback cookie
  - assert open/close render readback
- 首轮 RED 失败证据：
  - `cmake --build build --target test_picoui_native_combo_box`
  - `make: *** No rule to make target 'test_picoui_native_combo_box'. Stop.`
- 第二轮 RED 失败证据：
  - `cmake -S . -B build`
  - 报缺少 `picoui/src/native/native_combo_box.c`

### GREEN

- 新增 `picoui/src/native/native_combo_box.c`
  - 为 combo box 建立 focused native render state
  - 提供 `open/select` helper
  - 提供 selected index / text / open readback helper
- 更新 `picoui/src/widgets/combo_box.c`
  - `selected_index` 真值收口到 PicoUI 自身 `struct picoui_combo_box`
  - `picoui_combo_box_get_selected_index()` 不再从 backend truth 回读
  - `picoui_combo_box_get_text()` 不再依赖 `ldComboBoxGetText()`
  - `picoui_combo_box_is_open()` 复用 PicoUI/backend widget 上的 open state
  - `picoui_combo_box_add_item()` 在 `ld_widget == NULL` 时仍能推进 PicoUI item model，不再把 `ldComboBoxSetStaticItems()` 当必需状态推进路径
  - `picoui_combo_box_set_selected_index()` / native select helper 不再调用 `picoui_backend_combo_box_set_selected_index()`
  - popup open/close 与 selected callback 在 PicoUI 侧收口，不把 `ldComboBox*` 当 open/select 真值
- 更新 `picoui/src/backend/ldgui/backend_combo_box.c`
  - 新增 host sync helper，把 PicoUI/native 已确定的 `open/select` 结果同步到真实 `ldComboBox`
  - 新增 `point_to_slot/get_item_center` helper，让 native event 与 focused test 复用真实 host 几何
  - 本轮精确根因是：`picoui_timer_handler()` 的 capture 路径不会像 `picoui_app_run()` 一样先把 port pointer bridge 到 `ldCfgTouchSetPoint()`，所以 `header press` 虽能推进 PicoUI/native truth，但真实 `ldComboBox` 默认收不到同一份 press/release 事件；必须在 truth 推进后显式做 host sync
- 更新 `picoui/src/native/native_event.c`
  - `combo_box` 进入 native input backend 集合与 pointer/release 主路径
  - `header press` 走 native open dispatch，`release` 走 native select/callback dispatch
  - 本轮根因是 `combo_box` open 分支被错误包在 `checkbox/switch/list/calendar` 条件内，导致 `header press -> open` 根本不执行，`picoui_timer_handler()` 在 focused test 第一次 header press 时返回负值
  - 修复后将 `PICOUI_BACKEND_WIDGET_COMBO_BOX` 纳入该条件分支，`header press -> open` 与后续 `release -> select option 2` 都能走通真实 native pointer 主路径
  - 后续又把 slot 命中从理想化 `widget height` 计算切到 backend `point_to_slot()`，避免 expanded host geometry 与 native 点击点分叉
- 更新 `picoui/src/native/native_render.c`
  - `combo_box` 已接入 native render visit
  - focused render readback 不再依赖测试手工调用 render helper，而是通过 `picoui_timer_handler()` 驱动
- 更新 `tests/picoui/native/test_picoui_native_combo_box.c`
  - focused test 改为走 `picoui_input_push_pointer(...) + picoui_timer_handler()`
  - 覆盖 `header press -> open -> release -> select option 2 -> callback -> render readback`
  - 显式断言 `ld_combo_box->isExpand` 与 `ldComboBoxGetSelectItem(ld_combo_box)` 会跟随 PicoUI/native 真值同步
  - 证明点从“故意分叉”改为“truth 在 PicoUI/native，real host state 跟随同一结果”
- 更新 build 注册：
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`

### 验证

- `cmake -S . -B build`
- `cmake --build build --target test_picoui_native_combo_box`
- `ctest --test-dir build -R '^test_picoui_native_combo_box$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo combo_box_basic`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo combo_box_basic`

### 当前结论

- 当前 `P4-D combo_box` 已完成 closeout。
- fresh focused native test / runtime gate / visible gate 已绿，第三轮独立 review 已 `APPROVED`。
- 本轮完成后的有效结论：
  - PicoUI-side `item/open/select/callback` 真值已经从 public truth 层收口到 `widgets/native`
  - `combo_box` 已接入 `native_render` visit 和 `native_event` pointer/release 主路径
  - focused test 已证明 `header press -> open -> select option 2 -> callback -> render readback` 通过真实 native 主路径完成
  - 同时继续保持 popup/list state 推进不依赖 `ldComboBox*`，而真实 `ldComboBox` host state 仅作为同步结果跟随
  - focused test 的 item 点击点已经切到真实 host geometry helper，不再依赖理想化 `height * slot` 算法
- 本轮 closeout 仍只覆盖 `P4-D` 的控件级 native 迁移，不代表已经进入 `P7` 的 backend 默认依赖移除阶段。

## P4-E date_time

- 时间：2026-06-06
- 范围：仅收口 `date_time` 的 focused native test、`format/date/time` public truth、formatted output helper 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_date_time.c`，覆盖：
  - create date time
  - `set_use_system_time(0)`
  - `set_format`
  - `set_date`
  - `set_time`
  - `get_format`
  - formatted output readback
- 首轮 RED 失败证据：
  - `cmake --build build --target test_picoui_native_date_time`
  - 链接失败，缺少 `_picoui_native_date_time_format_text`

### GREEN

- 新增 `picoui/src/native/native_date_time.c`
  - 为 `date_time` 建立最小 native state
  - 持有 PicoUI-side `format` 真值
  - 提供 `picoui_native_date_time_format_text()`
- 更新 `picoui/src/widgets/date_time.c`
  - `picoui_date_time_set_format()` 先调用 backend，backend 成功后再提交 PicoUI/native public truth
  - `picoui_date_time_get_format()` 不再从 backend 取真值，改为返回 PicoUI/native format state
  - `date/time` 真值继续由 `struct picoui_date_time` 自持
- 更新 build 注册：
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`

### 验证

- `cmake --build build --target test_picoui_native_date_time`
- `ctest --test-dir build -R '^test_picoui_native_date_time$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo date_time_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo date_time_basic --build-dir build`

### 当前结论

- 当前 `P4-E date_time` 已按最小任务边界闭环。
- 当前 fresh 证据已经证明：
  - `format` 真值已收回 `widget/native` 自身存储，不再依赖 backend readback
  - `set_format/set_date/set_time/get_format` 可在 PicoUI-side 形成一致状态
  - backend fail-path 不会污染 `picoui_date_time_get_format()` 与 `picoui_native_date_time_format_text()` 的旧值

## P4-G graph

- 时间：2026-06-06
- 范围：仅收口 `graph` 的 focused native test、series/value PicoUI-side truth、native render snapshot 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_graph.c`，覆盖：
  - create graph
  - add series
  - set 3 points
  - readback 3 points
  - native render readback 等价证据
- 首轮 RED 失败证据：
  - `rtk cmake --build build --target test_picoui_native_graph`
  - `make: *** No rule to make target 'test_picoui_native_graph'. Stop.`
- 重新 `rtk cmake -S . -B build` 后，第二轮 RED 失败证据：
  - `rtk cmake --build build --target test_picoui_native_graph`
  - 链接失败，缺少：
    - `picoui_native_graph_render`
    - `picoui_native_graph_get_rendered_series_count`
    - `picoui_native_graph_get_rendered_value`

### GREEN

- 新增 `picoui/src/native/native_graph.c`
  - 为 `graph` 建立最小 native render snapshot
  - 提供 rendered `series_count` / `value` readback helper
- 更新 `picoui/src/widgets/graph.c`
  - `series_count` 与各 `series/value` 真值收口到 PicoUI 自身，不再从 `ldGraph*` 回读
  - `add_series/set_value/move_add/get_value/get_series_count` 改为先维护 PicoUI-side truth，再同步真实 backend graph
  - fail-path 补最小 rollback，避免 backend 失败污染本地 truth
- 更新 build 注册：
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`

### 验证

- `rtk cmake -S . -B build`
- `rtk cmake --build build --target test_picoui_native_graph`
- `rtk ctest --test-dir build -R '^test_picoui_native_graph$' --output-on-failure`
- `rtk cmake --build build --target picoui_graph_basic_demo`
- `rtk python3 tests/picoui/runtime/check_picoui_runtime.py --demo graph_basic --build-dir build`

### 当前结论

- `P4-G graph` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `create graph -> add series -> set 3 points -> readback 3 points` 已通过 focused test
  - `series_count/value` public truth 已不再依赖 `ldGraph*` backend readback
  - focused test 现在通过 `picoui_native_event_bind_root() + picoui_timer_handler()` 走统一 timer-driven render 主路径，并经 `PICOUI_BACKEND_WIDGET_GRAPH` 分支拿到 native render readback
  - `graph_basic` demo 目标与 runtime smoke / visible gate 已可构建并通过当前 gate
  - 当前 `graph` artifact 输出与样式承载仍经现有 `picoui/src/backend/ldgui/backend_graph.c` / `src/gui/ldGraph*` 路径完成；这是当前阶段允许的 mixed boundary，说明 `P4-G` 已在本任务范围内闭环，但不等于 `P7` 删除 ldgui runtime 依赖已完成。
- 独立 review：
  - 第一轮 `Copernicus`：`NOT APPROVED`
  - 第二轮 `Copernicus`：`APPROVED`
- 当前阶段下一任务推进到 `P4-H icon_slider`。

## P4-H icon_slider

- 时间：2026-06-06
- 范围：仅收口 `icon_slider` 的 focused native test、selected item PicoUI-side truth、callback/readback 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_icon_slider.c`，覆盖：
  - create icon_slider
  - add items
  - `set_selected_index`
  - callback/readback
  - native render readback 等价证据
- 首轮 RED 失败证据：
  - `cmake --build build --target test_picoui_native_icon_slider`
  - `make: *** No rule to make target 'test_picoui_native_icon_slider'.  Stop.`
- 重新 `cmake -S . -B build` 后，第二轮 RED 失败证据：
  - CMake 报缺少 `picoui/src/native/native_icon_slider.c`
  - 并导致 `picoui_core` 生成失败

### GREEN

- 新增 `picoui/src/native/native_icon_slider.c`
  - 为 `icon_slider` 建立最小 native render snapshot state
  - 提供 `picoui_native_icon_slider_set_selected_index()`
  - 提供 `picoui_native_icon_slider_select_index()`
  - 提供 rendered selected `index/text` readback helper
- 更新 `picoui/src/widgets/icon_slider.c`
  - `selected_index` public/native truth 改为收口到 PicoUI 自身，不再从 `ldIconSlider*` 回读
  - `set_selected_index()` 改走 native helper，同步 PicoUI state 与 backend mirror
- 更新 build 注册：
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`

### 验证

- `cmake -S . -B build`
- `cmake --build build --target test_picoui_native_icon_slider`
- `ctest --test-dir build -R '^test_picoui_native_icon_slider$' --output-on-failure`
- `cmake --build build --target picoui_icon_slider_basic_demo`
- `./build/examples/sdl/picoui_icon_slider_basic_demo`

### 当前结论

- 当前 `P4-H icon_slider` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `create -> add items -> set selected index -> callback/readback` 已通过 focused test
  - `selected_index` public/native truth 已不再依赖 `ldIconSlider*` backend readback
  - focused test 现在通过 `picoui_native_event_bind_root() + picoui_timer_handler()` 走 timer-driven native render 主路径
  - render snapshot 不再依赖进程级静态 render-state 表
  - 合法未选中初始态不会让 `picoui_timer_handler()` 失败
  - 未选中初始态的 render snapshot 语义已锁定为：
    - `get_rendered_selected_index()` 成功并回写 `-1`
    - `get_rendered_selected_text()` 成功并回写 `NULL`
- 当前 artifact 输出与运行时承载仍经现有 `picoui/src/backend/ldgui/backend_icon_slider.c` / `src/gui/ldIconSlider*` 路径完成；这表示 `P4-H` 在本任务边界内完成 closeout，不等于已经完成更大范围的 `P7` 级收敛。
- 独立 review 结论：
  - 第一轮 `Boyle`：`NOT APPROVED`
  - 第二轮 `Boyle`：`APPROVED`
- 当前阶段下一任务推进到 `P4-I keyboard + line_edit`。

## P4-I keyboard + line_edit

- 时间：2026-06-06
- 范围：仅收口 `keyboard + line_edit` 的 focused native test、text/editing public truth、提交/取消输入路径与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_keyboard_line_edit.c`，覆盖：
  - focus line edit
  - keyboard 输入 `A`
  - backspace
  - enter 提交
  - text buffer / submit callback
  - cancel path
  - destroy / recreate
- 首轮真实 RED 失败证据：
  - `cmake -S . -B build`
  - `cmake --build build --target test_picoui_native_keyboard_line_edit`
  - `ctest --test-dir build -R '^test_picoui_native_keyboard_line_edit$' --output-on-failure`
  - 失败点是 `editing == 1` 断言不成立。
- 该 RED 证明：
  - `line_edit` 的 `editing` truth 当时仍主要挂在 backend / `ldLineEdit*`
  - `keyboard -> line_edit` 的输入主路径还没有收口到 PicoUI/native 自身

### GREEN

- 新增 `picoui/src/native/native_keyboard.c`
  - 建立 keyboard 输入到 active editing owner 的 native 输入路径
  - 收口 `selected_key_code` truth
  - 处理 `ascii`、backspace、enter/commit、cancel/no-owner 路径
- 新增 `picoui/src/native/native_line_edit.c`
  - 去掉进程级静态槽表
  - 改为直接使用 `line_edit` 自身存储维护 `text/editing` native truth
  - 提供 `init/set_text/get_text/set_editing/get_editing/append_ascii/backspace`
- 更新 `picoui/src/core/internal.h`
  - `struct picoui_keyboard` 新增 `selected_key_code`
  - `struct picoui_line_edit` 新增 `text_buffer[256]`
- 更新 `picoui/src/widgets/keyboard.c`
  - public truth 不再回读 `ldKeyboard_t->keyCode`
  - 输入与退出路径改走 native helper
- 更新 `picoui/src/widgets/line_edit.c`
  - `accepts_text_input` 标记在 create 时建立
  - `get_text()` / `get_editing()` 改读 native truth
  - `set_text()` 先更新 PicoUI/native truth，再做 backend mirror
- 更新 build 注册：
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`

### 验证

- `cmake -S . -B build`
- `cmake --build build --target test_picoui_native_keyboard_line_edit`
- `ctest --test-dir build -R '^test_picoui_native_keyboard_line_edit$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo keyboard_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo line_edit_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo keyboard_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo line_edit_basic --build-dir build`
- `git diff --check`

### 当前结论

- 当前 `P4-I keyboard + line_edit` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `focus line_edit -> keyboard input/backspace/enter` 已通过 focused test
  - `line_edit` 的 `text/editing` public truth 已收口到 PicoUI/native 自身，不再依赖 `ldLineEdit*` 回读
  - `keyboard` 的 `selected_key_code` public truth 已收口到 PicoUI/native 自身，不再依赖 `ldKeyboard*` 回读
  - focused test 已覆盖 commit、cancel、无 editing owner、plain text 误目标、防 destroy/recreate 污染与 re-init 路径
  - `keyboard_basic` 与 `line_edit_basic` 的 runtime / visible gate 已通过
- 当前 artifact 输出与宿主承载仍经现有 `picoui/src/backend/ldgui/backend_keyboard.c`、`picoui/src/backend/ldgui/backend_line_edit.c` 与 `src/gui/ldKeyboard*` / `src/gui/ldLineEdit*` 路径完成；这表示 `P4-I` 在本任务边界内完成 closeout，不等于已经完成 `P7` 级别的 ldgui runtime 依赖删除。
- 独立 review 结论：
  - 历轮早期 review 在旧快照上曾给出 `NOT APPROVED`
  - 当前第三轮 `Harvey`：`APPROVED`
- 当前阶段下一任务推进到 `P4-J message_box`。

## P4-J message_box

- 时间：2026-06-06
- 范围：仅收口 `message_box` 的 focused native test、modal open/close truth、button hit-test/readback 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_message_box.c`，覆盖：
  - create modal message box
  - `OK/Cancel`
  - click `OK`
  - callback index / id
  - modal close readback
- 首轮真实 RED 失败证据：
  - `cmake --build build --target test_picoui_native_message_box`
  - 失败点是缺少：
    - `picoui_native_message_box_get_button_center`
    - `picoui_native_message_box_get_rendered_button_count`
    - `picoui_native_message_box_get_rendered_open`
- 该 RED 证明：
  - 当前仓库当时还没有 `native_message_box.c`
  - `message_box` 的 focused native readback 与 pointer 主路径都还没建立

### GREEN

- 新增 `picoui/src/native/native_message_box.c`
  - 建立 `message_box` 最小 native truth：
    - `is_open`
    - `pressed_button_index`
  - 提供：
    - `picoui_native_message_box_render()`
    - `picoui_native_message_box_get_rendered_open()`
    - `picoui_native_message_box_get_rendered_button_count()`
    - `picoui_native_message_box_get_button_center()`
    - `picoui_native_message_box_handle_press()`
    - `picoui_native_message_box_handle_release()`
  - button 几何/命中改为 PicoUI 自己按 dialog 尺寸与 button 数量计算，不再依赖 `ldMessageBox_t` / `btnRegion`
- 更新 `picoui/src/widgets/message_box.c`
  - create 时初始化 `is_open=1`
  - backend `open` mirror 与 PicoUI/native modal truth 对齐
- 更新 `picoui/src/core/internal.h`
  - `struct picoui_message_box` 新增：
    - `is_open`
    - `pressed_button_index`
- 更新 `picoui/src/native/native_event.c`
  - 将 `PICOUI_BACKEND_WIDGET_MESSAGE_BOX` 接入 native input backend 集
  - pointer `press/release` 改走 `message_box` native dispatch
- 更新 `picoui/src/native/native_render.c`
  - 将 `PICOUI_BACKEND_WIDGET_MESSAGE_BOX` 接入 timer-driven native render visit
- 更新 build 注册：
  - `cmake/LingDongGUI.cmake`
  - `tests/picoui/CMakeLists.txt`

### 验证

- `cmake --build build --target test_picoui_native_message_box`
- `ctest --test-dir build -R '^test_picoui_native_message_box$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo message_box_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo message_box_basic --build-dir build`
- `git diff --check`

### 当前结论

- 当前 `P4-J message_box` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `create modal -> OK/Cancel -> click OK -> callback index/id -> modal close` 已通过 focused native test
  - `message_box` 的 `is_open` / `pressed_button_index` truth 已收口到 PicoUI/native 自身
  - button 几何/命中已不再依赖 `ldMessageBox*` runtime 结构读回
  - focused test 现在还覆盖了 modal overlay 行为：
    - message box 打开时，underlay 点击会被 native input 主路径吞掉
    - click `OK` 关闭后，underlay 点击恢复
  - `message_box_basic` runtime / visible gate 已通过
- 当前 artifact 输出与宿主承载仍经现有 `picoui/src/backend/ldgui/backend_message_box.c` / `src/gui/ldMessageBox*` 路径完成；这表示 `P4-J` 的 focused native contract 已建立，但不等于更大范围的 `P7` 删除 ldgui runtime 依赖已经完成。
- 当前阶段下一任务推进到 `P4-K progress_bar and progress_wheel`。

## P4-K progress_bar and progress_wheel

- 时间：2026-06-06
- 范围：仅收口 `progress_bar/progress_wheel` 的 focused native test、`percent` clamp 合同、render smoke readback 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_progress.c`，覆盖：
  - create `progress_bar` / `progress_wheel`
  - set percent
  - assert clamp `0..100`
  - assert timer-driven render readback
- 首轮真实 RED 失败证据：
  - `cmake --build build --target test_picoui_native_progress`
  - 链接失败，缺少：
    - `picoui_native_progress_bar_get_rendered_state`
    - `picoui_native_progress_wheel_get_rendered_state`
- 该 RED 证明：
  - 当前仓库当时还没有 progress focused native readback
  - `P4-K` 所需的 native render smoke 证据链当时还不存在

### GREEN

- 新增：
  - `picoui/src/native/native_progress_bar.c`
  - `picoui/src/native/native_progress_wheel.c`
- 建立 progress 最小 native readback：
  - `picoui_native_progress_bar_render()`
  - `picoui_native_progress_bar_get_rendered_state()`
  - `picoui_native_progress_wheel_render()`
  - `picoui_native_progress_wheel_get_rendered_state()`
- 更新 `picoui/src/widgets/progress_bar.c`
  - `percent/horizontal/inverted` public truth 已收口到 `struct picoui_progress_bar` 自身
  - `set_percent()` 改为 clamp `0..100`
  - `get_percent()/get_horizontal()/get_inverted()` 不再回读 backend
- 更新 `picoui/src/widgets/progress_wheel.c`
  - `percent/dot_enabled` public truth 已收口到 `struct picoui_progress_wheel` 自身
  - `set_percent()` 改为 clamp `0..100`
  - `get_percent()/get_dot_enabled()` 不再回读 backend
- 更新 `picoui/src/native/native_render.c`
  - 将 `PICOUI_BACKEND_WIDGET_PROGRESS_BAR`
  - `PICOUI_BACKEND_WIDGET_PROGRESS_WHEEL`
  接入 timer-driven native render visit
- 更新测试与 build 注册：
  - `tests/picoui/unit/test_picoui_progress_bar.c`
  - `tests/picoui/unit/test_picoui_progress_wheel.c`
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`

### 验证

- `cmake --build build --target test_picoui_native_progress`
- `ctest --test-dir build -R '^test_picoui_native_progress$' --output-on-failure`
- `ctest --test-dir build -R '^test_picoui_progress_bar$' --output-on-failure`
- `ctest --test-dir build -R '^test_picoui_progress_wheel$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo progress_bar_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo progress_wheel_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo progress_bar_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo progress_wheel_basic --build-dir build`
- `git diff --check`

### 当前结论

- 当前 `P4-K progress_bar and progress_wheel` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `progress_bar` / `progress_wheel` 的 `percent` public truth 已收口到 PicoUI/widget 自身，不再依赖 `ldProgress*` getter/setter 回读
  - `clamp 0..100` 合同已由 focused native test 与 unit test 覆盖
  - focused native test 已走 `picoui_native_event_bind_root + picoui_timer_handler()` 主路径，并能读回 render snapshot
  - `progress_bar_basic` 与 `progress_wheel_basic` 的 runtime / visible gate 已通过
- 当前独立 review 已 `APPROVED`，且这轮最小修复已经证明：
  - 旧 `picoui_app_run -> picoui_backend_render -> ldGuiDraw` 路径在 draw 前会补跑 native render visit
  - `progress_wheel_basic` 的 host mirror 不再只依赖 timer-driven capture 路径
  - 该补点没有把 `progress_bar/progress_wheel` 的 public truth 重新绑回 backend setter success
- 当前 artifact 输出与宿主承载仍经现有 `picoui/src/backend/ldgui/backend_progress_bar.c`、`picoui/src/backend/ldgui/backend_progress_wheel.c` 与 `src/gui/ldProgress*` 路径完成；这表示 `P4-K` focused native contract 已建立，但不等于 `P7` 删除 ldgui runtime 依赖已经完成。
- 当前阶段下一任务推进到 `P4-L qrcode`。

## P4-L qrcode

- 时间：2026-06-06
- 范围：仅收口 `qrcode` 的 payload/ECC focused native test、module matrix readback、render smoke 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_qrcode.c`，覆盖：
  - create `qrcode`
  - set payload string
  - set ECC level
  - assert timer-driven render readback 前 `get_rendered_state == -1`
  - assert `module_count > 0`
- 首轮真实 RED 失败证据：
  - `cmake -S . -B build`
  - 失败点：
    - `cmake/LingDongGUI.cmake` 已接入 `picoui/src/native/native_qrcode.c`
    - 但该文件当时不存在
  - 典型错误：
    - `Cannot find source file: .../picoui/src/native/native_qrcode.c`
- 该 RED 证明：
  - focused native test 与 CMake 挂点已经开始要求 `P4-L` 最小 native 实现
  - 当前仓库当时还没有 `native_qrcode` 闭环

### GREEN

- 新增：
  - `picoui/src/native/native_qrcode.c`
- 建立 qrcode 最小 native readback：
  - `picoui_native_qrcode_render()`
  - `picoui_native_qrcode_get_rendered_state()`
  - `picoui_native_qrcode_reset_render_state()`
- 更新 `picoui/src/widgets/qrcode.c`
  - `picoui_qrcode_get_text()` 不再回读 backend getter
  - `text/ecc/max_version/zoom` 变更后会重置 native render state
- 更新 `picoui/src/native/native_render.c`
  - 将 `PICOUI_BACKEND_WIDGET_QRCODE` 接入 timer-driven native render visit
- 更新测试与 build 注册：
  - `tests/picoui/native/test_picoui_native_qrcode.c`
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`

### 验证

- `cmake -S . -B build`
- `cmake --build build --target test_picoui_native_qrcode`
- `ctest --test-dir build -R '^test_picoui_native_qrcode$' --output-on-failure`
- `ctest --test-dir build -R '^test_picoui_qrcode$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo qrcode_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo qrcode_basic --build-dir build`

### 当前结论

- 当前 `P4-L qrcode` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `qrcode` focused native test 已能覆盖 `payload + ECC + module_count`
  - `qrcode_basic` runtime / visible gate 已通过
  - `qrcode` 已接入 `picoui_native_event_bind_root + picoui_timer_handler()` 主路径
- 当前独立 review 已 `APPROVED`，并明确确认：
  - `payload string + ECC + module matrix exists` 的 focused native contract 已成立
  - `module_count > 0` 现在建立在 `qrcodegen_encodeText()` 的真实 encode 成功之上，不再是 version-derived 理论尺寸
  - 当前实现没有冒充 `P5` bitmap renderer 完成
- 当前 artifact 输出与宿主承载仍经现有 `picoui/src/backend/ldgui/backend_qrcode.c` / `src/gui/ldQRCode*` 路径完成；这表示 `P4-L` 的 focused native contract 已建立，但不等于 `P5` bitmap renderer 或 `P7` 删除 ldgui runtime 依赖已经完成。
- 当前阶段下一任务推进到 `P4-M radial_menu`。

## P4-M radial_menu

- 时间：2026-06-06
- 范围：仅收口 `radial_menu` 的 focused native test、angle hit-test、selected item public truth 与本阶段记录。

### RED

- 新增 `tests/picoui/native/test_picoui_native_radial_menu.c`，覆盖：
  - create radial menu
  - add menu sectors
  - timer-driven render readback
  - pointer press / release select sector
  - selected callback/index/id
  - backend value / native signal readback
- 首轮真实 RED 失败证据：
  - `cmake --build build --target test_picoui_native_radial_menu`
  - 先后暴露两层缺口：
    - `picoui/src/native/native_radial_menu.c` 使用了未声明的 `SIGNAL_CLICKED_ITEM`
    - focused test 直接解引用不透明类型但未引入内部结构定义

### GREEN

- 新增：
  - `picoui/src/native/native_radial_menu.c`
  - `tests/picoui/native/test_picoui_native_radial_menu.c`
- 更新：
  - `picoui/src/widgets/radial_menu.c`
  - `picoui/src/native/native_event.c`
  - `tests/picoui/CMakeLists.txt`
  - `cmake/LingDongGUI.cmake`
- 当前实现建立的最小 native contract：
  - `picoui_native_radial_menu_render()`
  - `picoui_native_radial_menu_get_rendered_state()`
  - `picoui_native_radial_menu_select_point()`
  - `radial_menu` 已接入 `picoui_native_event_bind_root + picoui_timer_handler()` 主路径
- focused test 当前证据边界：
  - 点击点不再调用被测 helper
  - 点击点也不再复写 native 扇区几何公式
  - 现在直接基于真实 host `ldRadialMenu_t::ptItemRegionList[].itemRegion` 中心取点，避免对 native hit-test 共模虚绿

### 验证

- `cmake --build build --target test_picoui_native_radial_menu`
- `ctest --test-dir build -R '^test_picoui_native_radial_menu$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo radial_menu_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo radial_menu_basic --build-dir build`
- `git diff --check`

### 当前结论

- 当前 `P4-M radial_menu` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `radial_menu` focused native test 已覆盖 add sectors、pointer select、selected callback/index/id、backend value/native signal
  - `native angle hit-test` 已建立在 PicoUI/native 自身椭圆/角度模型上，没有把选择语义交回 `ldRadialMenu*`
  - `press/release` 已走真实 runtime 主路径，release 才执行 sector selection
  - `add_item/add_item_with_source` 的尾部 selected mirror 已去掉，不再存在旧 fail-path host/local 分叉窗口
  - `radial_menu_basic` runtime / visible gate 已通过
- 当前独立 review 已 `APPROVED`，并明确确认：
  - focused test 已不再对 native hit-test 共模虚绿
  - `native_radial_menu.c` 的 angle hit-test 仍未回退到 `ldRadialMenu*` 选择语义
  - `tests/picoui/CMakeLists.txt` / `cmake/LingDongGUI.cmake` 中的非 P4-M 累积项属于前序脏改，不阻断本任务 closeout
- 当前 artifact 输出与宿主承载仍经现有 `picoui/src/backend/ldgui/backend_radial_menu.c` / `src/gui/ldRadialMenu*` 路径完成；这表示 `P4-M` 的 focused native contract 已建立，但不等于 `P7` 删除 ldgui runtime 依赖已经完成。
## P4-N `scroll_selecter`

### RED

- 新增 focused native test：
  - [tests/picoui/native/test_picoui_native_scroll_selecter.c](/Users/cys/embedded/LingDongGUI/tests/picoui/native/test_picoui_native_scroll_selecter.c)
  - [tests/picoui/CMakeLists.txt](/Users/cys/embedded/LingDongGUI/tests/picoui/CMakeLists.txt)
- 首轮真实 `RED`：
  - `cmake --build build --target test_picoui_native_scroll_selecter`
  - 链接失败，缺少：
    - `picoui_native_scroll_selecter_apply_scroll_delta`
    - `picoui_native_scroll_selecter_get_rendered_selected_index`
    - `picoui_native_scroll_selecter_get_rendered_selected_text`
    - `picoui_native_scroll_selecter_get_rendered_scroll_delta`

### GREEN

- 新增最小 native 实现：
  - [picoui/src/native/native_scroll_selecter.c](/Users/cys/embedded/LingDongGUI/picoui/src/native/native_scroll_selecter.c)
- wrapper 真值已收回 PicoUI/native：
  - [picoui/src/widgets/scroll_selecter.c](/Users/cys/embedded/LingDongGUI/picoui/src/widgets/scroll_selecter.c)
  - `selected_index` 不再回读 backend sync
  - `selected_text` 不再回读 backend getter
  - `edit_mode` 不再回读 backend getter
- render 主路径已接入：
  - [picoui/src/native/native_render.c](/Users/cys/embedded/LingDongGUI/picoui/src/native/native_render.c)
  - `PICOUI_BACKEND_WIDGET_SCROLL_SELECTER` 已进入 native render visit
- 真实 pointer 交互路径已接入：
  - [picoui/src/native/native_event.c](/Users/cys/embedded/LingDongGUI/picoui/src/native/native_event.c)
  - `press` 记录 `press_y`
  - `release` 计算 `delta_y = y - press_y`
  - 再走 `picoui_native_scroll_selecter_apply_scroll_delta()`
- focused test 当前证明：
  - 通过 `picoui_input_push_pointer(..., 1/0) + picoui_timer_handler()` 驱动真实主路径
  - 覆盖 `nearest-item snap` 半步阈值两侧：
    - `(item_step / 2) - 1` 不换项
    - `(item_step / 2) + 1` 换到最近项
  - `item_step` 从真实 host `ldScrollSelecter_t` 的 `itemSpace + font height` 读取，不再复写被测 rounding helper

### 验证

- `cmake --build build --target test_picoui_native_scroll_selecter`
- `ctest --test-dir build -R '^test_picoui_native_scroll_selecter$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo scroll_selecter_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo scroll_selecter_basic --build-dir build`
- `git diff --check`

### 当前结论

- 当前 `P4-N scroll_selecter` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `selected_index / selected_text / edit_mode` public truth 已收回 widget/native，自身不再由 backend getter/sync 驱动
  - `scroll delta` 已进入真实 pointer press/release 交互路径，而不是只停在 test helper
  - focused native test 已证明 `nearest-item snap` 的半步阈值行为
  - `scroll_selecter_basic` runtime / visible gate 已通过
- 当前独立 review 已 `APPROVED`，并明确确认：
  - truth 没有回退到 backend readback
  - `scroll delta` 已进入真实产品交互路径
  - focused test 已足够证明 `nearest-item snap`
- 当前 artifact 输出与宿主承载仍经现有 `picoui/src/backend/ldgui/backend_scroll_selecter.c` / `src/gui/ldScrollSelecter*` 路径完成；这表示 `P4-N` 的 focused native contract 已建立，但不等于 `P7` 删除 ldgui runtime 依赖已经完成。
- 当前阶段下一任务推进到 `P4-O table`。

## P4-O `table`

### RED

- 新增 focused native test：
  - [tests/picoui/native/test_picoui_native_table.c](/Users/cys/embedded/LingDongGUI/tests/picoui/native/test_picoui_native_table.c)
  - [tests/picoui/CMakeLists.txt](/Users/cys/embedded/LingDongGUI/tests/picoui/CMakeLists.txt)
- 首轮真实 `RED`：
  - `cmake --build build --target test_picoui_native_table`
  - 首次缺少：
    - `picoui_native_table_get_rendered_state`
    - `picoui_native_table_get_rendered_cell_text`
    - `picoui_native_table_get_item_center`
    - `picoui_native_table_select_point`

### GREEN

- 新增最小 native 实现：
  - [picoui/src/native/native_table.c](/Users/cys/embedded/LingDongGUI/picoui/src/native/native_table.c)
- 更新：
  - [picoui/src/widgets/table.c](/Users/cys/embedded/LingDongGUI/picoui/src/widgets/table.c)
  - [picoui/src/native/native_render.c](/Users/cys/embedded/LingDongGUI/picoui/src/native/native_render.c)
  - [picoui/src/native/native_event.c](/Users/cys/embedded/LingDongGUI/picoui/src/native/native_event.c)
  - [picoui/include/picoui/table.h](/Users/cys/embedded/LingDongGUI/picoui/include/picoui/table.h)
  - [picoui/src/core/internal.h](/Users/cys/embedded/LingDongGUI/picoui/src/core/internal.h)
  - [cmake/LingDongGUI.cmake](/Users/cys/embedded/LingDongGUI/cmake/LingDongGUI.cmake)
- 当前建立的最小 native contract：
  - `cell text` public truth 不再回读 backend getter
  - `current row/current column` public truth 不再回读 backend sync
  - `item width/item height/item region` layout truth 收回 PicoUI/native 自身
  - `table` 已接入 `picoui_native_event_bind_root + picoui_timer_handler()` 主路径
  - `pointer release` 已走 `native_event -> picoui_native_table_select_point()`
- focused test 当前证明：
  - `set cell text`
  - `set selected cell`
  - `readback`
  - `setter` 驱动 callback
  - `pointer` 选到新 cell `(1,2)` 后，`current cell + callback + backend packed value` 同步更新
  - 点击点不再调用被测 helper，而是直接基于真实 host `ldTableGetItemRegion(...)` 推导中心点，避免 table native hit-test 与 test 共模几何自证

### 验证

- `cmake --build build --target test_picoui_native_table`
- `ctest --test-dir build -R '^test_picoui_native_table$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo table_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo table_basic --build-dir build`
- `git diff --check`

### 当前结论

- 当前 `P4-O table` 已完成 closeout。
- 当前 fresh 证据已经证明：
  - `table` focused native test 已覆盖 `cell text/current cell` 的 public truth、timer-driven render readback、pointer select 新 cell、callback 与 backend packed value 同步
  - `item width/item height/item region` 已建立本地 layout truth，且 width/height host mirror 失败会 rollback，不再污染 PicoUI/native layout state
  - `table_basic` runtime / visible gate 已通过
- 当前独立 review 已 `APPROVED`，并明确确认：
  - 当前 worktree 在 `P4-O` 范围内已无违反 `implement table storage/layout without ldTable*` 的 blocker
  - focused test 现已足够证明 pointer 选中路径和 callback 更新
- 当前 artifact 输出与宿主承载仍经现有 `picoui/src/backend/ldgui/backend_table.c` / `src/gui/ldTable*` 路径完成；这表示 `P4-O` 的 focused native contract 已建立，但不等于 `P7` 删除 ldgui runtime 依赖已经完成。
- 当前阶段下一任务推进到 `P4-P animation and canvas`。

## P4-P `animation and canvas`

### RED

- 新增 focused native test：
  - [tests/picoui/native/test_picoui_native_animation_canvas.c](/Users/cys/embedded/LingDongGUI/tests/picoui/native/test_picoui_native_animation_canvas.c)
  - [tests/picoui/CMakeLists.txt](/Users/cys/embedded/LingDongGUI/tests/picoui/CMakeLists.txt)
- 首轮真实 `RED`：
  - `ctest --test-dir build -R '^test_picoui_native_animation_canvas$' --output-on-failure`
  - 失败点：
    - `frame_index == 1`
  - 调试证据表明：
    - 同一轮 `picoui_timer_handler()` 内，`animation` 在 native render 与 capture 链上发生重复推进
    - `canvas` 在 render 前 `render_ready == 0` 时，readback 不应提前暴露 command list

### GREEN

- 当前最小实现收口：
  - [picoui/src/native/native_animation.c](/Users/cys/embedded/LingDongGUI/picoui/src/native/native_animation.c)
  - [picoui/src/native/native_canvas.c](/Users/cys/embedded/LingDongGUI/picoui/src/native/native_canvas.c)
  - [picoui/src/widgets/animation.c](/Users/cys/embedded/LingDongGUI/picoui/src/widgets/animation.c)
  - [picoui/src/widgets/canvas.c](/Users/cys/embedded/LingDongGUI/picoui/src/widgets/canvas.c)
  - [picoui/src/backend/ldgui/backend_app.c](/Users/cys/embedded/LingDongGUI/picoui/src/backend/ldgui/backend_app.c)
- 当前建立的最小 native contract：
  - `animation`
    - `show_frame(0)` 只建立起始 frame truth 与 `start_ticks`
    - `picoui_timer_handler()` 的单次 timer/render 主路径只推进一次 frame
    - render readback 只在本轮 render 完成后可读
  - `canvas`
    - `fill_rect/draw_line/draw_circle` 已进入 portable command buffer
    - command count / command kind readback 只在 render 后可读
    - `canvas` host 已绑定，native render 可以基于真实 widget 访问 command buffer
- 为避免 `animation` 在同一 tick 被重复推进：
  - `picoui_backend_native_render_capture()` 改为复用本轮已完成的 native render 结果
  - 不再在 capture 路径再次调用 `picoui_native_render_once(...)`
- focused test 当前证明：
  - `sleep_ms(50) + picoui_timer_handler()` 后，`animation` frame 从 `0` 稳定推进到 `1`
  - `canvas` 在 render 前 readback 返回 `-1`
  - render 后可稳定读回 `3` 条命令，顺序分别是：
    - `fill_rect`
    - `draw_line`
    - `draw_circle`

### 验证

- `cmake --build build --target test_picoui_native_animation_canvas`
- `ctest --test-dir build -R '^test_picoui_native_animation_canvas$' --output-on-failure`
- `python3 tests/picoui/runtime/check_picoui_runtime.py --demo animation_basic --build-dir build`
- `python3 tests/picoui/runtime/check_picoui_visible_ui.py --demo animation_basic --build-dir build`
- `git diff --check`

### 当前结论

- 当前 `P4-P animation and canvas` 已按本阶段范围完成 closeout。
- 当前 fresh 证据已经证明：
  - `animation` 的 timer-driven frame truth 已进入 `picoui_timer_handler()` 主路径，且单次 tick 不再发生重复推进
  - `canvas` 的 portable command buffer / render-ready readback 已建立，focused test 已覆盖 `fill_rect/draw_line/draw_circle`
  - `animation_basic` runtime / visible gate 已通过
- 当前 artifact 输出与宿主承载仍经现有 `picoui/src/backend/ldgui/backend_animation.c`、`picoui/src/backend/ldgui/backend_canvas.c` 与 `src/gui/ldAnimation*` / `ldCanvas*` 路径完成；这表示 `P4-P` 的 focused native contract 已建立，但不等于 `P5` 的 `canvas primitive` / `animation frame source` 资源层去 ldgui 依赖已经完成。
- 当前阶段下一任务推进到 `P4-Q P4 closeout`。

## P4-Q `P4 closeout`

### 验证

- focused native tests：
  - `ctest --test-dir build -R '^(test_picoui_native_arc|test_picoui_native_calendar|test_picoui_native_clock|test_picoui_native_combo_box|test_picoui_native_date_time|test_picoui_native_gauge|test_picoui_native_graph|test_picoui_native_icon_slider|test_picoui_native_keyboard_line_edit|test_picoui_native_message_box|test_picoui_native_progress|test_picoui_native_qrcode|test_picoui_native_radial_menu|test_picoui_native_scroll_selecter|test_picoui_native_table|test_picoui_native_animation_canvas)$' --output-on-failure`
  - fresh 结果：`16/16 PASS`
- runtime smokes：
  - `arc_basic`
  - `calendar_basic`
  - `clock_basic`
  - `combo_box_basic`
  - `date_time_basic`
  - `gauge_basic`
  - `graph_basic`
  - `icon_slider_basic`
  - `keyboard_basic`
  - `line_edit_basic`
  - `message_box_basic`
  - `progress_bar_basic`
  - `progress_wheel_basic`
  - `qrcode_basic`
  - `radial_menu_basic`
  - `scroll_selecter_basic`
  - `table_basic`
  - `animation_basic`
  - fresh 结果：`18/18 PASS`
- visible smokes：
  - `arc_basic`
  - `calendar_basic`
  - `clock_basic`
  - `combo_box_basic`
  - `date_time_basic`
  - `gauge_basic`
  - `graph_basic`
  - `icon_slider_basic`
  - `keyboard_basic`
  - `line_edit_basic`
  - `message_box_basic`
  - `progress_bar_basic`
  - `progress_wheel_basic`
  - `qrcode_basic`
  - `radial_menu_basic`
  - `scroll_selecter_basic`
  - `table_basic`
  - `animation_basic`
  - fresh 结果：`18/18 PASS`

### 当前结论

- 当前 `P4` 已按阶段计划完成 closeout。
- 当前 fresh 证据已经证明：
  - 扩展控件 focused native contracts 已覆盖 `arc/calendar/clock/combo_box/date_time/gauge/graph/icon_slider/keyboard+line_edit/message_box/progress/qrcode/radial_menu/scroll_selecter/table/animation+canvas`
  - `P4` 计划要求的 focused native tests 已全部通过
  - `P4` 相关 demo runtime / visible smoke 已全部通过
- `P4` 的 closeout 只证明扩展控件 native state / event / render-readback 合同已经建立；不外推到 `P5` resource renderer 完成，也不外推到 `P7` 默认 ldgui runtime 依赖删除完成。
- 当前阶段下一任务推进到 `P5-A resource handle design contract`。
