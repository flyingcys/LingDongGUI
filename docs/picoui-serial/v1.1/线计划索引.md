# PicoUI v1.1 线计划索引

> 当前状态：`designing`
> 目标版本：`v1.1`

## 路线定义

`v1.1` 的目标不是继续维护 `v1.0-native` 的 `native/backend` 叠层，而是把 PicoUI 重构成真正独立、轻量、单心智的小型 GUI：

```text
picoui public API -> picoui widgets -> picoui core(hidden) -> arm2d/port
```

边界：

- 删除 `picoui/src/native/`
- 删除 `picoui/src/backend/`
- 删除 `picoui_app_*`
- `src/gui/ld*.c` 只作为算法参考，不进入默认 PicoUI 运行链
- 从 LingDongGUI 借来的代码必须改成 PicoUI 自己统一的函数风格
- PicoUI 保持“小型 GUI”定位，不走 LVGL 式大型框架路线

## 当前 spec

- `docs/picoui-serial/v1.1/00-重构设计.md`

## 计划文档索引

后续入口顺序：

1. 先读本文件确认 `v1.1` 目标与阶段。
2. 再读 `00-重构设计.md` 确认架构、删除项、迁移规则。
3. 实施阶段只读当前阶段文档，避免扩散到无关路线。

| 阶段 | 文档 | 用途 | 状态 |
| --- | --- | --- | --- |
| `C0` | `docs/picoui-serial/v1.1/00-重构设计.md` | 架构冻结、边界、迁移规则 | 已建立 |
| `C1` | 待建 | core 最小骨架 | 未开始 |
| `C2` | 待建 | vertical slice | 未开始 |
| `C3` | 待建 | layout 与公共机制 | 未开始 |
| `C4` | 待建 | 逐控件迁移 | 未开始 |
| `C5` | 待建 | 清场与 release gate | 未开始 |

## 当前阶段状态

- 当前阶段：`C0`
- 当前任务：`design`
- 已完成：`C0 design discussion`
- 未开始：`C1`、`C2`、`C3`、`C4`、`C5`

## 当前执行原则

- 严格按 `C0 -> C5` 串行推进。
- 非简单任务默认 subagent。
- 只允许从 `src/gui` 借算法，不允许带入 `ldBase/ldScene/ldMsg/SIGNAL/control_node` 宿主骨架。
- 所有 `v1.1` 文档统一写在 `docs/picoui-serial/v1.1/`。
