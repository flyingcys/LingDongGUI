# PicoUI v1.1 C5 清场与发布门禁实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删除 `picoui/src/native/` 与 `picoui/src/backend/`，清理默认构建和 demo/docs/test 残留，建立 v1.1 的最终 release gate。

**Architecture:** 这是收口阶段，不引入新 widget 语义。重点是 build graph、contract、demo、docs 和目录真相全部切到 v1.1。只有为满足 release gate 所必需的修正可以在本阶段进入。

**Tech Stack:** C11, CMake, SDL2, ARM-2D, Python contract checks, GitNexus detect_changes, `rtk cmake`, `rtk ctest`.

---

## Shared References

- 总计划：`docs/picoui-serial/v1.1/01-总实施计划.md`
- C4 记录：`docs/picoui-serial/v1.1/04-C4-阶段记录.md`
- 设计文档：`docs/picoui-serial/v1.1/00-重构设计.md`
- v1.1 索引：`docs/picoui-serial/v1.1/线计划索引.md`
- 现有构建：`cmake/LingDongGUI.cmake`
- 当前 docs：`picoui/docs/`, `docs/ability/`, `docs/picoui-serial/v1.0-native/`

---

## Task C5-A: no-native/no-backend 构建红线

**Files:**
- Create: `tests/picoui/contract/check_picoui_v1_1_no_native_backend.py`
- Modify: `tests/picoui/CMakeLists.txt`
- Modify: `cmake/LingDongGUI.cmake`
- Modify: `docs/picoui-serial/v1.1/05-C5-阶段记录.md`

- [ ] **Step 1: 写 RED checker**

新增 `tests/picoui/contract/check_picoui_v1_1_no_native_backend.py`：

```python
from pathlib import Path


ROOT = Path(__file__).resolve().parents[3]
TEXT = (ROOT / "cmake/LingDongGUI.cmake").read_text(encoding="utf-8")


def main() -> int:
    forbidden = [
        "picoui/src/native/",
        "picoui/src/backend/",
        "picoui_backend_ldgui",
        "picoui_backend_ldgui_runtime",
    ]
    for token in forbidden:
        assert token not in TEXT, f"default build still references {token}"
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: 注册 RED contract**

在 `tests/picoui/CMakeLists.txt` 中加入：

```cmake
ld_add_python_test(check_picoui_v1_1_no_native_backend
    SCRIPT "${CMAKE_CURRENT_SOURCE_DIR}/contract/check_picoui_v1_1_no_native_backend.py"
    LABELS "picoui;contract;v1_1;release"
)
```

- [ ] **Step 3: 运行 RED**

```bash
rtk ctest --test-dir build -R check_picoui_v1_1_no_native_backend --output-on-failure
```

- [ ] **Step 4: 清理默认构建图**

在 `cmake/LingDongGUI.cmake` 中：

- 删除 `picoui/src/native/*` source list
- 删除 `picoui/src/backend/ldgui/*` source list
- 删除 `picoui_backend_ldgui` / `picoui_backend_ldgui_runtime` / `picoui_legacy_ldgui*`
- 让默认 `picoui` 只链接 `picoui_core_v1_1 + picoui_widgets_v1_1 + picoui_port_v1_1`

最小目标片段应形如：

```cmake
    add_library(picoui INTERFACE)
    target_link_libraries(picoui INTERFACE
        picoui_core_v1_1
        picoui_widgets_v1_1
        picoui_port_v1_1
    )
```

- [ ] **Step 5: GREEN**

```bash
rtk ctest --test-dir build -R check_picoui_v1_1_no_native_backend --output-on-failure
rtk git diff --check
```

- [ ] **Step 6: Commit**

```bash
git add \
  tests/picoui/contract/check_picoui_v1_1_no_native_backend.py \
  tests/picoui/CMakeLists.txt \
  cmake/LingDongGUI.cmake \
  docs/picoui-serial/v1.1/05-C5-阶段记录.md
git commit -m "build: drop picoui native backend defaults"
```

## Task C5-B: 真删目录与 include 残留

**Files:**
- Delete: `picoui/src/native/`
- Delete: `picoui/src/backend/`
- Create: `tests/picoui/contract/check_picoui_v1_1_no_legacy_includes.py`
- Modify: `tests/picoui/CMakeLists.txt`

- [ ] **Step 1: 写 RED include checker**

新增 `tests/picoui/contract/check_picoui_v1_1_no_legacy_includes.py`：

```python
from pathlib import Path


ROOT = Path(__file__).resolve().parents[3]


def main() -> int:
    for path in (ROOT / "picoui/src").rglob("*.c"):
        text = path.read_text(encoding="utf-8")
        for token in ("backend/ldgui", "picoui_backend_", "ldMsg", "SIGNAL_"):
            assert token not in text, f"{path.relative_to(ROOT)} still contains {token}"
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: 运行 RED**

```bash
rtk ctest --test-dir build -R check_picoui_v1_1_no_legacy_includes --output-on-failure
```

- [ ] **Step 3: 删除目录并修残留**

执行：

- 删除 `picoui/src/native/`
- 删除 `picoui/src/backend/`
- 修正 `picoui/src/widgets/*.c` 中所有 legacy include/token

- [ ] **Step 4: GREEN**

```bash
rtk ctest --test-dir build -R check_picoui_v1_1_no_legacy_includes --output-on-failure
rtk git diff --check
```

- [ ] **Step 5: Commit**

```bash
git add -A picoui/src tests/picoui/contract/check_picoui_v1_1_no_legacy_includes.py tests/picoui/CMakeLists.txt
git commit -m "refactor: remove picoui legacy runtime directories"
```

## Task C5-C: demo 与 runtime gate 清场

**Files:**
- Modify: `picoui/demo/*/main.c`
- Create: `tests/picoui/runtime/check_picoui_v1_1_demo_matrix.py`
- Create: `tests/picoui/runtime/picoui_v1_1_demo_matrix.json`
- Modify: `tests/picoui/CMakeLists.txt`

- [ ] **Step 1: 写 RED demo matrix**

新增 `tests/picoui/runtime/picoui_v1_1_demo_matrix.json`，至少列出每个 demo：

```json
{
  "basic_widgets": {"main_style": "v1_1", "artifact": "required"},
  "hello_world": {"main_style": "v1_1", "artifact": "required"}
}
```

新增 checker，验证：

- 所有 demo `main.c` 不含 `picoui_app_`
- 运行矩阵中的 demo target 均可启动

- [ ] **Step 2: 运行 RED**

```bash
rtk python3 tests/picoui/runtime/check_picoui_v1_1_demo_matrix.py
```

- [ ] **Step 3: 修 demo skeleton 和矩阵**

每个 demo `main.c` 必须遵循：

```c
int main(void)
{
    if (picoui_init() != 0) {
        return 1;
    }
    if (picoui_sdl_hal_init(320, 480) != 0) {
        picoui_deinit();
        return 1;
    }
    create_demo_ui();
    while (1) {
        int rc = picoui_timer_handler();
        if (rc != 0) {
            break;
        }
    }
    picoui_deinit();
    return 0;
}
```

- [ ] **Step 4: GREEN**

```bash
rtk python3 tests/picoui/runtime/check_picoui_v1_1_demo_matrix.py
rtk git diff --check
```

- [ ] **Step 5: Commit**

```bash
git add \
  picoui/demo \
  tests/picoui/runtime/check_picoui_v1_1_demo_matrix.py \
  tests/picoui/runtime/picoui_v1_1_demo_matrix.json \
  tests/picoui/CMakeLists.txt
git commit -m "test: add picoui v1.1 demo runtime gate"
```

## Task C5-D: 用户文档与 serial 文档清场

**Files:**
- Modify: `picoui/docs/quick_start.md`
- Modify: `picoui/docs/api_overview.md`
- Modify: `picoui/docs/porting_rules.md`
- Modify: `docs/ability/*.md`
- Modify: `docs/picoui-serial/README.md`
- Modify: `docs/picoui-serial/v1.1/线计划索引.md`

- [ ] **Step 1: docs RED grep**

Run:

```bash
rtk rg -n "picoui_app_|native runtime|backend ldgui|wrapper" picoui/docs docs/ability docs/picoui-serial
```

Expected:

- 先命中旧说法。

- [ ] **Step 2: 重写用户入口文档**

`picoui/docs/quick_start.md` 至少写出：

```markdown
1. `picoui_init()`
2. `picoui_sdl_hal_init(width, height)`
3. `create_demo_ui()`
4. `picoui_timer_handler()` loop
```

`picoui/docs/api_overview.md` 要明确：

- `picoui_app_*` 已移除
- 只有 `picoui` 一套 public API

`docs/picoui-serial/README.md` 要把当前路线指向 `v1.1`。

- [ ] **Step 3: GREEN grep**

```bash
rtk rg -n "picoui_app_|native runtime|backend ldgui|wrapper" picoui/docs docs/ability docs/picoui-serial/v1.1
rtk git diff --check
```

Expected:

- `v1.1` 文档中不再命中旧术语

- [ ] **Step 4: Commit**

```bash
git add picoui/docs docs/ability docs/picoui-serial/README.md docs/picoui-serial/v1.1/线计划索引.md
git commit -m "docs: switch picoui docs to v1.1 route"
```

## Task C5-E: release matrix 与最终 closeout

**Files:**
- Create: `tests/picoui/contract/picoui_v1_1_release_matrix.json`
- Create: `tests/picoui/contract/check_picoui_v1_1_release_matrix.py`
- Modify: `tests/picoui/CMakeLists.txt`
- Modify: `docs/picoui-serial/v1.1/05-C5-阶段记录.md`
- Modify: `docs/picoui-serial/v1.1/线计划索引.md`

- [ ] **Step 1: 写 RED release matrix**

矩阵至少包含：

- `core` gate
- `widget groups` gate
- `demo` gate
- `no-native-backend` gate
- `docs switched to v1.1` gate

checker 要验证每项状态只允许 `covered` 或 `non_goal`。

- [ ] **Step 2: 运行 RED**

```bash
rtk ctest --test-dir build -R check_picoui_v1_1_release_matrix --output-on-failure
```

- [ ] **Step 3: 写 release matrix JSON**

示例骨架：

```json
{
  "version": "v1.1",
  "gates": {
    "core": "covered",
    "widget_groups": "covered",
    "demo_runtime": "covered",
    "no_native_backend": "covered",
    "docs_v1_1": "covered"
  }
}
```

- [ ] **Step 4: 跑最终验证集**

Run:

```bash
rtk cmake -S . -B build-v1_1-release -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
rtk cmake --build build-v1_1-release
rtk ctest --test-dir build-v1_1-release -L picoui --output-on-failure
rtk python3 tests/picoui/contract/check_picoui_v1_1_boundary_contract.py
rtk python3 tests/picoui/contract/check_picoui_v1_1_build_graph.py
rtk python3 tests/picoui/contract/check_picoui_v1_1_no_native_backend.py
rtk python3 tests/picoui/contract/check_picoui_v1_1_no_legacy_includes.py
rtk python3 tests/picoui/runtime/check_picoui_v1_1_demo_matrix.py
rtk git diff --check
```

- [ ] **Step 5: 主线程跑 detect_changes**

```text
mcp__gitnexus.detect_changes({repo:"LingDongGUI", scope:"all"})
```

- [ ] **Step 6: 更新 closeout 与索引**

在 `docs/picoui-serial/v1.1/05-C5-阶段记录.md` 写：

```markdown
## Closeout

- `C5 gate`：PASS
- `v1.1` 状态：ready
- 默认 PicoUI 路线：`v1.1`
```

在 `docs/picoui-serial/v1.1/线计划索引.md` 中：

- 把 `C5` 改为 `已完成`
- 把顶部状态改为 `v1.1 ready`

- [ ] **Step 7: Commit**

```bash
git add \
  tests/picoui/contract/picoui_v1_1_release_matrix.json \
  tests/picoui/contract/check_picoui_v1_1_release_matrix.py \
  tests/picoui/CMakeLists.txt \
  docs/picoui-serial/v1.1/05-C5-阶段记录.md \
  docs/picoui-serial/v1.1/线计划索引.md
git commit -m "docs: close picoui v1.1 release gate"
```
