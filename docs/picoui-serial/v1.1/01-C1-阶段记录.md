# PicoUI v1.1 C1 阶段记录

## 当前目标

- 冻结删除边界
- 建立新的 core/header/build 骨架
- 为 C2 垂直切片准备最小着陆点

## 当前红线

- `picoui_app_*` 全部删除，不做兼容层
- `picoui/src/native/` 与 `picoui/src/backend/` 将在 `C5` 真删，但 `C1` 起不再允许新代码依赖它们
- 不允许把 `ldBase/ldScene/ldMsg/SIGNAL/control_node` 搬进 PicoUI 新骨架

## 当前状态

- `C1` 进行中
- `C1 gate` 尚未 closeout

## 当前完成

- `picoui_v1_1_boundary_contract` 已冻结 `picoui_app_*`、`src/native`、`src/backend` 删除边界
- `picoui/include/picoui/core.h`、`picoui/include/picoui/screen.h`、`picoui/include/picoui/input.h` 作为 `v1.1` 最小 lifecycle 入口头集合已接线
- `picoui/include/picoui/display_v1_1.h` 与 `picoui/include/picoui/port/sdl_v1_1.h` 作为 `v1.1` 最小 port/display 公开头已接线
- `picoui_core_v1_1`、`picoui_widgets_v1_1`、`picoui_port_sdl_v1_1`、`picoui_port_v1_1`、`picoui_v1_1` 已建立
- `picoui/src/core/{core,screen,input,runtime_state}.c|h` 只挂到 `v1.1` target，不与默认 `picoui` 目标混链
- `tests/support/CMakeLists.txt` 已新增 `picoui_v1_1_test_support`，普通 support 仍只链默认 `picoui`

## 当前约束

- 当前入口仍保持默认 `picoui = v1.0-native`，`v1.1` 仅通过独立 skeleton target 挂载
- `test_picoui_v1_1_public_headers` 只验证 `core/screen/input` 最小 lifecycle skeleton，不把旧 umbrella `picoui/picoui.h` 作为 `C1` gate 证据
- `v1.1 port` 当前只证明 `SDL HAL + default display + pointer indev` 最小接线，不把 `picoui_port_sdl_attach(struct picoui_app *)` 当作 `C1` 完成证据

## Closeout

- `C1 gate`：PASS
- 下一阶段：`C2`
- 允许进入 `window/label/button` 垂直切片
