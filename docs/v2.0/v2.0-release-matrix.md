# TinyUI v2.0 Release Matrix

## 文档状态

- 状态：`P6` release-facing 真相矩阵已完成；不等于放宽保守边界
- 日期：`2026-06-10`
- 判定原则：只陈述当前 checkout 已有证据，不替代 fresh gate 输出
- 作用域：本文是 `v2.0` release-facing 历史真相源，只用于回看 `v2.0/P0-P6` 是否闭环；不应覆盖当前 `v2.1` 对 `tests/tinyui/*` canonical truth 的定义

## 当前矩阵

| 领域 | 当前状态 | 真相源 | 保守边界 |
| --- | --- | --- | --- |
| 路线与阶段边界 | 已完成 | `docs/v2.0/2026-06-07-tinyui-v2-0-design.md`、`docs/v2.0/plans/v2.0-orchestration-plan.md`、`docs/v2.0/plans/stages/README.md` | 证明路线和阶段定义已固定，不等于所有阶段自动完成 |
| `P0` 基线与迁移守门 | 已完成 | `docs/v2.0/2026-06-07-tinyui-v2-0-baseline-inventory.md`、`tests/picoui/contract/check_picoui_tinyui_transition_guards.py` | 证明 baseline 与 transition guard 已存在，不等于后续阶段可跳过 fresh gate |
| `P1` shared/core flatten | 已完成 | `docs/v2.0/plans/stages/README.md`、`picoui/src/core/widget.c`、`picoui/src/core/runtime_bridge.c` | 证明 shared architecture 已拆平，不等于 `backend_*.c` 文件已全部移除 |
| `P2` 试点控件直连 | 已完成 | `docs/v2.0/plans/stages/README.md`、`picoui/src/widgets/window.c`、`label.c`、`button.c`、`switch.c` | 只覆盖 `window/label/button/switch`，不外推到所有控件 |
| `P3` app-free runtime 主路径 | 已完成 | `docs/v2.0/plans/stages/README.md`、`picoui/include/picoui/runtime.h`、`picoui/src/core/runtime.c`、`picoui/demo/basic_widgets/main.c` | 证明用户主路径已不依赖 `app`，不等于 `app.h/app.c` 已删除 |
| `P4` `tinyui_*` public surface | 仍处过渡态 | `tinyui/include/*`、`picoui/include/picoui/runtime.h`、`widget.h`、`label.h`、`button.h`、`switch.h` | 证明试点 `tinyui_*` surface 与 compatibility bridge 已存在，不等于全量 `picoui_*` 已退场 |
| `P5` 性能/体积/RAM 守门 | 已完成 | `docs/v2.0/v2.0-performance-baseline.md`、`tests/picoui/perf/picoui_tinyui_perf_baseline.json`、`tests/picoui/perf/check_picoui_tinyui_*.py` | 只证明三条窄口径 guard 与 `P6 Blocking Policy` 已闭环，不外推成完整首帧或整体 RAM 证明 |
| `P6` closeout / release truth | 已完成 | `docs/v2.0/v2.0-closeout.md`、本文件、`docs/v2.0/线计划索引.md`、`tests/picoui/contract/check_picoui_release_capability_matrix.py` | 证明 closeout/release 真相源、broad gate 与 contract wording 已收口，不等于全量 public API 已切换或历史文件已清零 |

## 真实 gate

- broad `picoui`：
  - `rtk ctest --test-dir build -L 'picoui' --output-on-failure`
- runtime / visible / backend mapping：
  - `rtk ctest --test-dir build/picoui-runtime -R 'check_picoui_runtime|check_picoui_visible_ui|check_picoui_backend_mapping' --output-on-failure`
- perf / size / memory：
  - `rtk ctest --test-dir build -R 'check_picoui_tinyui_binary_size|check_picoui_tinyui_perf|check_picoui_tinyui_object_overhead' --output-on-failure`
- transition guard：
  - `rtk ctest --test-dir build -R '^check_picoui_tinyui_transition_guards$' --output-on-failure`
- 文档格式：
  - `git diff --check`

## 当前 release 口径

- 当前可以写的保守结论：
  - `TinyUI v2.0` 已完成 `P0` 到 `P6` 当前定义范围内的仓库内证据收口
  - `P5` 的三条性能守门线与 `P6` 的 release-facing truth/gates 都已在当前 checkout fresh 通过
- 当前不能写的超额结论：
  - “`TinyUI v2.0` 已完成全量 public API 切换并可无条件发布”
  - “所有 backend/app 历史痕迹都已清零”
  - “性能、首帧、RAM、所有控件开销都已被完整量化证明”
